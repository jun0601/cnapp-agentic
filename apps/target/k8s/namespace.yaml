# 타깃 앱 공용 네임스페이스. product/order/member 매니페스트가 namespace: shop을 가정하므로
# 이걸 먼저 apply(또는 ArgoCD App의 syncOptions: CreateNamespace=true)해야 한다.
apiVersion: v1
kind: Namespace
metadata:
  name: shop
  labels:
    app.kubernetes.io/part-of: cnapp-target
    warning: intentionally-vulnerable-demo
