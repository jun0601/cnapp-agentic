# 작업 로그 (트러블슈팅 + 진행)

> **이 파일의 역할:** 구현하면서 겪은 **문제·해결(트러블슈팅)**과 **추가·작업한 것(진행)**을 한 줄씩 누적하는 *중앙 운영 로그*. 설계 문서(`docs/`)가 "무엇을 만들지", 이 파일은 "만들면서 실제로 무슨 일이 있었는지".
>
> **왜 영역별 파일이 아니라 중앙 1개인가:** 둘 다 전 영역을 만지므로 중앙집중이 전체 그림을 주고, `[영역]` 태그로 영역별 필터(Ctrl+F `[engine]`)가 가능해 per-area 파일 없이도 분리된다. 흩어놓으면 *영역을 가로지르는 패턴*(예: 권한 문제가 여러 영역에서 반복)을 못 본다.
>
> **기입 형식 (한 줄):** `YYYY-MM-DD / 작성자(준형·진우) / [영역] / 내용`
> **영역 태그:** `[infra]` `[scanners]` `[pipeline]` `[engine]` `[rag]` `[attackpath]` `[apps-target]` `[apps-console]` `[contracts]` `[docs]` `[ci]`
>
> **규칙:** 최신이 위로. 트러블슈팅은 *문제 → 해결*을 한 줄에. 진행 로그는 *무엇을 추가/완료했는지*. 굵직한 설계 변경은 여기 말고 `CLAUDE.md` 변경 로그에(둘은 목적이 다름 — 여기는 작업 디테일, 거기는 pull 알림).

---

## 1. 트러블슈팅 로그 (문제 → 해결)

- `2026-07-04 / 준형 / [engine][rag] 목업만이던 "핵심 조사·설명"을 실 배선 — ① RealToolExecutor가 S3 2종만 실구현이라 골든 경로의 IAM(f4)·SG(f3)·PII(f7) finding을 실 조사 못 함 → 계약④ AWS allowlist 9종 전부 구현(LLM 플래너 toolConfig는 이미 enum으로 전종 노출해 즉시 정합). Macie/AccessAnalyzer 미활성 계정이면 ClientError를 잡아 중립결과로 강등(라이브 크래시 방지). 가짜 boto3 세션으로 9종+강등 오프라인 검증(캐시된 클라이언트 때문에 강등 테스트가 처음엔 안 먹혀 ex._clients.pop으로 캐시 비우고 재검증 — 클라이언트 지연캐싱은 올바른 동작). ② rag retriever 실검색이 NotImplementedError → Titan Embed v2(적재 CorpusLoader와 동일 모델 필수=벡터 정합)로 쿼리 임베딩 → pgvector `embedding <=> vec` cosine top_k. ③ console-backend /chat 에코 → 같은 파이프라인 TS판(bedrock-runtime InvokeModel 임베딩 + pg cosine + Sonnet Converse). 전 회귀 무영향(전부 mock=False 실경로라 목업 안 건드림)`
- `2026-07-03 / 준형 / [apps-console][infra] console-backend real pg 경로가 배포돼도 런타임 module not found 위험 — infra/console archive_file은 dist/만 zip(node_modules 제외)하는데 tsc 산출물은 pg를 런타임 require로 남김. use_mock 기본값이 false라 실 pg가 기본 경로. 해결: 빌드 tsc→esbuild 번들(--bundle --external:@aws-sdk/* — pg는 dist/handler.js에 인라인, aws-sdk는 Node20 런타임 제공이라 external) + clean 단계로 stale 산출물 제거. dist=단일 handler.js 187KB(pg-pool 인라인 확인). data.ts 동기→async + handler await + Secrets Manager 자격증명(engine/pipeline Lambda 동일 패턴)`
- `2026-07-03 / 준형 / [contracts][infra] Lambda 실코드 패키징 데이터파일 경로 함정 — 각 패키지가 contracts를 __file__ 기준 상대경로로 로드하는데 zip에 패키지만 담으면 런타임에 못 찾음. 해결: build_lambdas.py가 zip 루트에 패키지+contracts/를 나란히 복사(상대경로 해석 그대로) → 코드 무변경. psycopg2는 C확장이라 로컬(win/mac) 설치본을 zip하면 Lambda(리눅스) import 실패 → pip --platform manylinux2014_x86_64 --only-binary로 리눅스 휠 강제, .so가 x86_64-linux-gnu인지 file로 검증`
- `2026-07-03 / 준형 / [rag] RAG answer_gen model ID가 bare name(anthropic.claude-sonnet-4-5)이라 실 Bedrock 404 — mock만 돌아 안 드러남. aws bedrock list-inference-profiles로 실 Global 프로파일 확정(global.anthropic.claude-sonnet-4-5-20250929-v1:0). 진우 파일이라 CLAUDE 변경로그 [PULL 필요] 플래그`
- `2026-07-03 / 준형 / [ci] npm을 PowerShell 파이프(| Select-Object) 중간에 물리면 "Cannot run a document in the middle of a pipeline"(npm은 .cmd 문서) — 파이프 없이 실행하거나 cmd /c "npm ..."로 감쌀 것. 이 세션 반복 실수라 기록`
- `2026-07-03 / 준형 / [infra] backend destroy에서 Lambda Hyperplane ENI가 SG를 ~25분 붙잡음(실측) — normalize Lambda ENI 2개가 함수 삭제 후에도 in-use 유지, SG 삭제가 계속 재시도. AWS API로 추적: in-use→available 전환까지 ~25분, available 되는 즉시 aws ec2 delete-network-interface 수동 삭제 → terraform SG 재시도가 수십 초 내 통과(in-use 동안엔 Lambda 서비스 소유라 강제삭제 불가=대기 유일). 코드 하드닝: backend·console Lambda SG에 timeouts{delete="40m"}(기본 15분이면 실패 후 재실행 필요) + 가속절차 infra/README 기록`
- `2026-07-03 / 준형 / [infra] shared destroy 막바지 IGW detach가 DependencyViolation("has some mapped public addresses") — IGW 삭제 재시도가 NAT 인스턴스 종료(41초)와 레이스해 public IP 매핑이 남은 시점에 마지막 시도를 침. NAT 종료 후 재-destroy(-Layer shared)로 즉시 통과(잔여 2개). 재발 방지: aws_instance.nat에 depends_on=[module.vpc] — destroy 역순에서 NAT가 IGW보다 항상 먼저 완전 삭제되어 레이스 자체 소멸(생성 순서 무해)`
- `2026-07-03 / 준형 / [infra] Karpenter 1.1.1 컨트롤러가 K8s 1.34에서 시작 즉시 panic — 라이브 풀사이클 apply에서 CrashLoopBackOff, kubectl logs로 원인 즉시 확정("karpenter version is not compatible with K8s version 1.34"). 교훈: 차트 버전은 '존재 확인'(helm show chart --version)이 아니라 'K8s 호환성 매트릭스'를 봐야 함 — 1.1.1은 존재하지만 2024년 릴리스라 1.34 미지원. helm show chart로 최신(1.13.0) 확인 후 helm upgrade → 1/1 Running. 이후 t3.micro 스팟 프로비저닝(~30초)·consolidation 회수까지 전체 수명주기 실검증 완료`
- `2026-07-03 / 준형 / [infra] helm_release wait=true가 시작-즉시-panic형 CrashLoop을 못 잡고 성공 통과 — 1.1.1 사고 때 컨트롤러가 크래시루프인데 helm wait가 통과해 terraform apply가 green(레이어 22개 생성 완료)으로 끝남. "terraform green ≠ 컨트롤러 healthy" — 클러스터 런타임 레이어는 apply 후 kubectl로 파드 1/1 Running 확인이 필수 검증 단계(infra/karpenter/README §4.1에 명시)`
- `2026-07-03 / 준형 / [infra] helm은 upgrade 때 CRD를 안 올림(install 때만) — 1.1.1→1.13.0 in-place 업그레이드 후 컨트롤러가 "unknown field status.nodes" 경고(구버전 CRD 스키마에 새 필드 없음). fresh apply(destroy→apply)에선 발생 안 함. in-place 업그레이드 시엔 릴리스 CRD를 kubectl로 별도 적용 필요`
- `2026-07-03 / 준형 / [infra] terraform-aws-modules eks//karpenter v20 권한셋에 Karpenter 1.13 instance-profile GC가 쓰는 iam:ListInstanceProfiles 없음 — 매 리컨실 AccessDenied ERROR(프로비저닝은 무영향, GC만 실패). infra/karpenter에 /karpenter/* 경로 스코프 보충 정책 1문 추가 → 최근 3분 ERROR 0 확인`
- `2026-07-03 / 준형 / [infra][ci] infra/deploy.ps1(순서강제 실행기)이 스크립트 안에서만·간헐적으로 terraform init 실패 — 증상: $LASTEXITCODE 빈값+terraform 출력 0줄인데 "[layer] init failed" throw(직접 실행은 항상 성공). 진단: dir·pwd·mainexists 전부 정상인데 init exit=[](빈값=native 명령이 실행조차 안 됨)을 계측으로 확인. 근본원인 = Write 도구가 만든 .ps1이 BOM 없는 UTF-8인데 Windows PowerShell 5.1은 이를 시스템 코드페이지(한글=cp949)로 읽음 → 한글 주석의 UTF-8 멀티바이트가 cp949 lead-byte로 오독되며 바로 다음 코드 라인(& terraform init)을 주석으로 삼켜버림 → 명령 미실행. 편집할 때마다 바이트패턴이 바뀌어 간헐적으로 보였음(mojibake 콘솔출력 ?덉씠???쒖꽌가 cp949 오독의 증거였음). 해결: 스크립트 전체를 ASCII 전용으로 재작성(순수 ASCII는 cp949·UTF-8 바이트 동일이라 mojibake 불가) — 영어 주석+영어 메시지, 상세 한글설명은 infra/README §3.5로. 재작성 후 전 6레이어 validate Success. 부수: EAP='Stop'이 terraform stderr를 종료에러로 바꿔 exit code 오염 → 'Continue'로(제어는 명시적 throw), $Profile 예약변수 충돌 회피로 $AwsProfile 리네임, -chdir=<한글절대경로> 인자 대신 Push-Location 사용(exe 인자 인코딩 회피)`
- `2026-07-03 / 준형 / [infra] Karpenter가 infra/shared에 결합돼 apply/destroy 취약 — shared(순수 AWS 폴대)가 helm/kubectl provider+라이브 EKS에 의존, 컨트롤러 helm이 context deadline exceeded로 안 뜨면 shared 전체 apply 실패하고 destroy 때 helm_release를 state rm으로 손봐야 했음. 해결: karpenter.tf를 새 레이어 infra/karpenter로 분리(shared엔 discovery 태그만, local.cluster_name으로 3참조 묶음), 컨트롤러 replicas 2→1(2노드 anti-affinity 스케줄압박 완화)·timeout 600→900. karpenter는 shared를 remote_state로 참조+data.aws_eks_cluster로 엔드포인트/CA 직접조회. 독립 리뷰 에이전트로 교차의존 clean 확인(참조 output 전부 실재)`
- `2026-07-03 / 준형 / [infra] gitops/autoscaling/karpenter.yaml이 infra/karpenter와 NodePool/EC2NodeClass 중복 정의 + instance-category t,m,c/size small,medium,large라 프리티어 계정에서 RunInstances 거부될 값이었음(infra/karpenter는 t3.small/micro로 올바르게 제한). 노드층 Karpenter는 이제 infra/karpenter가 SSOT라 gitops yaml 삭제(gitops는 HPA=파드층만 유지), gitops/README 부트스트랩·폴더트리 갱신`
- `2026-07-03 / 진우 / [infra] CloudTrail update-trail이 CloudWatchLogsLogGroupArn에 로그그룹 순수 ARN을 주면 InvalidCloudWatchLogsLogGroupArnException으로 거부함 — 로그 스트림 와일드카드 :* 접미사가 붙은 형태를 요구(terraform output이 주는 값은 순수 로그그룹 ARN이라 그대로 못 씀). :* 붙여서 재시도 후 성공`
- `2026-07-03 / 진우 / [infra] Windows(cp949) 콘솔에서 aws cloudwatch get-dashboard가 한국어 위젯 제목의 em-dash(—, U+2014)를 못 인코딩해 크래시 — PYTHONUTF8=1 PYTHONIOENCODING=utf-8 환경변수로 우회(다른 스크립트의 sys.stdout.reconfigure(encoding="utf-8")와 같은 계열 문제)`
- `2026-07-03 / 진우 / [scanners][docs] "Inspector·kube-bench는 infra apply 후"라는 README 문구가 둘을 뭉뚱그린 오기재였음 — scanners/cspm/cspm.py의 scan_securityhub()를 다시 읽어보니 Security Hub get_findings()가 제품 필터 없이 계정 내 모든 FAILED finding을 가져와서 Inspector 발견사항도 ASFF 공통 파서로 이미 커버되고 있었음(별도 스캐너 코드 불필요, 계정에서 서비스만 켜면 됨). kube-bench는 반대로 진짜 미작성이었지만 "EKS 있어야 한다"는 것도 절반만 맞음 — 실 클러스터 스캔(scan_cluster)만 EKS 필요, mock 경로(scan_from_json)는 Trivy처럼 지금 바로 만들 수 있었음. README(scanners·루트) 정정 + kube-bench 스캐너 신설(아래 진행 로그)`

- `2026-07-03 / 진우 / [attackpath][pipeline] 준형 판단(a·b) 반영 — n5 pillar 하드코딩 제거 + Trivy dedup_key 2세그먼트 수정. a) correlation.py Node("n5",...) 의 pillar를 r5.get("pillar","ciem")로 도출(하드코딩 제거) — _R5_ENTRA_APP이 ciem/cspm 두 control을 매칭 대상으로 두는데 DB 쿼리 순서 무관하게 항상 정확한 pillar 반영. b) normalizer.py _parse_trivy() dedup_key를 resource_id|control_id|cve(3세그) → resource_id|control_id(2세그, 계약① 정의)로 수정, title도 "KEV-listed CVE ({cve}) in {artifact} image"로 골든톤 정합 — 이미지당 CVE 여러 개면 이제 finding 1건으로 dedup되고 sources에 CVE 누적. 부수: scanners/workload/run_demo.py 골든검증이 옛 3건 기대값 하드코딩돼있어 FAIL 뜸 → 1건 dedup 기대값으로 갱신, sources[0] 인덱스 의존 체크(dedup 후 set merge라 순서 비보장)를 any() 순회로 수정. run_demo 10개+contracts validate+py_compile 재통과`

- `2026-07-03 / 진우 / [infra][engine][scanners][contracts][apps-console] 전체 레포 재검증(자동 회귀 통과 후 코드 재독해) — 병렬 리뷰 4개(스캐너·파이프라인·RAG·attackpath / 엔진 / 앱3종 / infra전체) + jsonschema 라이브러리로 mock 데이터 직접 스키마검증. 실버그 6건 발견·수정: ①infra/target member_pii_bucket이 계정ID 접미사 없어 apply 시 전역 이름충돌 실패 가능(front/audit 버킷과 다른 컨벤션) → 접미사 추가 + k8s manifest ACCOUNT_ID placeholder(기존 IRSA arn과 동일 패턴) ②scanners/cspm scan_prowler()가 CLI는 `-M json-ocsf`로 실행하면서 envelope는 source_format="prowler-json"으로 잘못 태깅해 정규화부가 엉뚱한 파서(_parse_prowler)로 라우팅 → 실 스캔결과 전부 INTERNAL-UNKNOWN-001로 깨지는 버그(AWS CSPM+Azure CIEM 실경로 둘 다 영향, mock은 scan_from_json이 format 직접 지정해 우회라 안 걸림) → "ocsf"로 수정 ③engine/remediation.py가 실 변경 성공 후 AUDIT_BUCKET/DB_* env 없으면 조용히 감사기록 스킵(§17 위반 가능) → RuntimeError로 승격 ④console-backend data.ts의 getAudit/getCompliance가 프론트 view-fixtures.ts와 shape만 같고 값이 달라(5건vs12건, 0점vs17점) MSW→실API 전환 시 숫자 바뀜 → 값 동기화 ⑤finding.schema.json의 resource_id 정규식이 숫자 미허용이라 s3_bucket 타입(골든 20건 중 6건=30%)이 전부 스키마 위반이었는데 validate.py는 문자열 일치만 봐서 못 잡음(jsonschema로 직접 검증해 발견) → [a-z0-9_]+로 수정 ⑥contracts/validate.py에 윈도우 UTF-8 콘솔 크래시 fix 누락(다른 run_demo엔 다 있음) → 추가. 판단 필요해서 안 고친 것 4건(attackpath n5 pillar 하드코딩 order-dependency·Trivy dedup_key 3세그먼트vs골든mock 2세그먼트 설계불일치·engine RemediateSG IAM 스코프 과다·console Remediation.tsx 승인버튼 미배선)은 CLAUDE.md 변경로그에 [PULL 필요]로 상세 기록, 준형 판단 대기. 수정 후 전 회귀(run_demo 10개·contracts validate·terraform validate 7층·tsc 2개·vite build) 재통과 확인`

- `2026-07-03 / 진우 / [infra][engine] infra/monitoring 전체 재검증 중 버그 3건 발견·수정 — ①triage_escalate_rate_zero 알람이 metric_query.expression에 SUM(SEARCH(...)) 사용 → CloudWatch 알람은 SEARCH()를 지원 안 함(대시보드 전용, 동적 시계열 개수라 단일 임계값과 안 맞음). terraform validate는 HCL 스키마만 봐서 못 잡음(AWS API 시맨틱 검증은 apply 때 발생) → 그대로 뒀으면 apply 시 이 알람 리소스 생성이 API에서 거부됐을 가능성 큼. 해결: engine/reasoning/orchestrator.py의 _emit_case_metrics가 Dimensions=[[], ["Verdict","RiskLevel"]](빈 세트 포함)로 발행하도록 수정해 무디멘션 총계 시계열을 EMF 표준 기능으로 같이 얻고, 알람은 SEARCH 없이 그 총계를 직접 참조. ②bedrock_errors 알람이 ModelId 디멘션 없이 AWS/Bedrock 지표 조회 → Bedrock 지표는 ModelId별로만 발행되고 무디멘션 롤업이 없어 데이터가 안 잡혀 알람이 영원히 INSUFFICIENT_DATA(기능적으로 죽은 알람) → dimensions={ModelId=...} 추가. ③Bedrock 모델별 위젯(y=42+j*6, 모델수만큼 자동배치)과 그 아래 비용/EMF/CloudFront 위젯 y좌표(48·54·60·66) 하드코딩이 안 맞음 → Sonnet 추가 시(모델 2개) 겹치는 잠재 버그, "확장 쉬운 뼈대"라던 설계 의도가 실제론 안 지켜졌던 사례 → local.bedrock_rows_end_y = 42+length(bedrock_model_ids)*6로 상대좌표 전환. fmt/init/validate 3종 + py_compile + run_demo + run_e2e 재통과. infra/monitoring/README.md §13에 근거 기록`
- `2026-07-02 / 준형 / [infra][pipeline][apps-console] apply 직전 최종 객관 리뷰(독립 에이전트 3: infra/architecture/code) — 확정 apply-blocker/버그 수정. ★apply-blocker(plan이 못 잡음, API로 검증): ①EKS 1.30=EXTENDED_SUPPORT라 컨트롤플레인 $0.10→$0.60/h(6배!) → aws eks describe-cluster-versions로 표준지원 확인 후 1.34로. ②RDS 16.4가 서울에서 제거됨(가용 16.9~16.14, describe-db-engine-versions 확인) → 16.9로. 엔진버전 검증은 apply-time이라 plan 클린이어도 실패했을 것. ③RDS 마스터 시크릿 recovery_window 미지정(기본30일)→destroy 후 이름 삭제예약 점유→2번째 apply의 CreateSecret 실패(apply→destroy→재apply 규율 근간을 깸) → recovery_window_in_days=0. ★실모드 버그(데모 미통과): ④console-backend getAudit/getCompliance 응답 shape가 프론트 view-types(AuditEvent[]·ComplianceReport{domains}) 와 불일치→VITE_USE_MOCK=false 스왑 시 Audit/Compliance 화면 깨짐 → 프론트 계약대로 재작성. ⑤normalize handler가 raw_location(S3 포인터)를 fetch 안 해 실 Prowler OCSF 경로 finding 0건 → _hydrate()로 s3.get_object→raw_inline 추가. ★보안 하드닝: ⑥remediation 역할 iam:PutRolePolicy on *(admin 역할 재작성=권한상승) → 타깃 role/${project}-target-*·버킷만 스코프. ★문서: EKS LB destroy 순서·감사 Object Lock bypass·SSO entity ID 갱신·HTTP 무인증 노출을 infra/README 규율에 추가. 전 수정 validate·tsc·py_compile·shared plan(74) 재확인. 진우 플래그(그들 파일): RAG retriever 실모드 NotImplementedError·INTERNAL-UNKNOWN-001 pillar 오분류·case.py reasoning verdict/rag_refs 계약⑦ 드리프트. 아키텍처 조언(전략): 미apply 인프라 과투자 중단·실 스캐너 1건 통과($0)가 진짜 다음 우선순위·Karpenter는 데모엔 부채`
- `2026-07-02 / 준형 / [docs] cnapp-architecture.svg 업그레이드 — 개념 흐름도(1400×1200)를 실 설계 반영 AWS 아키텍처 다이어그램(2240×1110)으로 격상. 참조 그림(siseon.live 멀티리전·IoT)은 다른 프로젝트라 구조 안 베끼고 우리 실물 반영: Seoul Region→VPC 10.20.0.0/16→public/private(workload EKS 3파드·serverless λ6개·data RDS+S3) 서브넷 중첩, CI/CD·AI(Bedrock)·스캐너6기둥·Azure(Entra)·거버넌스 패널, 2-pass 파이프라인 흐름, 골든 크로스클라우드 attack-path 4단계. 검증: Chrome headless(--screenshot)로 PNG 렌더→육안 확인→겹침 1곳(골든 배너 제목↔부제) 수정 후 재렌더 OK. AWS 카테고리 컬러 범례. 함정: SVG를 파일로 덮어쓰기 전 Read 필요(Write 규칙), 비ASCII(U+FFFD) 0 확인, viewBox 매칭 window-size로 1:1 캡처`
- `2026-07-02 / 준형 / [infra][docs] infra/README.md 신설 + apply 전 plan 스윕(무과금 오류 선점) — ①infra 루트 개요 README 신설(레이어링 개념·의존 다이어그램·레이어표·apply 규율·preflight 체크리스트·비용 — 하위 6개 README는 있는데 전체 조립 개요가 없었음). ②plan 스윕 결과: shared=클린(74 add)·slice=클린(5 add). target/console/pipeline/engine 4층은 S3 백엔드 init까지 OK, full plan은 구조적으로 불가 — terraform_remote_state가 실제 shared state(S3)를 읽는데 아직 apply 전이라 "Unable to find remote state"(target으로 실증, 코드 버그 아님 → 내일 shared apply 직후 4층 plan→apply 순서). ③정적 제약 감사(plan 대체): SG description 비ASCII 잔존 0·이름길이 전부 제약 내(ALB21/32·TG24/32·S3 40&32/63·SFn·IAM)·전역 유일 이름 가용 확인 — S3 "member-pii-prod" head-bucket 404(사용 가능, 골든 resource_id 정합 유지)·Cognito 도메인 "cnapp-agentic-demo" describe 빈 응답(가용). 내일 apply 세션은 preflight→shared plan(저장분 유효)→apply→4층 plan→apply→검증→destroy`
- `2026-07-02 / 준형 / [apps-console][docs] 진우 SSO GUID 변경 정합 후속 — 진우가 Entra 무료티어 제약으로 그룹 클레임을 이름→GUID로 바꾸고 auth.ts를 GUID 매칭으로 수정(6d45f07). 준형 쪽 영향 점검: ①infra/console/main.tf attribute_mapping은 무변경으로 여전히 정확(groups 클레임 URI→custom:groups 매핑은 그대로, 값이 이름→GUID인 건 하류 auth.ts가 처리) — GUID임을 알리는 주석만 보강. ②console-app-design §7이 "그룹 이름 배정" 뉘앙스로 stale → GUID 매칭(무료티어 보안그룹 모드·개별 사용자 앱 할당)으로 갱신, manual-infra §3.6.5 참조. ③console-backend tsc(npx tsc --noEmit) exit0·infra/console validate 통과 재확인. SSO 관련 우리 코드 수정 불필요(인프라 정합), 문서만 정합`
- `2026-07-02 / 준형 / [infra] 실 apply 사전점검(preflight+plan)에서 SG description ASCII 위반 발견·수정 — plan이 잡아 apply 전 차단(돈 쓰기 전에 잡힘, 레이어드 plan의 가치). AWS 보안그룹 description(그룹·규칙 둘 다)은 charset 제한(^[0-9A-Za-z_ .:/()#,@[]+=&;{}!$*-]*$) — 한글·em대시(—)·가운뎃점(·) 불가. plan은 규칙 description만 검증해 shared rds SG 규칙에서 에러(exit1), 그룹 description(nat·rds)·console backend SG(·)은 apply 때 CreateSecurityGroup에서 터질 것 → 4곳 전부 ASCII로 교정(shared:112 nat·279 rds 그룹, 283 rds 규칙, console:238 backend). 주석의 한글은 AWS로 안 가므로 무관. 재plan exit0·에러0·74 to add. ※EventBridge 규칙 description(engine/pipeline의 →·한글)·IAM 정책 description은 Unicode 허용이라 제외. preflight: sts(jh_lee/066107819776/서울)·state버킷 존재·fck-nat AMI owner 568608671756 검증OK`
- `2026-07-02 / 준형 / [engine][infra] 보너스 조치(remediation) 실행기 + 감사 Object Lock — 설계/함정: ①engine/remediation.py를 새 subfolder 대신 engine/ 루트 모듈로(handler.py·run_real.py와 동일 패턴, 폴더 최소화). MVP 3종(s3_block_public/sg_remove_open_ingress/iam_least_privilege) 각각 dry_run=계획만/apply=실변경. ②SG revoke는 IpPermissions에서 0.0.0.0/0(IpRanges)·::/0(Ipv6Ranges)만 골라 그 규칙만 revoke(다른 인바운드 보존) — describe로 실제 규칙 읽어 매칭. ③감사=S3 Object Lock(object_lock_enabled=true+versioning 필수, GOVERNANCE 1일 데모). ⚠️함정: 보존기간 내 객체는 잠겨 force_destroy로도 destroy 실패 가능 → GOVERNANCE(특권 우회 가능)로 두고 주석. ④수정→소멸 루프(console §6.1): apply 성공 시 remediation_requests=applied·findings=remediated UPDATE → 이 때문에 remediation Lambda를 VPC 배치(RDS) + secretsmanager + s3:PutObject(audit) IAM 추가. RDS I/O는 env(DB_HOST/DB_SECRET_ARN) 없으면 조용히 skip(로컬 안전). ⑤IAM 변경 API(s3/ec2/iam)는 VPC Lambda라 NAT egress 경유(shared NAT). py_compile+import OK, engine validate·run_e2e(exit 0) 무회귀. 실 변경은 apply 세션에서 dry-run 먼저`
- `2026-07-02 / 준형 / [engine][infra][docs] 오늘작업 독립리뷰(에이전트2) 버그 2건 수정 + 문서 스윕 — ①(CONFIRMED, 내 engine/handler.py) verdict·confidence_score를 case["reasoning"]에서 읽었는데 case.py set_reasoning은 {narrative,risk_level,recommended_actions}만 쓰고 verdict/confidence는 set_evidence가 case["evidence_meta"]에 씀 → finding_explanations.confidence_score 항상 NULL·반환 verdict 항상 null. run_e2e.py는 이미 evidence_meta에서 읽어 정합(핸들러만 diverge). 수정: 둘 다 evidence_meta에서 읽기. ②(터라폼 하드닝) infra/console이 acm_certificate_arn 빈값이면 aws_lb_listener.https가 apply에서 "certificate ARN '' is not valid"로 하드실패(authenticate-cognito=HTTPS 필수) → HTTPS 리스너 count=has_cert?1:0 가드 + HTTP 리스너를 dynamic default_action으로(cert 있으면 443 redirect, 없으면 Lambda로 forward) → 인증서 없이도 레이어 apply되고 API가 HTTP로 동작, SSO는 cert 확보 후 활성. ③리뷰가 제기한 SAML custom:groups attribute_mapping은 false positive로 판정·유지(Cognito 커스텀 속성 대상키는 custom: 접두가 정식 형식). 문서: README(authenticate-oidc→cognito·폴더트리 5층+db+slice·stale Status행·변경요약 6→1줄)·CLAUDE §3·infra/shared README 소유·console-app-design §12·pipeline/engine/attackpath README handler.py 반영. handler 4개 import + terraform 3층 validate 재통과`
- `2026-07-02 / 진우 / [infra][apps-console] Entra SSO App Registration — 그룹 클레임이 라이선스 제약으로 계획대로 안 됨, 2번 우회 — ①"애플리케이션에 할당된 그룹" 모드로 그룹 클레임을 만들려 했으나 Entra 무료 티어(P1/P2 없음)라 엔터프라이즈 앱에 "그룹" 자체를 할당할 수 없음(개별 사용자만 가능) → 그룹 클레임 소스를 "보안 그룹"(로그인 사용자가 속한 전체 보안그룹)으로 전환 + viewer@·approver@ 개별 사용자를 앱에 직접 할당. ②"보안 그룹" 모드로 바꾸니 이번엔 "클라우드 전용 그룹의 그룹 이름 내보내기" 체크박스가 비활성화됨(이 옵션은 "애플리케이션에 할당된 그룹" 모드 전용으로 확인) → 클레임에 그룹 이름 문자열이 아니라 그룹 개체 ID(GUID)만 실림. 원래 코드(apps/console-backend/src/auth.ts)가 text.includes('cnapp-approver') 문자열 매칭이라 이대로면 전원 viewer로만 인식됨 → cnapp-approver 그룹의 실제 개체 ID(4d6be000-2a4b-43f7-95ff-cfdb87da786d)를 Entra에서 조회해 GUID 매칭으로 코드 변경(오히려 이름보다 불변 식별자라 실무적으로 더 견고 — 임시방편이 아니라 개선). infra/console/variables.tf의 saml_metadata_url 기본값에 실제 발급 URL 반영, terraform validate + npm run typecheck 둘 다 통과 확인`
- `2026-07-02 / 준형 / [infra][pipeline][engine][attackpath] DB 스키마 + Lambda wrapper — 설계/함정: ①진짜 공백 발견: SQL 파일이 하나도 없어 실 데이터 평면(normalize→RDS·corpus→pgvector·console-backend real·cases upsert)이 전부 "테이블 가정"만 했음 → infra/shared/db/schema.sql 신설(계약①③⑦⑥+console§5 도출, 멱등 IF NOT EXISTS). ②FK 순서 함정: findings.attack_path_id→attack_paths인데 2-pass상 findings가 먼저 삽입(attack_path_id NULL)되고 attack_paths는 나중 → FK를 nullable+ON DELETE SET NULL로, 스키마도 findings 먼저 만들고 attack_paths 생성 후 ALTER ADD CONSTRAINT. ③sources 배열 머지 upsert: ON CONFLICT(dedup_key) DO UPDATE에서 sources=(SELECT ARRAY(SELECT DISTINCT unnest(findings.sources||EXCLUDED.sources)))로 출처 누적(계약① dedup 머지 정합). ④psycopg2 jsonb: 기본 typecaster가 jsonb→Python(list/dict) 자동 디코드라 attack_paths.nodes/edges 로드 시 json.loads 불요(correlate/orchestrator에 그대로 전달). ⑤wrapper는 기존 클래스(Ingestor·Normalizer·CorrelationEngine·Orchestrator) 재사용 + 이벤트 어댑팅 + RDS I/O만 얹음(psycopg2/boto3 지연 import라 무 DB 로컬에서 import는 됨, 실행은 apply 세션). engine/handler는 REAL_TOOLS=1이면 RealToolExecutor+BedrockEvidenceAgent(run_real 미러). py_compile+import 4개 OK, terraform 스텁 주석을 실 핸들러 경로로 갱신 후 validate 재통과. ⚠️ 실 RDS 대상 SQL은 라이브 미검증(apply 세션)`
- `2026-07-02 / 진우 / [attackpath][contracts] f16 노드 매핑(n4 vs n5) 확정 — n5로 옮기려다 correlation.py 코드 의존성 발견해 취소, n4 고정+실물 조정으로 대체 — attackpath/correlation.py의 _R3_AZURE_SP="INTERNAL-ENTRA-SP-CRED-001"가 이 control_id를 가진 finding의 resource_id를 코드로 그대로 n4 노드에 씀(_first(findings,_R3_AZURE_SP)["resource_id"]). f16을 n5(overpriv-app, a1b2c3d4)로 옮기면 n5도 같은 control로 채워지는 R5(_R5_ENTRA_APP)와 resource_id가 겹쳐 n4=n5 그래프 붕괴(같은 노드 중복). 처음엔 mock-findings.json만 보고 "이동하면 됨"으로 판단했으나 correlation.py 하드코딩까지 확인 후 뒤집음 — 계약 파일 수정 전 상관 로직의 control_id 의존성을 먼저 확인해야 함(교훈). 해결: f16=n4(order-sp) 고정 유지, 대신 실 Entra 포털이 "무만료" 옵션을 아예 지원 안 함(최대 24개월)을 확인 후 order-sp 시크릿을 24개월로 재발급 + control 타이틀을 "무만료"→"장기 유효(>6개월)"로 완화. 구조 필드(resource_id·control_id·severity_id·pillar·dedup_key) 무변경, 타이틀 텍스트만 수정이라 회귀 없음(10개 데모+validate exit 0)`
- `2026-07-02 / 준형 / [infra] infra terraform 3층(console·pipeline·engine) 작성·검증 — 설계 포인트/함정: ①검증은 terraform init -backend=false + validate로(백엔드 S3·AWS 자격증명 없이 스키마만) → 3층 다 "Success! The configuration is valid." ②Lambda 패키지: 핸들러 wrapper가 아직 없는 Python 컴포넌트(normalize·correlation·orchestrator·remediation)는 archive_file의 source{content=inline stub}로 '배포 가능한 스텁'(200 반환) 생성 후 실코드 스왑 포인트로 표시 — validate는 data source 미실행이라 통과, plan/apply는 inline이라 항상 성공. console-backend(Node)만 dist에서 archive(빌드 선행 필요). ③authenticate-cognito는 HTTPS 리스너 필수 → acm_certificate_arn 변수(도메인+ACM 선행), 미정 시 빈값으로 validate는 통과하나 apply는 인증서 필요. ④커스텀 2-pass 이벤트(cnapp.findings.batch.completed·cnapp.attackpath.correlation.completed)는 별도 EventBridge 버스 안 만들고 기본 버스에 PutEvents+규칙 → pipeline↔engine 하드 apply 순서(버스 존재 의존) 회피·리소스 최소화. ⑤조치는 read-only(콘솔·엔진 역할)와 격상(변경 API=remediation Lambda 역할)을 분리(§17) — 콘솔은 states:StartExecution만. ⑥cross-layer 참조: shared는 remote_state(필수 선행), engine SM ARN은 console에 -var 주입(옵션, 하드 순서 회피). ⑦slice는 아키텍처 레이어 아닌 '일회용 저비용 픽스처'로 §4.6·트리에 명시(병합=독립·저비용 목적 상실이라 유지)`
- `2026-07-02 / 진우 / [pipeline] normalizer OCSF 파서 부재(준형 피드백 #2) — 실 Prowler는 json-ocsf 출력이고 ingest.from_s3_event가 이미 source_format="ocsf"로 봉투화하는데 normalize에 ocsf 분기가 없어 이음새 갭. 방향 결정: 처음 하이브리드(AWS→json-asff 재사용/Azure→prowler-json) 제안했으나 설계서 §24·계약⑤가 "Prowler→OCSF"로 이미 확정돼 있어 OCSF 파서 신설로 확정(ASFF는 AWS 전용이라 Azure Entra 못 담음, OCSF는 클라우드 중립→파서 1개로 AWS+Azure). 구현 포인트: ①check_id는 metadata.event_code(Prowler OCSF의 체크ID 위치), unmapped.check_id 폴백. ②리소스 타입은 resources[].group.name(=service)→_PROWLER_RTYPE, resourceId가 이미 azure: 캐논이면 passthrough. ③severity는 문자열("High") 우선, 없으면 OCSF severity_id(0~6, 방향 반대)를 내부 컨벤션(1~5)으로 뒤집어 매핑. ④OCSF 버전 편차 대비 전 필드 방어적 탐색(.get 체이닝). run_demo에 OCSF fixture 2건(AWS S3·Azure Entra)+단독 검증(dedup 머지가 silent-fail 가리는 것 방지) 추가, 10개 데모 exit 0`
- `2026-07-02 / 준형 / [apps-console] console-backend(ALB→Lambda TS) 구현·검증 — ①PowerShell 파이프에서 npm(.cmd) 실행 불가("Cannot run a document in the middle of a pipeline") → cmd /c로 npm install·tsc·node 실행. ②mock 데이터는 계약 SSOT 중복 방지 위해 import 대신 fs.readFileSync(CONTRACTS_DIR 기본 __dirname/../../../contracts)로 런타임 로드 → tsc rootDir 문제 회피. ③@types/aws-lambda 의존 회피 위해 ALB event/result 타입 인라인 정의. ④auth: ALB authenticate-cognito가 넘기는 x-amzn-oidc-data(JWT) payload의 custom:groups→approver/viewer, 미인증/로컬은 viewer 기본. 스모크(ALB 이벤트 직접 호출): findings 18 open·detail 조인·attack-path 5노드·RBAC viewer403/approver200·404 전부 OK. 실 pgvector 경로는 스텁(USE_MOCK=false+PG_DSN 후)`
- `2026-07-02 / 준형 / [attackpath][apps-target][infra] 전 영역 리뷰(에이전트2+준형) 반영 — ①ArgoCD glob(진우): recurse+include "k8s/*.yaml"는 ArgoCD 글롭(*가 / 미횡단)상 depth1 namespace만 잡고 앱(depth2) 0개 → "{k8s/*.yaml,*/k8s/*.yaml}"로 수정. ②IRSA 역할명: 매니페스트 cnapp-target-*-irsa vs infra/target ${project}-target-*-irsa(=cnapp-agentic-target-*) 불일치 → 매니페스트 정정. ③infra/shared 하드닝: NAT·EKS노드 metadata_options http_tokens=required(IMDSv2, CKV_AWS_79) + EBS encrypted(CKV_AWS_8) 추가 → terraform validate 통과. ④contracts-validate.yml 삭제(ci.yml checks가 매 push마다 validate 실행 → 중복). ⑤karpenter discovery 태그 cnapp-eks→cnapp-agentic-shared(실 클러스터명). 리뷰 결과 콘솔/타깃/contracts는 clean(진짜 버그 0). 진우 파일 이슈(answer_gen Sonnet bare ID 404·normalizer OCSF 파서 부재)는 변경로그 [PULL 필요]로 피드백`
- `2026-07-02 / 진우 / [scanners] Entra CIEM 스캐너(scanners/ciem) — 설계 포인트: ①resourceId를 Prowler Azure는 ARN이 없어 스캐너 단계에서 캐논 형식(azure:type:native_id) passthrough로 직접 준다(_canon_resource_id가 "aws:"/"azure:" 접두면 재가공 안 함, 4.4.1a). ②service 필드(appregistration/entraid)가 _PROWLER_RTYPE 매핑의 resource_type을 결정하므로, resourceId가 이미 캐논이어도 service는 정확히 맞춰야 함(안 그러면 resource_type만 틀림). ③실 스캔(scan_prowler)은 subprocess 로직을 중복 구현하지 않고 scanners/cspm/cspm.py의 CSPMScanner.scan_prowler(provider="azure")에 위임 — Trivy와 달리 얇은 래퍼만 추가. ④control-catalog 와일드카드 매칭 확인: entra_id_app_registration_overprivileged → "entra_id_app_*_overprivileged" 매칭, entra_id_app_redirect_uri_insecure는 exact match. run_demo가 mock-findings.json 골든 f8·f9·f16·f17(resource_id·severity_id·pillar·resource_type)과 전부 일치 확인.`
- `2026-07-02 / 준형 / [ci] CI/CD 코드 세팅 — 설계 포인트: ①이 레포는 의도적 취약(infra/target·apps/target) 포함이라 Trivy/Checkov를 hard-gate로 걸면 CI가 항상 red → shift-left 잡을 report 모드(soft-fail/exit 0)로. 실서비스면 hard-gate가 정석(주석·docs 명시). ②CI checks 잡은 순수 stdlib 데모라 pip 설치 불필요($0·빠름) — bedrock/boto3는 지연 import라 목업 경로 무영향. ③CD=ArgoCD인데 살아있는 EKS 필요라 CI(코드쪽)와 분리, apply 세션에서만. ④오토스케일 CAS→Karpenter 확정하되 "데모 규모선 노드 오토스케일러가 실비 좌우 안 함, 진짜 절감=destroy+spot" 정직하게 문서화(cost-strategy §2.7) — 과장 방지. gitops/ 선언형 코드는 apply 전(EKS 세션 대기)`
- `2026-07-02 / 준형 / [scanners][pipeline][rag] 준형 컴포넌트 half 3종(cspm·ingest·corpus) 구현 — 계약 정합 포인트: ①CSPM 봉투 raw_inline은 계약⑤상 object라 finding 1건=봉투 1개(EventBridge 1이벤트=1봉투 모델), 배치는 여러 봉투. 진우 normalize 데모가 raw_inline=array(배치)로 쓴 것과 대비되나 normalizer가 list/dict 둘 다 처리해 무해. ②secret finding resourceId는 canonical passthrough("aws:secret_plaintext:...")로 줘야 _canon_resource_id가 재가공 안 함(prowler service=secretsmanager는 secret_plaintext, eks면 eks_pod라 golden과 불일치할 수 있음). ③rag/corpus는 진우 mock_corpus 내용 소비(중복 안 함)+embedding만 얹어 계약⑥ 완성(embedding_model·dim const, embedding 1024). mock 임베딩=sha256 시드 결정적 벡터(실 의미 아님, 차원·계약 검증용). ④run_e2e에 CSPM 스캐너 물릴 때 Azure Entra만 mock-findings에서 채움(진우 ciem 미구현). 전 데모 9개+validate exit 0`
- `2026-07-02 / 준형 / [pipeline][engine] Phase2 end-to-end 러너(run_e2e.py) 배선 — 각 영역 run_demo만 있고 통합 흐름이 없어 신설. 배선 이슈: ①스캐너발 CVE를 attack-path n1으로 쓰려면 mock KEV와 충돌 → load_findings()에서 control_id=INTERNAL-VULN-KEV-001 제외 후 스캐너 finding 합류(correlation _first가 스캐너 KEV를 n1으로 집음). ②correlate()가 findings에 attack_path_id backfill(2-pass) 후 Orchestrator.run에 같은 리스트 전달해야 case_findings 선정됨(순서 지킴). ③trivy·docker 로컬 미설치 → scan_from_json fixture(실 trivy --format json 동일 구조)로 fallback, --image 주면 실스캔 시도. 결과 exit 0(CVE→KEV→n1→confirmed→RAG 설명 관통). 발견: rag/answer_gen.py _BEDROCK_MODEL_ID가 bare name(404) — 진우에게 실 RAG 시 global 프로파일로 교체 헤드업(변경로그)`
- `2026-07-02 / 준형 / [engine][infra] Phase1 실 tool-use END-TO-END 검증 — ①스모크(리소스 0, MockToolExecutor+실 Bedrock)로 루프 먼저 격리 검증 → LLM이 6개 툴 자가 선택(규칙 플래너 4개보다 많음), OK. ②infra/slice apply(계정 S3 BPA=NoSuchPublicAccessBlockConfiguration=OFF라 공개 정책 거부 안 됨) → run_real: 실 Bedrock Haiku가 s3:GetBucketPolicy·GetPublicAccessBlock 자가 호출 → 실 S3 응답 CONFIRMED(100%) → destroy(5개 삭제·s3 ls로 버킷 부재 확인). 함정: RealToolExecutor는 GetBucketAcl 미구현이라 LLM이 골라도 NotImplementedError→성공 카운트 제외(정상 처리). terraform apply -auto-approve는 auto-mode 분류기가 blind-apply로 차단 → plan 먼저 보여주고 사용자 승인 후 apply. run_real 표시 "N개 선택/M회 성공"으로 정정`
- `2026-07-02 / 준형 / [engine][infra] Bedrock 모델 액세스 — 콘솔 '모델 액세스' 페이지가 폐지(retired)됨(이전 안내대로 승인 요청 불가). 서버리스 모델은 첫 호출 시 자동 활성, Anthropic만 최초 1회 use-case 폼 제출 필요 → 모델 카탈로그→Claude Haiku→플레이그라운드에서 폼 제출 후 응답 확인=액세스 열림(계정당 1회). model ID는 bare name(404 함정) 대신 콘솔 '추론 프로파일'의 Global inference profile ID(global.anthropic.claude-haiku-4-5-20251001-v1:0) 사용 → DEFAULT_MODEL_ID 반영. manual-infra §4 기록`
- `2026-07-02 / 진우 / [rag] mock_corpus.py 계약⑥ const 필드 누락 버그 수정 — 최초 구현 시 청크 dict에 embedding_model·dim 필드를 추가하지 않아 계약⑥ required 위반(24청크 전부). 원인: embedding(1024 floats)만 제외하면 된다고 생각했으나 embedding_model("amazon.titan-embed-text-v2:0")·dim(1024)도 required 필드임. 해결: _MOCK_CONST 상수 정의 + _inject_const() 함수로 getter(get_chunks_by_control·all_chunks)에서 자동 주입 → 데이터 정의를 깔끔하게 유지하면서 계약 정합`
- `2026-07-02 / 진우 / [scanners][pipeline] normalizer _parse_trivy ArtifactName 태그 미제거 — scan_image("shop/product:latest") 시 Trivy가 ArtifactName에 ":latest" 포함해 내놓으면 resource_id="aws:eks_pod:shop/product:latest"로 생성돼 mock("aws:eks_pod:shop/product")과 불일치. 해결: _parse_trivy에서 ArtifactName rsplit(":",1) 후 태그 세그먼트("/"없는 마지막 세그먼트)를 제거`
- `2026-07-02 / 진우 / [scanners] trivy.py scan_from_json의 image 파라미터 봉투 미반영 — _build_envelope에 image를 전달하나 실제로 사용 안 돼 봉투 어디에도 이미지명이 없었음. 해결: scan_batch_id에 이미지명 포함("trivy-{safe_img}-{timestamp}")으로 수정 → remediated 판정 스코프(4.4.1c)에서 이미지별 구분 가능`
- `2026-07-02 / 준형 / [engine] Bedrock LLM tool-use 플래너(bedrock_planner.py) 구현 — 실 Bedrock 없이 로직 검증 필요 → 가짜 converse() 클라이언트(1턴 tool_use 2개→2턴 end_turn) 주입해 오프라인 테스트: 에이전틱 루프가 toolUse 파싱→MockToolExecutor 실행→toolResult 되먹임→종료→verdict=confirmed까지 정확. 실 boto3 client는 __init__에서 session.client("bedrock-runtime")(네트워크 없음)라 오프라인 생성 가능, _client만 가짜로 교체해 검증. 실 Bedrock 호출/서울 model ID/모델 액세스는 apply 세션에서 확정(bare name 404 함정 주석). run_real.py를 스크래치패드 아닌 repo에서 실행할 땐 PYTHONPATH=repo root 필요`
- `2026-07-02 / 준형 / [engine] hypothesis.py _has_cross_cloud가 edge.get("edge_type")=="cross_cloud" 검사 → 계약③ 엣지는 type(credential_theft…)+cross_cloud(불리언) 구조라 edge_type 키·"cross_cloud" type 값이 없음 → 항상 False → _CROSS_CLOUD_HYPOTHESIS(크로스클라우드 체인 가설)가 절대 추가 안 됨(데모 Hypothesis 출력에도 누락). 골든 검증이 가설 목록을 안 봐 CI 미탐. edge.get("cross_cloud")로 수정 → 데모에 크로스클라우드 체인 가설 출력 + 골든 정합·case 스키마 유지(exit 0). 진우 reasoning 영역 코드라 CLAUDE 변경로그 [PULL 필요]로 알림`
- `2026-07-02 / 진우 / [pipeline] normalizer.py Azure resource_id 이중 prefix 버그 — Prowler Azure raw에서 resourceId가 이미 "azure:app_registration:..." 캐논 형식으로 들어왔는데, _canon_resource_id()가 또 "azure:app_registration:" prefix를 앞에 붙여 "azure:app_registration:azure:app_registration:..."으로 이중화. 원인: ARN 체크 없이 무조건 cloud:type: prefix 추가. 해결: raw_id가 "aws:" 또는 "azure:"로 시작하면 캐논 형식으로 간주하고 그대로 반환`
- `2026-07-02 / 진우 / [pipeline] normalizer.py Prowler check ID → control 매핑 실패(UNKNOWN-001) — mock checkID "entra_id_app_directory_readwrite_all"이 catalog 와일드카드 패턴("prowler:entra_id_app_*_overprivileged", "prowler:entra_id_application_*")에 매칭 안 됨. 원인: 패턴이 과도하게 구체적(suffix 강제). 해결: demo checkID를 catalog 패턴에 맞는 "entra_id_app_registration_overprivileged"로 변경 → INTERNAL-ENTRA-OVERPRIV-APP-001 매핑 OK`
- `2026-07-01 / 준형 / [apps-console] 외부 리뷰: Evidence 탭 데이터 소스 이중(§5 finding_explanations.evidence_json ↔ §15.4 mock-cases) 발견 → UC0=case단위/UC1=finding단위로 확정, finding_explanations에 case_id FK + cases 테이블 신설, 상세는 finding_id∈case.finding_ids 조인. ai_status placeholder·정합 3건·스택 확정(RF/MSW/TS)+폴리글랏 반영`
- `2026-07-01 / 준형 / [contracts] §2.1 계약 정합 4건 수정 중 attack-path 노드 n4(azure SP)에 대응 finding 부재 발견(assert c 위반) → f16을 골든 경로 편입(control도 ENTRA-SP-CRED-001 신규). validate.py로 4-assert 자동화, CI 게이트(.github/workflows) 추가. validate 통과`
- `2026-07-01 / 준형 / [infra] infra/shared terraform init provider 버전 충돌(fck-nat 모듈 aws>=6.0 vs vpc/eks 5.x <6.0) → fck-nat 모듈을 raw NAT instance(fck-nat 공개 AMI + source_dest_check=false + private route)로 교체해 충돌·모듈 변수 불확실성 동시 제거. fmt·validate 통과`
- `2026-07-01 / 준형 / [docs] 진우 문서 검증 6건 총괄 반영 — ①CLAUDE §5 앱분담 stale행(0b1d493 병합 때 되돌아감) → 준형 2개 전담으로 정정 ②project-draft §4.4.1(b) 카탈로그표 누락 control 4행 추가(13→14) ③console §3·§5.1 "D3/Recharts"→React Flow(§15.1 확정) ④f15 title↔control 모순(NOENCRYPT인데 source는 버저닝) → 신규 INTERNAL-S3-LOGGING-DISABLED-001(cspm) 추가 후 재매핑 ⑤§15 모델ID: bare name은 Bedrock invoke ID 아님(404) → Bedrock 형식/inference profile 구현시 확정 주석 ⑥R1 "同 resource" 모호 → "워크로드↔부착 SG 토폴로지" 완화 + 파드→ENI→SG 매핑 주석. 전 md stale 스윕(13종→14종 일괄) + README 역할분담 섹션 추가. validate 통과(14종)`
- `2026-07-01 / 준형 / [contracts] 진우 합의 반영 — f16·f12 객관 기준으로 c463054 값 최종 복원. f16: OVERPRIV-APP-001(의미 느슨)→SP-CRED-001(ciem, title 정확 일치)로 재매핑 + 카탈로그에 INTERNAL-ENTRA-SP-CRED-001 재추가(13종). f12/ECR: pillar vuln→cspm(control도) — "스캔 비활성"은 CVE가 아니라 미스컨피그, 소스가 전부 posture(securityhub ECR.1·config·prowler ecr_scan)라 cspm이 객관. validate OK. 교훈: 기둥=소스 성격 기준을 처음부터 일관 적용할 것`
- `2026-07-01 / 준형 / [contracts] 진우 커밋 0b1d493 pull 후 validate.py 2건 FAIL(main RED) 발견 → f5 resource_id가 aws:eks_pod:shop/order로 리버트돼 type(secret_plaintext)와 assert(b) 불일치 → aws:secret_plaintext:shop/order/AZURE_SP_CRED로 재복원. f17이 ENTRA-RISKY-CONSENT-001(ciem)로 재매핑됐으나 pillar cspm 유지해 assert(a) 위반 → 설계 충실(Azure Defender secure-score=cspm)로 INTERNAL-ENTRA-INSECURE-CFG-001(cspm) 카탈로그 재추가 후 f17 재매핑. validate 통과. ※f16→OVERPRIV-APP-001 재매핑은 CI-green이나 의미 느슨 — 진우 검토 요청`
- `2026-07-01 / 준형 / [apps-console] apps/console 스캐폴드(§15) — Vite+React+TS+Tailwind+TanStack+React Flow+Recharts+MSW. contracts/*.schema.json→타입(손번역 types.ts + gen-types.mjs), MSW handlers가 @contracts alias로 mock-*.json 직접 서빙(vite fs.allow=repoRoot). 화면 8종(핵심 4: Dashboard·Findings·FindingDetail[Evidence탭]·AttackPath + 스텁 4). 트러블: ①tsconfig -b project-ref 제거→tsc --noEmit ②FindingsFilter가 Record 인덱스시그니처 없어 apiGet 타입에러→[key:string] 추가 ③Findings.tsx 손오타(깨진 유니코드) 제거. npm i(252pkg)·msw init·tsc·vite build(499 모듈) 전부 통과`
- `2026-07-01 / 준형 / [apps-console] tsconfig baseUrl deprecation 경고(TS7.0에서 제거 예정) → baseUrl 제거. TS 5.x는 paths를 baseUrl 없이 tsconfig 위치 기준 상대경로로 해석 → @/*·@contracts/* 그대로 동작(Vite는 자체 alias라 무관). build 501 모듈 통과`
- `2026-07-01 / 준형 / [apps-target][infra] 타깃 앱 구현(member Python/FastAPI + PII seeder / product·order 결함 매니페스트 / infra/target 결함 IaC 토글) + 독립 리뷰 반영. 리뷰 H1: infra/target이 참조한 OIDC 출력명 불일치(oidc_provider_arn/url) → infra/shared에 eks_oidc_provider(URL) 출력 추가 + target을 eks_oidc_provider_arn/eks_oidc_provider로 정정(리뷰 반환 전 선반영). M1: IRSA 신뢰정책이 :aud만 걸어 아무 SA나 assume 가능 → member/order별 :sub 고정(system:serviceaccount:shop:*)로 분리, member '최소권한 대조군' 실질화. namespace shop 매니페스트 신설(apps/target/k8s), TF 결함 주석에 control_id 추가. seeder head_object는 botocore ClientError로 404 판정(s3.exceptions.ClientError 오사용 선수정). py_compile·terraform fmt 통과`
- `2026-07-01 / 준형 / [apps-target] member 로컬 실행 시 uvicorn 기동 실패 — 로컬 기본 python이 3.8인데 코드가 PEP585 빌트인 제네릭(list[Member]·dict[str,str]) 사용 → pydantic/FastAPI 런타임 평가·decorator response_model에서 TypeError. Docker는 3.12라 무관하나 로컬용으로 typing.List/Dict로 교체(3.8+ 호환). import OK·/health·/members 응답 확인. ※함께 py_compile 산물 __pycache__/*.pyc가 커밋됐던 것 발견 → member/.gitignore 추가 + git rm --cached로 추적 해제`
- `2026-07-01 / 준형 / [docs] attackpath 영역 문서 정합 스윕 중 project-draft §4.4 R5 오류 발견 — identity_takeover를 cross_cloud:true로 적었으나 n4→n5는 Azure 내부 이동이라 false(경계횡단은 R3 credential_theft 하나뿐) → 실구현·mock-attack-paths.json과 정합하게 수정(validate_graph가 이 불변식 강제). target-app-design §3 산문 4단계↔그래프 5노드(n1~n5) 매핑도 명시`
- `2026-07-01 / 진우 / [engine] run_demo.py 최초 실행 시 UnicodeEncodeError(cp949) — Windows PowerShell 기본 인코딩이 cp949이라 print()의 em 대시(—·U+2014) 인코딩 실패. 해결: $env:PYTHONIOENCODING="utf-8" 설정 후 재실행 → 정상 출력`
- `2026-07-01 / 준형 / [docs][engine] 폴더 규칙 누락분 복원 — "하위 폴더 최소화(영역당 소유자별 2개, a=준형·b=진우, 공유만 예외)"가 예전 Claude 채팅에만 있고 문서 누락 → 그래서 engine을 core/triage/evidence로 과다 분할했었음. project-draft §4.6·CLAUDE §4에 규칙 명문화 + engine 재구성: triage.py를 evidence/로 이동해 준형 몫=evidence/ 한 폴더(triage.py·evidence.py), triage/ 폴더 삭제. run_demo import 갱신. 재실행 exit=0. 진우 몫은 reasoning/(예정). 사람이름 폴더는 여전히 금지 — 이름은 컴포넌트로`
- `2026-07-01 / 준형 / [docs] infra/console·apps console 소유가 project-draft §4.6에 "진우"로 stale — 앱 2개 준형 재배정(CLAUDE §5) 때 §4.6 terraform 표만 누락됐던 것. 준형으로 정정(§4.6 표·코드블록·apps 행·apply 자동화 불릿 + CLAUDE §4 트리). SSO는 준형 Cognito↔진우 Entra App Reg(manual-infra §3) 연동으로 명시 — 진우는 IdP(Entra)만, AWS 콘솔 인프라는 준형. 교훈: 한 곳 고치면 전 md(README·draft·CLAUDE·트슈) 교차정합 스윕 필수`
- `2026-06-30 / 준형 / [contracts] contracts/ 졸업 — 계약 7종 JSON Schema + control-catalog + mock 3종 생성. python json.load 10/10 통과, 교차참조(control_id 카탈로그·dedup_key·골든 경로 id·case finding_ids·evidence allowlist) 정합 검증 통과. 콘솔 출력 cp949 em대시 깨짐은 PYTHONIOENCODING=utf-8로 우회`
- `2026-06-30 / 준형 / [contracts] 검증 후속 확정 — 4.4.1 정규화 규칙 3종(resource_id 캐논·INTERNAL control_id 카탈로그·remediated source별 스코프) 신설, §24 3건 닫음, 2-pass·핵심영역 분담 확정 전환. README 폴더구조 실제 구조로 갱신`
- `2026-06-30 / 준형 / [docs] CLAUDE.md §6·§7이 HEAD~4 머지에서 유실 + §5 "시간 컷" 불릿이 깨진 바이트(ef bf bd)로 잘린 것 발견 → head -126으로 온전부 보존 + HEAD~5에서 §6/§7 추출 복원, §7.1 Next Up 추가`
- `2026-06-30 / 준형 / [contracts] 진우 9개 확정 검증 — attack-path 상관과 Evidence 실행순서 상충 발견(UC0 vs 트리거/게이트) → 2-pass(1차 규칙상관→트리아지 게이트→Evidence→2차 확정)로 docs 통일. dedup용 resource_id 캐논·INTERNAL control_id 카탈로그·remediated 스캐너별 스코프 3건은 §24 미확정 등록`
- `2026-06-30 / 준형 / [docs] project-draft 17~24번이 커밋 cb2c55a에서 파일 꼬리째 유실된 것 발견 → git show 53f8bd6(v5.4)에서 ## 17~끝 구간 추출해 복원, 이음매 빈 줄 정리`
- `2026-06-30 / 준형 / [docs] git pull이 .git/index.lock·ORIG_HEAD.lock stale로 막힘 → del /S /Q .git\*.lock 후 재pull로 해결`
<!-- 여기부터 실제 항목을 위에 추가 -->

---

## 2. 진행 로그 (추가·작업한 것)

> 무엇을 만들었/추가했는지 한 줄씩. (git 커밋이 1차 기록이지만, 사람이 빠르게 훑는 용도)

- `2026-07-03 / 진우 / [infra] 세션 종료 destroy — infra/monitoring만 destroy(27개 리소스 전부 삭제 확인, plan -destroy로 27 to destroy·0 add·0 change 미리 확인 후 실행). shared·target·console·backend(준형 소유)는 이번 세션에서 안 건드림. CloudTrail 트레일의 CloudWatchLogsLogGroupArn이 삭제된 로그그룹을 가리키는 dangling 상태로 남는 것 확인 — 재apply 시 같은 이름으로 로그그룹 재생성돼 자동 복구됨(별도 재연결 불필요, 고정 이름이라 ARN 불변)`

- `2026-07-03 / 진우 / [infra] infra/monitoring 첫 실 apply 완료 + CloudTrail 연결 + gitops/monitoring 신설 — 준형 4층(shared·target·console·backend) apply 뒤 terraform plan(27 add·0 change·0 destroy)→apply 성공, output 6종 발급. aws cloudwatch get-dashboard로 대시보드 실체 조회해 24위젯 정확히 등록·겹침 없음 확인. CloudTrail(cnapp-org-trail)에 update-trail로 1회 연결(트레일 자체는 tfstate 밖 그대로). grafana_irsa_role_arn 실값으로 gitops/monitoring/kube-prometheus-stack-values.yaml(Grafana IRSA+CloudWatch 추가 데이터소스+Alertmanager 비활성화)·gitops/argocd/app-monitoring.yaml(멀티소스 Application: 공식 helm 차트+이 레포 values) 신설. infra/monitoring/.gitignore 신설(tfplan 등 실수 커밋 방지, 실제로 untracked로 남아있던 걸 발견). gitops/README.md 부트스트랩 순서에 관측 CD 단계 추가`
- `2026-07-03 / 진우 / [engine][infra] AI 관측 3종 추가 — Bedrock 비용 알람(bedrock_cost_high, 비용 위젯과 동일 metric math 재사용+period 3600+var.bedrock_hourly_cost_alarm_usd) / 케이스별 Bedrock 토큰(bedrock_planner.py가 converse() 응답 usage를 tool-use 루프 반복마다 누적 → EvidenceOutput.input_tokens/output_tokens 신규 필드 → case.py set_evidence(tokens=)로 case.schema.json의 기존 미사용 필드 model_trace[].tokens를 처음 채움 → orchestrator.py _emit_case_metrics에 BedrockInputTokens/OutputTokens EMF 추가, CaseId는 카디널리티 문제로 Dimension 미사용·Logs Insights로 드릴다운) / tool별 breakdown(EvidenceOutput.plan 기존 필드를 _emit_tool_usage_metrics가 Dimensions=[["Tool"]]로 tool별 EMF 라인 발행). infra/monitoring 대시보드 위젯 2개 추가(22→24, CloudFront y좌표 밀림), README §15 신설. 오프라인 fake Bedrock 클라이언트로 2턴 루프 재현해 토큰 누적(800/200)·model_trace.tokens(1000) 검증. 전 회귀(run_demo 9개+run_e2e+contracts validate) 무회귀 + infra/monitoring fmt/validate 재통과`
- `2026-07-03 / 진우 / [scanners][pipeline] kube-bench KSPM 스캐너 신설 — scanners/workload/kube_bench.py(KubeBenchScanner), Trivy와 동일한 이중 경로: scan_from_json(kube_bench_json, target)(mock, EKS 불필요) / scan_cluster(target)(kubectl로 aquasecurity 공식 Job 배포→wait→logs→cleanup, EKS 미apply라 미검증). 파드 귀속 정보가 없는 kube-bench 원본 특성상 target 파라미터를 raw_inline.target_resource로 주입하는 모델링 단순화를 적용(docstring에 명시). pipeline/normalize/normalizer.py에 _parse_kube_bench 신설(source_format="custom"+source=="kube-bench" 분기, severity 없는 CIS 벤치마크라 control-catalog severity_default 사용). scanners/workload/run_demo.py에 kube-bench 데모 통합 — product(f2 골든)·member(f13 골든) FAIL→open 일치 확인 + order PASS→remediated 상태전환 회귀케이스 추가. scanners/README.md·루트 README.md(폴더트리·스캐너현황표·역할분담)에 Inspector 오기재 정정(별도 코드 불필요, scan_securityhub 경유 커버) 포함해 갱신. 전 회귀(scanners.cspm·workload·ciem, pipeline.ingest·normalize, rag.corpus·retrieval, attackpath, engine 9개 run_demo + run_e2e + contracts validate) 재통과`
- `2026-07-03 / 진우 / [infra][engine][docs] infra/monitoring 운영 관측 레이어 완성(CLAUDE §5 "모니터링·관제·추적" 코드화) — Grafana IRSA(CloudWatch read-only, OIDC federated) + CloudWatch Dashboard 17위젯(기존 4→17: Lambda 6종·SQS 백로그나이·RDS 스토리지/IOPS·Step Functions(remediation)·S3 감사버킷·Cognito·Bedrock 호출/에러/토큰(모델별)+비용 metric math·엔진 EMF 4종 SEARCH기반) + CloudTrail→CloudWatch Logs 배관(로그그룹+IAM역할, 트레일 자체는 계속 수동 유지) + Teams 알림 풀스택(SNS·Lambda 실코드 lambda_src/teams_notifier.py·시크릿·알람 6종). terraform fmt+init(-backend=false)+validate 3개 통과, HCL 백슬래시 줄바꿈 문법오류 1건 작업 중 발견·즉시 수정. remote_state 출력명(eks_oidc_provider_arn·alb_arn_suffix·remediation_state_machine_arn·audit_bucket·cognito_user_pool_id) 전부 각 레이어 실제 정의와 대조 확인. CloudFront 위젯은 infra/console에 distribution ID output이 없어 변수 게이트(기본 비활성)로 스켈레톤만 — infra/console/outputs.tf에 alb_arn_suffix output 1줄 추가는 완료, cloudfront_distribution_id는 준형 쪽 요청 대기. 이어서 engine/reasoning/orchestrator.py의 run()에 EMF 계측(_emit_case_metrics) 추가(README §2③.1 스켈레톤 그대로, AWS_LAMBDA_FUNCTION_NAME 없으면 미발행이라 run_demo·run_e2e 무회귀 확인) — 엔진 축 위젯·트리아지 알람이 실데이터 받을 준비 완료. docs/manual-infra.md(Teams 웹훅 레닥션 유지+infra/monitoring 실 연동상태 반영, CloudTrail 연동 준비 각주 추가)도 함께 갱신`
- `2026-07-02 / 진우 / [rag] rag/retrieval 검색·답변 구현 완료 — RAGRetriever(mock=control_id 직접 매핑/real=pgvector cosine), RAGAnswerGenerator(mock=템플릿/real=Bedrock Claude Sonnet converse), mock_corpus.py(14 control 한국어 청크 24개, 계약⑥ 구조). python -m rag.retrieval.run_demo: 골든 6개 OK ✅ (control_id 정합·rag_refs·설명 생성·evidence 반영·코퍼스 14종 전체 커버). 실배포 스왑 = RAGRetriever(mock=False)+RAGAnswerGenerator(mock=False) (PG_DSN+Bedrock 모델 액세스 후)`
- `2026-07-02 / 진우 / [scanners] scanners/workload/trivy 워크로드 스캐너 구현 완료 — TrivyScanner(scan_image: trivy CLI→계약⑤ ingest-envelope, scan_from_json: mock/CI용). python -m scanners.workload.run_demo: 3 CVE→INTERNAL-VULN-KEV-001, resource_id 캐논, pillar=vuln, 골든 정합 OK ✅. 계약 검증(ingest-envelope·finding 스키마 수동 대조) 통과. 실배포 스왑 = scan_image("ECR이미지:tag") 호출만`
- `2026-07-02 / 진우 / [pipeline] pipeline/normalize 정규화부 구현 완료 — Normalizer(ASFF·prowler-json·trivy-json → OCSF-lite finding 변환), control-catalog 역인덱스(정확+와일드카드 매핑), resource_id 캐논화, severity 변환, dedup(sources 누적). python -m pipeline.normalize.run_demo: 골든 control_id 7종 전부 매핑, UNKNOWN 없음, dedup 9→8건 OK ✅. 실배포 스왑 = Lambda 핸들러에서 Normalizer().normalize(envelope) 호출만`
- `2026-07-02 / 준형 / [engine][infra] Phase1(엔진 실 tool-use) 배관 작성 — apply/test는 준형과 함께 대기. ①engine/core/tools.py에 RealToolExecutor 추가(boto3 read-only, MockToolExecutor와 동일 인터페이스, boto3 지연 import로 Mock/run_demo 무영향, allowlist는 base가 강제). slice 범위=무료 S3 read-only(GetBucketPolicy·GetPublicAccessBlock)만 실구현, macie/iam/ec2는 NotImplementedError. ②infra/slice/ 신설(공개 S3 1개+가짜 PII, infra/shared 무관 독립, force_destroy, enable_public_policy 토글=계정BPA 대비). py_compile OK·run_demo 무회귀(4회·confirmed)·terraform fmt·validate 통과. 남은 것(준형과 함께): Bedrock LLM tool-use 플래너(진짜 tool use 핵심) + apply→test→destroy`
- `2026-07-02 / 준형 / [docs] 진우 App Reg(ef0ad44) 파악 + 실전환 정합 2건 명시 — manual-infra §3.6.4 신설: ①mock placeholder GUID(n4 b2c3d4e5…·n5 a1b2c3d4…)→실 appId(order-sp 541938e7…·overpriv-app 283ca885…) 스왑(실 finding 흐를 때) ②f16 노드 매핑 애매(contracts는 f16→n4 SP인데 실물은 무만료 시크릿을 overpriv-app=n5쪽에 붙임) → 진우와 n4/n5 확정 필요. 지금은 mock이라 정상. CLAUDE §7.1 진우 진행에 App Reg 3종 ✅ 반영`
- `2026-07-02 / 진우 / [infra] Azure App Registration 3종 + Federated Credential 생성 완료 — ①overpriv-app(Directory.ReadWrite.All, 24개월 시크릿, f8·f16 소스) ②order-sp(Directory.Read.All+Application.Read.All, 6개월 시크릿, f5 평문 노출 예정) ③prowler-sp(Directory.Read.All+Policy.Read.All+AuditLog.Read.All, 시크릿 없음, GitHub OIDC Federated Credential). manual-infra.md §3.6 갱신`
- `2026-07-01 / 준형 / [docs] docs/cost-strategy.md 신설(포폴 셀링포인트) — 비용 최적화를 "돈 없어서"가 아니라 "의도적 엔지니어링 트레이드오프"로 정리한 결정 원장: mock-first(빌드 대부분 AWS $0)·apply→destroy 규율·경량 대체(NAT Instance/RDS t3.micro/pgvector)·Bedrock 모델 티어링+Triage 게이트·Azure 평가판+신원만·거버넌스 회피. 각 결정에 절감+트레이드오프, 정직한 "포기한 것" 절, 비용 order-of-magnitude 표. README docs 안내·폴더트리에 링크`
- `2026-07-01 / 준형 / [docs] attackpath/README.md 신설 — model+correlation 전체 워크스루(R1~R5 표·그래프 모델·2-pass 흐름·목업→실배포 스왑)`
- `2026-07-01 / 준형 / [attackpath] attackpath/model 그래프 데이터 모델 구현(Node·Edge·AttackPathGraph·validate_graph — 계약③ SSOT, cross_cloud↔경계횡단 불변식 검사) + correlation.py 리팩터(인라인 _node/_edge 제거→모델 사용, R1~R5 로직 무변). 계약③ additionalProperties:false 위반이던 finding_ids를 출력에서 제거(모델이 backfill provenance로 보유). run_demo.py 윈도우 UTF-8 수정. python -m attackpath.run_demo 5노드/4엣지/backfill 8건 골든 정합 OK, exit=0`
- `2026-07-01 / 준형 / [engine] engine/README.md 5단계 워크스루 상세화(§0 챗봇탈출 기준~§7 실험거리, 출력↔코드 매핑·파일링크) + run_demo.py 윈도우 콘솔(cp949) UnicodeEncodeError 수정(sys.stdout.reconfigure utf-8 강제 — 한글·em-dash 출력 크래시 방지). 준형 복습·진우 클로드 이해·심사자 설명 겸용 문서화`
- `2026-07-01 / 준형 / [apps-console] 진우 Cognito SSO 3건(a2b2a2f) 검증 완료 — 전부 정확(authenticate-cognito 전용액션·Identity Pool 제거·그룹 클레임 경로). auth.ts·Login.tsx 용어 정합(authenticate-oidc→cognito, cognito:groups→custom:groups). 코드 로직 변화 없음(주석/스텁만)`
- `2026-07-01 / 준형 / [apps-console] README 현행화(apps/console 골격 🔨·TF state 부트스트랩 ✅ 반영) + 파비콘 추가(public/favicon.svg — 방패 안 크로스클라우드 attack-path 모티프: AWS 주황→Azure 파랑, 붉은 횡단 엣지)`
- `2026-07-01 / 준형 / [apps-console] 진우 앱 피드백 반영 + 디자인 고도화 — role 스토어(useSyncExternalStore) localStorage>VITE_MOCK_ROLE>viewer, 헤더 역할 스위처, ErrorBoundary 최상위 추가. 공용 ui(Card/Skeleton/SectionTitle/ErrorNote)·StatCard, 대시보드 KPI 4카드+점수 바+기둥분포, Findings 좌측 심각도 컬러바+집계+스켈레톤. tsc·build(501 모듈) 통과`
- `2026-07-01 / 준형 / [apps-target] member를 'shop 데모 포털'로 확장 — /(포털: 설명+3서비스 카드+골든 경로)·/product·/order(소개+결함표)·/members(회원 관리 UI). JSON API는 /api/members로 이동. 쇼핑백 파비콘 추가. 4페이지+파비콘+API 전부 200 확인`
- `2026-07-01 / 준형 / [engine] 엔진 Evidence·Triage 목업 능동조사 구현 완료 — 트리아지 게이트(20→12 승급) → Evidence read-only 툴 4회 자가 호출(챗봇 탈출) → confirmed 판정 → case(계약⑦) 스키마·골든 정합 검증. MockToolExecutor·규칙 플래너로 RealToolExecutor(boto3)·Bedrock LLM 스왑 준비 완료`
- `2026-07-01 / 진우 / [attackpath] attackpath/correlation 구현 완료 — CorrelationEngine R1~R5 규칙(KEV+SG->과도IRSA->평문Azure cred+SP->공개S3+PII->Entra App). 체인 >=3 Critical 격상, 2-pass backfill. python -m attackpath.run_demo: 5노드 AWS3+Azure2, 4엣지, cross_cloud credential_theft, backfill 8건 골든 정합 OK`
- `2026-07-01 / 진우 / [engine] engine 폴더 재구성 완료 — hypothesis/+orchestrator/ → reasoning/ 1폴더로 합침(준형 폴더 규칙 반영). import 경로 갱신, 빈 캐시 폴더(triage/ 포함) 전부 삭제. python -m engine.run_demo 재실행 exit=0. engine/ = core/(공유)+evidence/(준형)+reasoning/(진우) 3폴더 확정`
- `2026-07-01 / 진우 / [engine] Hypothesis·Reasoning·Orchestrator 구현 완료 — HypothesisAgent(control→가설 템플릿, cross_cloud edge 감지), ReasoningAgent(verdict→한국어 내러티브·risk_level·권고사항), Orchestrator(전체 루프 단일 클래스), case.set_reasoning() 추가. run_demo.py Orchestrator 기반 리팩토링. python -m engine.run_demo: 20→12 escalate, 툴 4회, confirmed, CRITICAL, stage=reasoning, 골든 정합 OK`
- `2026-07-01 / 진우 / [infra] Azure/Entra ID 초기 설정 완료 — cnappagentic.onmicrosoft.com 테넌트(M365 체험), jw_kim Global Admin·jh_lee Global Admin, cnapp-viewer·cnapp-approver 보안 그룹, viewer@·approver@ 데모 계정, Teams cnapp-agentic 워크스페이스·cnapp-alerts 채널·Workflows 웹훅 생성. manual-infra.md §3 갱신`
- `2026-07-01 / 진우 / [apps-console] Cognito SSO 설계 검증 3건 반영 — authenticate-oidc→authenticate-cognito 전면 교체(project-draft·console-app-design 전 위치), Identity Pool 제거(프론트가 AWS 직접 호출 없음), §7 그룹 클레임 매핑 경로(Entra SAML attr→custom:groups→x-amzn-oidc-data→Lambda 분기) 추가`
- `2026-07-01 / 진우 / [infra] S3 Terraform state 버킷(cnapp-agentic-tfstate, ap-northeast-2) 생성 완료 — 버저닝·SSE-S3·퍼블릭차단 설정. infra/shared main.tf backend bucket 교체, manual-infra.md §2 ✅ 갱신`
- `2026-06-30 / 준형 / [docs] 공통 계약 7종(4.4) + terraform 레이어링·폴더소유(4.6) 확정`
<!-- 여기부터 실제 항목을 위에 추가 -->
