# CLAUDE.md

> 이 파일은 Claude Code(및 협업자)가 이 레포에서 작업할 때 가장 먼저 읽는 기준 문서다.
> 상세 설계는 [docs/project-draft.md](docs/project-draft.md)(메인 설계서 v5), [docs/target-app-design.md](docs/target-app-design.md)(타깃 앱 상세), [docs/console-app-design.md](docs/console-app-design.md)(관제 앱 상세)에 있다. **이 셋이 단일 진실 공급원(SSOT)이며, 본 문서는 그 요약 + 협업 규칙이다.**

---

## 0. 작업 규칙 (모든 클로드·협업자가 지킬 것) ⚡

1. **시작 전:** 어떤 작업이든 시작 전 반드시 이 **CLAUDE.md를 먼저 읽고**, `git pull`로 최신 main을 받는다.
2. **변경 로그 기록:** 작업 중 중요한 변경·결정·방향 전환이 생기면 아래 [변경 로그](#변경-로그-최신이-위로)에 한 줄로 남긴다. *사소한 수정(오타·포맷 등)은 적지 않는다 — 다른 사람이 알아야 할 것만.*
3. **`[PULL 필요]` 태그:** 상대가 반드시 pull해야 할 중요 변경(설계 방향 전환, 핵심 결정, 구조 변경)은 변경 로그에 **`[PULL 필요]`** 태그를 붙여 명시한다.
4. **작업 후:** `commit` + `push`로 공유한다(커밋 메시지 `타입: 내용`). 상세 협업 규칙은 [6번](#6-협업-규칙-) 참조.
5. **작업 로그 기입:** 구현하며 겪은 문제·해결과 추가·작업한 것은 루트의 **[troubleshooting.md](troubleshooting.md)** 에 한 줄씩 남긴다(중앙 1개 파일, 영역별 파일 금지). 형식 `YYYY-MM-DD / 작성자 / [영역] / 내용`, 영역 태그 `[infra]` `[scanners]` `[pipeline]` `[engine]` `[rag]` `[attackpath]` `[apps-target]` `[apps-console]` `[contracts]` `[docs]` `[ci]`. *굵직한 설계 변경*은 여기 말고 위 변경 로그에(목적 분리: troubleshooting=작업 디테일, 변경 로그=pull 알림).
6. **진척 갱신:** 굵직한 진척(영역 완료·다음 착수 변경)이 생기면 **[§7.1 현황+다음](#71-현황--다음-할-일-next-up--진행-관리-단일-지점-작업-후-여기부터-갱신)**을 갱신한다(진행 관리 단일 지점 — "지금 어디까지/다음 뭐"를 여기서 관리).

### 변경 로그 (최신이 위로)

> 형식: `날짜 / 작성자(준형·진우) / 한 줄 요약 / (필요 시 [PULL 필요])`. 최근 10~15개만 유지하고 오래된 항목은 [아카이브](#변경-로그-아카이브)로 내린다.

- **2026-07-07 / 준형 / 🔢 콘솔 하드코딩 3종(감사로그·컴플라이언스·AWS/Azure 점수) → 실 RDS 산출로 전환 + 실 HITL 조치기록 감사 편입** — "감사로그·점수가 아직 목업 아니냐"는 지적에, MVP 때 상수로 남았던 `console-backend/src/data.ts`의 3개를 실데이터화(라이브 반영·검증). **① 컴플라이언스:** ISMS-P 프레임워크↔control 매핑은 표준이라 정적 유지, pass/fail·건수·score는 `findings` 실집계(control별 open 수) — **조치로 finding이 사라지면 통제가 pass로 뒤집힘**(2.6.4 S3공개차단이 내 0006 조치로 실제 pass 전환, score 23). **② 감사로그:** `remediation_requests`(HITL)+`cases`(엔진 판정)+`findings`(스캔 first_seen/sources)를 시간순 병합한 실 RDS 이벤트(12건 — 실 Prowler 스캔·confirmed 판정·HITL 승인). 실제로 한 0006 조치(jh_lee 승인→S3 차단 applied)를 `remediation_requests`에 1건 기록해 "승인→적용" HITL 흐름 편입(ASCII-only SQL). mock(`MOCK_AUDIT`)은 dev/CI 픽스처로 분리. **③ AWS/Azure 점수:** open findings 심각도 가중 posture로 산출(33/83) + 라벨 "실 open findings 기반 posture"로 정정 — Security Hub는 이 계정 구독제약으로 실점수 불가·Azure Defender는 집계 대기라 findings 기반으로 정직하게 대체. **⚠️ 옛 원칙 폐기:** "mock=real 값 동일해야 MSW→실 API 스왑 성립"(2026-07-03)은 이제 의도적으로 무효 — real 모드가 실 RDS 값을 반환(mock과 다름이 정상). console-backend tsc+esbuild 빌드→Lambda 코드 재배포→라이브 API 3종 검증. **미결:** 대시보드 #1 스크린샷은 옛 62/74라 실 33/83과 불일치(사용자 판단으로 재캡처 보류). **`[PULL 필요]`** (진우: `apps/console-backend/src/data.ts`·`handler.ts` — getScores/getAudit/getCompliance를 async+실쿼리로. Lambda 재배포됨. monitoring 무관)
- **2026-07-07 / 진우 / 🤖 Prowler 스캔 자동화 — GitHub Actions cron으로 AWS+Azure 매일 스캔→S3→기존 파이프라인 자동 합류** — 설계 문서엔 진작부터 있던 계획("GitHub Actions cron → S3 → EventBridge → ingest")을 실제로 구현. **① `infra/backend`**: Prowler 결과 드롭용 S3 버킷(`prowler_results`, 30일 lifecycle) 신설 — 코드(`pipeline/ingest/ingest.py from_s3_event()`)가 클래식 S3 Event Notification 형태(`Records[].s3...`)를 기대하는 걸 발견해 **EventBridge가 아니라 S3→Lambda 직접 알림**으로 배선(더 단순하고 코드와 정확히 일치). **② `infra/shared`**: `github_ci`(OIDC) 역할에 지금까지 ECR push 권한만 있던 걸 Prowler 스캔에 필요한 AWS 관리형 `SecurityAudit` 정책 + 새 버킷 쓰기 권한 추가(레이어 역방향 참조 불가라 예측가능한 버킷명으로 ARN 구성 — 실 리소스는 backend가 생성). **③ `.github/workflows/prowler-scan.yml` 신설**: `schedule`(매일 KST 02시)+`workflow_dispatch` 둘 다 지원, AWS는 기존 OIDC 역할 재사용·Azure는 `prowler-sp`의 Federated Credential(둘 다 키리스, D4)로 인증 → `--az-cli-auth`로 로컬 세션 재사용 없이 CI 자체 인증. **인프라 destroy 상태 대비**: 첫 스텝에서 대상 S3 버킷 존재를 확인해 없으면 나머지를 조용히 skip(`build-images.yml`과 같은 이유로 "매일 빨간 실패" 방지). Prowler 자체는 FAIL 발견 시 non-zero exit이라 스캔 스텝에 `continue-on-error` 처리. `terraform fmt`+`validate` 2개 레이어 통과. 아직 워크플로 실 실행(GitHub Actions에서 트리거)은 검증 전 — 다음 세션에서 `workflow_dispatch`로 1회 수동 실행해 실제로 S3→ingest까지 관통하는지 확인 필요. **`[PULL 필요]` ⚠️ 준형 apply 필요(진우가 직접 apply 안 함)**: `infra/shared`(`github_ci` 역할에 `SecurityAudit`+버킷쓰기 권한 추가) + `infra/backend`(`prowler_results` S3 버킷 신설) 코드는 완성·validate 통과했지만 **아직 라이브에 apply 안 함**. 이유: `infra/shared`에서 `terraform plan`을 진우 계정(jw_kim)으로 돌려보니, 마지막 apply가 준형(jh_lee) 계정이었어서 **"cluster_creator" EKS access entry가 destroy+재생성되는 의도치 않은 부수 변경**이 같이 잡힘(호출자 계정이 바뀌면 이 access entry가 그 계정 기준으로 다시 계산되는 구조 — `cluster_admin_principal_arns`로 관리되는 별도의 안정적 admin 목록과는 무관하지만 그래도 손 안 대는 게 안전 판단). **준형이 본인 계정(jh_lee)으로 `infra/shared`를 apply하면 이 drift 자체가 안 나타날 가능성이 높음** — 다음 작업 시작 시 `git pull` 후 `infra/shared`→`infra/backend` 순서로 `terraform apply` 부탁드립니다(순서 중요 — backend가 shared의 새 IAM 출력을 참조하진 않지만 레이어 순서 원칙 유지). apply 후 GitHub Actions에서 `prowler-scan` 워크플로 `workflow_dispatch`로 1회 수동 실행해 S3→ingest→콘솔 반영까지 확인 필요(§7.1 진우 다음 항목 참고).
- **2026-07-07 / 진우 / 📊 Grafana 실데이터 완전 정상화(CloudWatch 버그 4건) + 골든 시나리오 파이프라인 검증 + Postgres 데이터소스·5번째 대시보드 신설 + Grafana 보안 강화(admin 비밀번호 교체) + Defender for Cloud 시도(보류)** — 준형 풀사이클 재구축 뒤 모니터링 레이어를 처음부터 다시 손봄. **① CloudWatch 대시보드 3종("No data") 완전 정상화**: 원인이 하나가 아니라 4개 층위였음 — ⓐ 손으로 작성한 CloudWatch 패널 JSON에 `queryMode`/`metricQueryType`/`metricEditorMode` 판별 필드가 없어 그라파나 프론트가 쿼리 모드를 못 정해 자동실행 자체를 안 함(61개 타겟 일괄 추가) ⓑ Bedrock 비용 패널(metric math expression)만 `metricEditorMode:0`을 넣으면 AWS가 400(Namespace 요구), 필드 없으면 프론트가 쿼리를 요청에서 통째로 드롭 — **expression 쿼리는 반드시 `metricEditorMode:1`(Code)** ⓒ Cognito 로그인 패널이 `SignInSuccesses`를 보고 있었는데 SAML 페더레이션 로그인이라 실제론 `FederationSuccesses`(+`IdentityProvider:EntraID`)로 발행됨. 전부 `list-metrics`·Grafana Inspect(Query 탭 raw request/response)로 실측 확인 후 수정. **② 골든 시나리오 실행**: ingest Lambda→SQS→normalize→correlation→orchestrator(실 Bedrock Haiku) 전체 관통 + remediation Step Functions 실행(member-pii-prod 버킷 `s3_block_public`, 이미 막혀있어 멱등·실질 영향 없음, 사용자 명시 승인)으로 Grafana의 나머지 빈 패널(ingest·remediation·Step Functions) 채움 — 이 과정에서 **준형이 콘솔 UI로 동시에 같은 조치를 테스트 중인 걸 발견**(골든 f6가 실제로 remediated로 전환됨, 양쪽 다 같은 기능을 다른 경로로 검증한 셈). **③ Grafana Postgres 데이터소스 + 신규 대시보드**: RDS에 `grafana_readonly`(SELECT-only) 계정 신설 — 마스터 비밀번호를 사람/로그에 노출 안 하려고 EKS 안 일회성 Job(자체 IRSA로 시크릿 접근→계정 생성→새 비밀번호를 K8s Secret에 직접 기록, 임시 IAM 역할·RBAC은 즉시 삭제)으로 처리. **콘솔 앱(제품 콘텐츠)과 겹치지 않게** — 새 대시보드 "데이터 파이프라인 운영 건강"은 finding 내용이 아니라 정규화 매핑 실패율·스캐너별 유입량 이상·트리아지 지연·조치 적체·row count 등 **파이프라인 자체 건강**만 봄. **④ Grafana 보안**: admin 비밀번호를 차트 기본값(`prom-operator`, 공개적으로 알려진 값)에서 전용 랜덤 비밀번호로 교체(외부 도메인 공개 상태라 필요, k8s Secret 참조 방식 — ArgoCD self-heal이 UI 직접변경을 되돌리는 걸 피함). **⑤ ArgoCD 컨트롤러 버그 발견·해결**: 완료된 PreSync hook을 계속 "Running"으로 착각해 sync가 안 끝나는 증상 — application-controller 재시작(공유 인프라라 준형 확인 후 진행)으로 해결. **⑥ Defender for Cloud는 보류**: `Discovery`·`FoundationalCspm` Standard 활성화까지 했으나 1시간+ 지나도 secure score/assessment가 안 뜸(M365 체험판 트라이얼 구독의 평가 파이프라인 제약으로 추정) — 다음 세션 재시도 또는 포기 결정 필요. 상세는 `infra/monitoring/README.md` §3.2·3.4·3.5·7, `troubleshooting.md`. **`[PULL 필요]`**(준형: `gitops/monitoring/dashboards/*.yaml` 3개 수정+1개 신규, `gitops/monitoring/kube-prometheus-stack-values.yaml`(Postgres 데이터소스+admin 비밀번호 배선) — EKS 재생성 시 `grafana-pg-datasource`·`grafana-admin-credentials` 두 k8s Secret을 재발급해야 함, 절차는 README §3.4·3.5)
- **2026-07-07 / 준형 / 🖼️ 포폴 스크린샷 9장 재캡처(주소창+로그인 계정 증거) + Evidence 문구 정리·라이브 재배포 + Evidence 케이스 골든 confirmed 복원(0007) + Defender for Cloud 확인** — 포트폴리오용 콘솔/ArgoCD/CloudWatch 스크린샷을 **주소창(라이브 도메인 cnapp-agentic.cloud)+로그인 APPROVER 계정**이 보이게 재캡처(headed Chromium + PowerShell **PrintWindow** — `page.screenshot`은 브라우저 크롬 미포함이라 대상 창만 PrintWindow로 캡처, z-order 무관·IDE 뒤여도 OK, 상세 troubleshooting). **① EvidenceTab 문구 정리:** 판정 히어로의 "(챗봇이 아닌 능동 조사)" 제거 → SPA 재빌드·S3·CloudFront 무효화까지 라이브 반영(cnapp-agentic.cloud). **② Evidence 케이스 골든 confirmed 복원:** 라이브 재조사가 실 AWS에서 mock 리소스를 못 찾아(0006은 조치로 차단됨) DB 케이스 5개가 전부 refuted였던 걸, 설계된 골든 confirmed 케이스(0.93)를 finding 0007(open·member PII)에 복원(공유 RDS UPDATE 1건 — 크라운주얼 evidence 샷용). **한글 seed 함정:** psql이 UTF-8 파일 한글을 깨뜨려(PGCLIENTENCODING=UTF8도 불충분) JSON을 `\uXXXX` ASCII로 넣어야 정상(troubleshooting 기록). **③ Defender for Cloud 확인(진우가 켬):** Foundational CSPM·Discovery만 Standard(무료 활성), 유료 플랜 전부 Free(비용 0), secure score 미집계(방금 켬)·Azure는 신원 전용이라 리소스 CSPM 대상 적음(신호는 Entra CIEM). **`[PULL 필요]`** (진우: `apps/console/src/components/EvidenceTab.tsx` 문구 1줄 변경 — 라이브 반영됨. finding 0007 evidence 케이스가 골든 confirmed로 복원된 DB 상태 참고. monitoring 무관)
- **2026-07-07 / 준형 / 🚀 풀사이클 재구축(destroy→전 레이어 재apply) + ArgoCD 최초 손 부트스트랩·타깃 앱 실기동 + 실데이터 크래시 2건·PII seeder 403·Karpenter 고아노드 근본수정 + 관제 콘솔 사용 매뉴얼 + deploy.ps1 하드닝** — "다시 test하게 다 올려줘"부터 시작해 전 스택을 새로 세우고 실동 검증 중 드러난 실버그들을 봉합. **① 풀 destroy→재apply:** 어제 destroy했던 걸 `deploy.ps1`로 5레이어 재apply(shared→karpenter→target→backend→console, monitoring은 진우 별도) + console 커스텀도메인/SSO. **Fresh apply라 Cognito 풀ID가 바뀜**(`ap-northeast-2_g7Q21LGFJ`) → SPA 새 client ID로 재빌드·재배포, **Entra SAML 식별자 교체는 사용자 수동**(manual-infra §5). **② ArgoCD 최초 손 부트스트랩:** ArgoCD 설치(install.yaml 커서 `--server-side` 필수) + `app-target` Application → **Git→클러스터 reconcile로 member/product/order 자동 배포**(각 2 replica) + metrics-server·HPA·PDB. **타깃 앱 이미지는 `build-images.yml`(GitHub Actions workflow_dispatch, 사용자 클릭)로 ECR push** → 파드 Running(CI→ECR→ArgoCD 완전 관통 증명). ArgoCD **Synced/Healthy**(내 rollout restart 애노테이션 드리프트는 제거해 정착). **③ 🔴 실데이터 크래시 2건(핵심·mock엔 안 나던 것):** ⓐ **Finding 상세 페이지 전체 크래시** — console-backend 실쿼리 `F_COLS`에 `sources` 누락 → 프론트 `f.sources.join()`이 `undefined.join()`(mock엔 sources 있어 안 터짐). F_COLS에 sources·first_seen·last_seen 추가 + 프론트 방어적(`?.`·`??[]`). ⓑ **Evidence 탭(크라운주얼)이 실모드에서 반쪽** — 실 case 쿼리가 `triage·hypotheses·evidence_meta·model_trace` 누락 → 전 필드 SELECT. **교훈: mock(계약 JSON=전 필드) vs real(pgvector=선택 컬럼) 2경로라, 프론트 필드↔실쿼리 컬럼 전수 대조 필요.** playwright 9/9 페이지 렌더 검증 + 실 API 필드 감사로 확정. **④ PII seeder 403 근본수정:** member IRSA 정책 `s3:HeadObject`(무효 IAM 액션) + `ListBucket` 부재 2단 함정 — HeadObject는 `s3:GetObject`로 인가되고 없는 객체 Head를 404로 받으려면 `s3:ListBucket` 필요(없으면 존재은닉 403). 둘 다 추가 → **합성 PII S3 적재 성공**(Macie 미끼 완성). **⑤ Karpenter 고아노드 근본수정:** 어제 destroy 2회 실패 원인(컨트롤러가 노드 회수 전 삭제→고아 스팟노드 pod ENI가 node SG 붙잡음)을 `deploy.ps1`에 **karpenter destroy 직후 `karpenter.sh/nodepool` 태그 인스턴스 자동 종료 스윕**으로 코드화. **⑥ deploy.ps1: monitoring을 'all'에서 제외**(진우 소유·별도 운영, `-Layer monitoring` 개별 유지·되돌리기 1줄). **⑦ 관제 콘솔 사용 매뉴얼 신설**(`docs/console-manual.md` — 화면별 기능·AI 동작·데모 동선·FAQ) + 비주얼 고도화(로그인/콜백 프리미엄·전역 fade-up·카드 그림자 대비·내비 가로스크롤 제거·컴플라이언스 PDF 활성). **⑧ 실 CSPM 3건:** 콘솔 findings=23 = 골든 20 + 실 계정 스캔 3(remediation-executor·Karpenter 역할·ECR — sources 빈 실 리소스). **검증:** playwright 9/9·실 API 전수 감사·EKS 3노드/RDS/Lambda/ArgoCD Synced·member API 200·PII 적재 확인. **`[PULL 필요]`** (진우: `infra/target`(member IRSA)·`apps/console-backend/data.ts`(실쿼리)·`apps/console/*`(방어·비주얼)·`infra/deploy.ps1`(고아스윕+monitoring 제외)·`docs/console-manual.md` 신규 — monitoring 무관. **deploy.ps1 destroy 'all'은 이제 monitoring 안 건드림 → monitoring은 `-Layer monitoring`으로 진우가 따로**)
- **2026-07-06 / 진우 / 📬 일일 비용·로그인 알림 실apply·실 Teams 도달 완료 + 채널 분리(`cnapp-cost`·`cnapp-login`) + Power Automate 함정 3건 규명(`infra/monitoring`)** — 앞서 코드만 작성했던 항목을 실제로 apply하고 Teams까지 뚫음. **핵심 함정**: Power Automate "HTTP 요청이 수신되면" 트리거는 Premium인 데다 **이 테넌트(Default 환경)가 모든 수동 트리거에 OAuth를 강제**(`DirectApiAuthorizationRequired`) — 새 Developer 환경을 만들어도 동일(테넌트 전역 정책으로 실측 확인), 옛 Incoming Webhook 커넥터도 이 테넌트에서 완전 제거됨 → 유일한 우회는 Teams 채널 "워크플로" 공식 템플릿("채널에 웹후크 알림 보내기")뿐(sig= 서명 URL 발급). **이 템플릿 기본 액션 "Post card"는 신뢰 불가**(messageBody를 AdaptiveCard로 직접 파싱 시도, 어떤 형태를 보내도 실패하거나 콘텐츠에 따라 불규칙하게 실패) → 삭제하고 "채팅 또는 채널에서 메시지 게시"(평문)로 교체가 유일한 안정적 방법. **채널 분리**: 비용·로그인은 성격이 달라 전용 채널+전용 흐름+전용 시크릿(`teams/webhook-cost`·`teams/webhook-login`)으로 분리하되, **일반 알람은 계속 `cnapp-alerts` 기본값**으로(채널 남발 방지 원칙 확정) — `daily_cost_notifier`·`login_notifier`가 SNS/teams_notifier를 안 거치고 각자 시크릿으로 직접 POST. **비용 알림 함정**: 이 계정 프로모션 크레딧이 실사용을 100% 상쇄해 순액(UnblendedCost)만 보면 매일 $0으로 보임(실측 2026-07-05: 순액 -0.00000009 USD, 실제 실사용 +0.3803 USD) → 실사용·크레딧·순액 3줄 다 표시하도록 수정. 로그인 알림 시각도 UTC→KST 변환. 전부 실 Lambda invoke·실 콘솔 로그인으로 Teams 도착 확인 + EventBridge 스케줄 `State=ENABLED`(매일 09:00 KST) 확인. 상세는 `infra/monitoring/README.md §17`. **`[PULL 필요]`** (준형: `infra/monitoring` 전용 변경 — 다른 레이어 무관)
- **2026-07-06 / 준형 / 🎨 관제앱 대규모 비주얼 고도화 + AI 어시스턴트 챗 UI + SSO 실 로그인 라이브 + 보안서비스 계정제약 규명** — 사용자 대면 완성도 끌어올림(전부 라이브 배포·검증). **① 관제앱 비주얼 고도화(전 페이지):** 디자인 시스템(brand 액센트·layered 그림자·`Badge`/`Card` 프리미티브) + 그라데이션 로고·활성 pill nav·blur 헤더·soft 배경 shell + 대시보드(히어로·아이콘 KPI·그라데이션 스코어바·프리미엄 attack-path 배너) + **Evidence 탭(AI 능동조사) = 판정 히어로 + tool 호출 타임라인 stepper**(챗봇 탈출 시각화) + FindingDetail 세그먼트 탭 + Findings·조치·AttackPath·컴플라이언스·감사 전부 프리미엄 톤 통일. tsc·vite·playwright 무회귀, S3 배포+CloudFront 무효화. **② AI 어시스턴트 챗 UI 신설(`/chat`):** RAG 백엔드(Titan→pgvector→Bedrock Haiku)는 됐는데 UI가 없어 "AI를 못 느끼던" 갭 해소 — 프리미엄 챗(버블·타이핑·추천질문) + **근거를 chunk UUID → control_id + 미리보기로**, "pgvector 코사인 유사도 검색" 강조(백엔드 `data.ts` refs에 `metadata.control_id`·스니펫 추가). **③ SSO 실 로그인 라이브 완료:** Entra 앱 SAML 식별자를 현 Cognito Pool(`ap-northeast-2_axVuh328G`)로 교정 + jh_lee@·jw_kim@ 앱 할당 + jh_lee@→`cnapp-approver` → **`jh_lee@cnappagentic…` 실 로그인 성공, 우상단 APPROVER·실데이터 20건 확인**(manual-infra §5 완료 기록). **④ 🔴 보안서비스 계정제약 규명(중요·초기 오진 정정):** Security Hub·Macie·Inspector·GuardDuty **4종 모두 enable이 `SubscriptionRequiredException`** — standalone(Org 아님)·Admin·코드 아님·**free tier도 아님**. 계정이 이 보안서비스류만 구독 개시가 막힘(결제/계정검증 이슈, AWS Support/콘솔로만 해제). → **실 CSPM findings는 구독 불필요한 Prowler로 우회**(별도 세션). 현 콘솔 20건은 골든 시드. **부수:** 이 윈도우 `aws s3 sync`가 `.js`를 text/plain로 태깅해 SPA 순백화면 나던 함정 → `aws s3 cp --content-type`으로 배포(troubleshooting·메모리 기록). **`[PULL 필요]`** (진우: `apps/console` 전면 비주얼 변경 + `/chat` 페이지 신설 + `apps/console-backend/src/data.ts` refs 개선 — monitoring 무관)
- **2026-07-06 / 준형 / 🔐 플랫폼 보안 하드닝 2/2 — 서울 ACM+ALB HTTPS(#1)·RDS TLS(#2)·액세스 로깅(#6) + 콘솔 라이브 apply로 #3~#6 전부 실환경 검증** — 하드닝 1/2(#3 JWT·#4 CORS·#5 헤더)에 이어 마무리 + **라이브 apply·검증**. **#1 ALB HTTPS:** 서울(ap-northeast-2) ACM 인증서(CloudFront용 us-east-1과 별도, DNS 검증) → ALB **443 HTTPS 리스너**(TLS13-1-2, 옵션 B라 authenticate-cognito 없이 forward — SPA가 OIDC 직접·백엔드가 JWT 검증). HTTP:80은 CloudFront /api 프록시(http-only origin)가 쓰므로 forward 유지(전 구간 TLS[80→443+CloudFront https-only]는 api 서브도메인 필요 = 후속). **#2 RDS TLS:** psycopg2 4곳(pipeline/normalize·attackpath/correlation·engine/handler·remediation) `sslmode=require`(강제 암호화). **#6 로깅:** 전용 로그 버킷(SSE·PAB 엄격) + ALB access_logs + front S3 서버 액세스 로깅(둘 다 ACL 미사용=버킷정책 방식으로 안전). CloudFront 로깅은 awslogsdelivery ACL/standard-logging-v2 필요라 ACL 완화 회피 위해 후속. **🎯 라이브 검증(콘솔 apply exit 0):** playwright로 **SPA 정상 렌더+CSP 위반 0**(#5 CSP가 SPA 안 깨뜨림 실증)·`/api/findings` 실데이터 200·보안헤더 5종 라이브·ALB 443 리스너 present·access_logs enabled 확인. **부수:** 콘솔 apply가 SPA dist를 aws_s3_object(content_type mime맵)로 재업로드 → 앞서 `aws s3 sync`가 만든 **.js text/plain MIME 순백화면 버그가 terraform으로 영구 교정**(troubleshooting 기록). build-images.yml 옛 주석("order 빌드 안 함") 수정 — matrix는 이미 order 포함(옛 run "Re-run"은 order 없음, "Run workflow" main으로 새로 돌려야 order 빌드). **`[PULL 필요]`** (진우: `infra/console`·pipeline/engine handler·build-images.yml — monitoring 무관)
- **2026-07-06 / 준형 / 🔐 플랫폼 보안 하드닝 1/2 — JWT 서명 검증(실 취약점 봉합)·CORS 화이트리스트·CloudFront 보안 응답헤더** — 자기 제품(플랫폼) 도그푸딩. ⚠️ 타깃 앱 의도적 결함 f1~f8은 그대로(제품의 findings 소스라 유지) — 이 작업은 '플랫폼'(console·console-backend)만 대상. **#3 JWT(핵심 취약점):** `apps/console-backend/src/auth.ts`가 Bearer ID 토큰을 **디코드만 하고 서명 검증을 안 해**, 공격자가 `custom:groups=approver GUID`를 넣은 **위조 JWT로 조치 승인 권한 상승이 가능**했음 → `aws-jwt-verify`(Cognito JWKS)로 **서명·issuer·audience·exp 검증**, 검증기 미구성(env 없음)·검증 실패 시 **fail-closed(viewer)**. Lambda env `COGNITO_USER_POOL_ID`·`COGNITO_CLIENT_ID`(SPA 클라이언트=ID 토큰 aud) 배선(infra/console). **#4 CORS:** 응답에 CloudFront 오리진(도메인)만 화이트리스트(와일드카드 `*` 금지) + OPTIONS 프리플라이트(`ALLOWED_ORIGIN` env). **#5 CloudFront 응답헤더:** `aws_cloudfront_response_headers_policy`(HSTS 1년+preload·CSP[connect-src에 Cognito 도메인 포함]·X-Content-Type-Options nosniff·X-Frame-Options DENY·Referrer-Policy)를 SPA 기본 behavior에 강제. **검증:** console-backend tsc+esbuild 빌드(aws-jwt-verify 번들 229kb)·infra/console fmt+validate 통과. **⚠️ 적용 보류(코드+validate까지)** — 라이브 /api 프록시가 녹화용으로 정상 동작 중이라 CloudFront/Lambda 실 apply는 준형 세션에서(태스크 §검증 지침 "실 apply는 준형 세션"). **남은 하드닝(2/2):** #1 서울 ACM+ALB HTTPS 리스너·#2 RDS TLS(CA 검증)·#6 액세스 로깅. at-rest(#7)는 확인 완료(front/audit S3 SSE+PAB·RDS 암호화 이미 됨). **`[PULL 필요]`** (진우: `apps/console-backend`·`infra/console` 변경 — monitoring 무관)

- **2026-07-06 / 준형 / 🔐 관제앱 로그인 버튼/가드 실배포 + 인스턴스 이름 `default` 추방(라이브 교체) + Karpenter 혼합 용량(spot 우선·on-demand 폴백)·스팟 회수 복원력(PDB·topologySpread·order HPA)** — 사용자 대면 마감 + 노드층 아키텍처 하드닝(전부 라이브 apply 완료). **① 관제앱 로그인:** 라우터에 인증 가드가 없어 로그인 버튼이 안 보이던 것(루트가 대시보드로 직행) → `apps/console/src/App.tsx`에 가드(실데이터+미인증 → `/login` 자동 리다이렉트) + 헤더 유저메뉴(이메일·역할·로그아웃). SPA 재빌드 → S3 sync → CloudFront 무효화로 라이브 반영(이제 https://cnapp-agentic.cloud 열면 로그인 화면 자동 표시). **관제앱 실데이터 전수 검증**: `/findings`·detail(explanation+case 6 evidence 조인)·attack-paths(골든 5노드/4엣지)·scores·audit·compliance 전부 200 실 RDS, RAG `/chat` 실 Haiku(pgvector refs 4). **② 이름 `default` 추방:** shared EKS 관리형 노드그룹 `default`→`baseline`(라이브 apply, **7 add/7 destroy로 노드그룹만 교체·타 리소스 0**), Karpenter EC2NodeClass·NodePool `default`→`cnapp-spot` + Karpenter 인스턴스 Name 태그 `…-karpenter-spot`(EC2 콘솔 공란/기본값 제거). **③ Karpenter 혼합 용량:** `capacity-type [spot] → [spot, on-demand]` — 스팟 우선(비용) + 회수·부족 시 on-demand 자동 폴백(연속성), 평상시 추가비용 0. **단일 NodePool 유지(2-pool 분리 안 함)** — 프리티어(t3.small/micro 전부 스팟적격) + 전부 stateless라 on-demand 전용 풀은 전시용 복잡도가 됨(판단 근거 `infra/karpenter/README §2.1`). 프리티어 t3 제한·`limits.cpu 8`·consolidation 유지. **④ 스팟 복원력(파드층):** 타깃 앱 member·product·order에 **replicas 2 + topologySpreadConstraints(maxSkew 1·ScheduleAnyway) + PodDisruptionBudget(minAvailable 1)** 세트(셋이 함께여야 실효 — replica 1이면 PDB가 드레인 차단) + product/order resource requests(HPA CPU% 전제) + **order HPA 신설**(그동안 "동일 패턴" 주석만 있고 리소스 없던 누락). **검증:** terraform fmt/validate(karpenter Success) · kubectl dry-run 5개 OK · **shared+karpenter 라이브 apply 완료**(health gate 통과, NodePool `cnapp-spot` READY·`capacity-type=[spot,on-demand]` 확인·컨트롤러 1/1·t3.micro spot 노드 실가동). 문서: `infra/karpenter/README §2.1`·`gitops/README`·`docs/cost-strategy §2.7` 갱신. **`[PULL 필요]`** (진우: `gitops/autoscaling/`(hpa+pdb 신규)·`apps/target/*/k8s/deployment.yaml`·`infra/karpenter`·`infra/shared` 노드그룹 이름 변경 — monitoring 무관, 다음 monitoring apply 시 노드그룹 이름만 `baseline`으로 바뀐 것 참고)
- **2026-07-06 / 진우 / 🔧 EKS 대시보드 — 노드 "이름"은 EKS 구조상 불가, 대신 baseline/spot 구분으로 대체** — "IP 말고 이름으로" 요청에 실제론 AWS EKS 노드명 자체가 EC2 프라이빗 DNS(`ip-x-x-x-x...`)라 그 자체가 정식 이름이고 별도 친화적 이름이 없음을 확인(EC2 Name 태그도 무용 — 관리형 노드그룹 전부 동일값). 대신 더 유용한 구분인 **"상시 관리형 노드그룹(baseline) vs Karpenter 스팟(spot)"**을 범례에 추가 — `kube-prometheus-stack-values.yaml`에 `kube-state-metrics.metricLabelsAllowlist`(`eks.amazonaws.com/nodegroup`·`karpenter.sh/capacity-type`) 신설(기본은 카디널리티 보호로 이 라벨들을 비워서 노출 안 함). 범례 최종: `{{short_node}} ({{nodegroup}}{{capacity-type}}, {{az}})` 예 `ip-10-20-11-123 (baseline, ap-northeast-2a)`. 라이브 patch로 값 확인 중 kube-state-metrics Deployment도 ArgoCD self-heal 대상이라 원복되는 걸 재확인(같은 교훈 반복 — 매번 push 먼저). **`[PULL 필요]`** (준형: `kube-prometheus-stack-values.yaml`+`cnapp-eks-dashboard.yaml` 갱신)

- **2026-07-06 / 준형 / 🔴 실 findings 라이브 관통(스캐너→실 파이프라인→콘솔) + 전구간 TLS·CloudFront 로깅 마감 + 콘솔 AI·시스템 관측 뷰·AI 재조사 라이브 트리거** — "실 findings가 없다(시드뿐)"는 마지막 갭을 **실 스캐너 2종을 라이브로 돌려** 봉합 + 잔여 하드닝·관측 마감. **① 실 findings 관통(핵심):** 진우가 짠 스캐너 **코드**는 mock 검증까지였고 EKS가 없던 시점이라 **실행**을 못 했던 것 → 이제 라이브 EKS 위에서 실제로 돌림. **kube-bench**를 실 3노드에 Job으로 배포(`KubeBenchScanner.scan_cluster` 실경로) → CIS 4.1.1(cluster-admin 과다바인딩) **실 미스컨피그 탐지** → 실 `normalize` Lambda invoke(SQS 봉투) → RDS → **라이브 콘솔에 실 KSPM finding**. **Trivy**를 인클러스터 Job(hostNetwork로 노드 ECR 권한)으로 실 ECR product 이미지 스캔 → **실 CVE 205개** 집계(INTERNAL-VULN-KEV-001, sources 205) → 실 normalize Lambda → 콘솔. **콘솔 23건 = 골든 20 + 실 스캔 3**(kube-bench cluster-admin·Trivy KEV·UNKNOWN 1). **카탈로그:** `INTERNAL-KSPM-CLUSTERADMIN-001`(kube-bench:4.1.*) 신설로 실 finding이 UNKNOWN 대신 의미있게 매핑. **normalizer 노이즈 개선:** 실 클러스터 스캔이 쏟는 수백 PASS 미매핑 체크가 전부 UNKNOWN-remediated로 쌓이던 걸 — "매핑 안 되고 PASS면 finding 안 만듦" 규칙으로 차단(진우 파일 `pipeline/normalize/normalizer.py` `_parse_kube_bench`). **Prowler(CSPM/CIEM)는 라이브 미실행** — 이 머신 Python 3.8이라 pip 불가 + 인클러스터는 광역 read IRSA(SecurityAudit) 필요 + 출력이 비큐레이티드 계정 posture 노이즈(타깃 결함 토글 off라 골든도 안 맞음) → **객관적으로 데모 가치 낮아 opt-in 보류**(원하면 read-only IRSA 세팅해 스코프 스캔 가능). **② 전구간 TLS(#1 완성):** 서울 ACM에 `api.<도메인>` SAN 추가 + Route53 api→ALB alias + CloudFront /api 오리진을 **https-only(api 서브도메인)**로 전환 + ALB :80을 443 redirect → 뷰어→CloudFront→ALB 전 구간 암호화. **③ CloudFront 로깅(#6 완성):** 로그 버킷만 ObjectOwnership=BucketOwnerPreferred + awslogsdelivery 캐논유저 ACL grant(완화 범위 로그 버킷 1개, public은 PAB가 계속 차단)로 CloudFront 표준 로깅 활성. **④ 콘솔 AI·시스템 뷰(`/system` 신설):** "AI가 어떻게 도는지"를 앱 한 화면에 — 파이프라인 스트립·모델 3종·**RAG 지식베이스 통계(rag_chunks·pgvector HNSW)**·**Bedrock 사용량 24h(console-backend가 CloudWatch AWS/Bedrock GetMetricData 집계)**·데이터 현황. **⑤ AI 재조사 라이브 트리거:** Finding 상세에 approver 전용 "🤖 AI 재조사" 버튼 → `POST /findings/:id/reanalyze` → console-backend가 orchestrator Lambda **비동기 invoke**(실 Bedrock tool-use 재조사) → 지연 폴링 갱신. **⑥ AI 챗 정식 페이지 승격**(`/chat`, 네비 편입). **검증:** console-backend tsc+esbuild(232kb)·console vite(무회귀)·workload run_demo·terraform validate/apply(console 9add/9change·backend 재배포×2)·**라이브 API `/findings`·`/system` 200**·kube-bench/Trivy 실 스캔 JSON·normalize invoke `{normalized:N}` 실측. **⚠️ 잔여:** UNKNOWN 노이즈 1건은 auto-mode 가드가 공유 RDS DELETE를 막아 남김(normalizer가 앞으로는 안 만듦, 승인 시 삭제). **`[PULL 필요]`** (진우: `pipeline/normalize/normalizer.py`(kube-bench 파서 노이즈 규칙)·`contracts/control-catalog.json`(KSPM control 1종 추가) 변경 — monitoring 무관, mock 무회귀. 실 스캐너는 이제 라이브 검증됨)

- **2026-07-06 / 진우 / 🔧 EKS 대시보드 — 노드 이름 IP→식별가능하게 개선 + 패널 순서 재배치** — "노드가 IP로만 나와서 구분이 힘들다" 피드백 반영. node-exporter `instance`(IP:port)를 `kube_node_info`의 `internal_ip`로 join해 실제 노드명+가용영역을 얻고 범례를 `{{short_node}} ({{az}})`로 표시(예: `ip-10-20-11-58 (ap-northeast-2c)`) — Karpenter spot 노드가 어느 AZ인지 그래프에서 바로 구분됨. `kube_node_labels`(인스턴스타입 등)는 kube-state-metrics 기본 설정이 카디널리티 보호로 비활성이라 못 씀(추후 `metricLabelsAllowlist` 필요, 미착수 기록). **패널 순서도 요청대로 재배치**: 노드 CPU/메모리 → 파드 CPU/메모리 top10 → 노드별 파드개수/비정상목록 → 네임스페이스별 재시작. **트러블슈팅**: push+sync "Succeeded" 떠도 라이브 ConfigMap이 실제로는 안 바뀌는 재현 버그 발견(원인 미특정, `kubectl delete`후 재sync=create 경로로 우회 확인) — 매 대시보드 수정마다 이 패턴으로 검증 필요할 수 있음. **`[PULL 필요]`** (준형: `cnapp-eks-dashboard.yaml` 갱신)

- **2026-07-06 / 진우 / 📊 EKS 개요 대시보드에 파드/노드 이름별 지표 4종 추가(35패널)** — "파드 관련 지표가 없다"·"노드/파드 이름별로 보고 싶다" 피드백 반영. `cnapp-eks-dashboard.yaml`에 4개 패널 추가: **파드별 CPU/메모리 사용량 top10**(`namespace/pod` 레전드) · **노드별 파드 개수**(Karpenter bin-packing 확인용) · **비정상 파드 목록**(이름·상태 표시, `Pending|Failed|Unknown`만 — `Succeeded`는 완료된 Job이라 정상 제외). 전부 Prometheus 쿼리로 실데이터 확인(`container_cpu_usage_seconds_total`·`container_memory_working_set_bytes`·`kube_pod_info`). **`[infra]` 트러블슈팅**: 로컬 `kubectl apply`로만 테스트하고 git push 전이라 ArgoCD self-heal이 몇 초 만에 되돌려버려 한참 헤맴 — 이 레포 GitOps 대상은 항상 push 먼저, 그 다음 재sync로 검증해야 함(교훈 troubleshooting.md 기록). **`[PULL 필요]`** (준형: `gitops/monitoring/dashboards/cnapp-eks-dashboard.yaml` 갱신 — 다음 EKS 세션에서 자동 반영)

- **2026-07-06 / 준형 / 🌐 "구현한 거 다 켜기" — 커스텀 도메인 라이브 + 관제 콘솔 실데이터 브라우저 렌더(CloudFront /api 프록시) + SPA 실+OIDC 빌드 + target 파드 3종 배선** — 라이브 인프라 위에서 사용자 대면 기능을 실제로 켬. **① 커스텀 도메인:** `console apply -var enable_custom_domain=true` → ACM(us-east-1) 발급 + **DNS 전파 완료 상태라 검증 1초** + CloudFront alias + apex A레코드 → **https://cnapp-agentic.cloud HTTPS 200(유효 인증서)**. **② 관제 콘솔 실데이터(핵심 배선):** 콘솔은 HTTPS인데 백엔드 ALB는 HTTP라 브라우저가 mixed-content 차단 → **CloudFront에 `/api/*` → ALB 프록시 behavior 신설**(ALB를 2번째 오리진으로, CachingDisabled+AllViewer, 뷰어엔 HTTPS·오리진엔 HTTP)로 같은 오리진 HTTPS API 확보. `https://cnapp-agentic.cloud/api/findings` → 실 RDS 20건 JSON 200 검증. **③ SPA 실+OIDC 빌드:** `apps/console` `.env.production.local`(gitignore)로 `VITE_USE_MOCK=false`·`VITE_API_BASE=/api`·`VITE_COGNITO_*`(SPA client `7kp3b1...`·hosted UI 도메인·redirect `/callback`) 주입 재빌드 → console apply가 dist 교체 업로드 + CloudFront 무효화 → 실데이터 렌더 + **SSO 로그인 버튼 배선**(클릭→Cognito Hosted UI→Entra, 실제 Entra 완료는 진우 SAML 확인 §3.6.5). **④ gitops 파드:** CI(build-images.yml) OIDC 키리스로 member·product 이미지 ECR push → **member 1/1 Running(CI→ECR→ArgoCD→앱 실행 개념 완전 증명)**. product는 이미지 CMD가 **Dockerfile exec-form 함정**(JSON 배열 뒤 인라인 주석 → shell-form 폴백 → `/bin/sh: [sleep,: command not found` 크래시)으로 CrashLoop → **Dockerfile 근본수정**(주석 윗줄로) + 매니페스트 keep-alive `command`로 즉시 Running. **order** Dockerfile 신설(placeholder)·`build-images.yml` 매트릭스에 order 추가(다음 Action 재실행 시 product 클린 재빌드+order 빌드). **⑤ 접근 URL:** 관제 콘솔 https://cnapp-agentic.cloud · CloudFront https://d5c0my8expgl3.cloudfront.net · API ALB(http, CloudFront가 프록시) · Cognito Hosted UI `cnapp-agentic-demo.auth.ap-northeast-2.amazoncognito.com`. (ArgoCD/member LoadBalancer 공개는 권한 정책이 차단 → port-forward 안내.) `terraform validate` 통과. **`[PULL 필요]`** (진우: monitoring 무관. product/order Dockerfile·CI 매트릭스·CloudFront /api 프록시 추가 — 실 apply 시 참고)

- **2026-07-06 / 진우 / 📊 Grafana 분야별 대시보드 4종 신설 — AWS CloudWatch 24위젯과 1:1 대응(EKS 안팎 전부 Grafana에서)** — 기본 kube-prometheus-stack 대시보드 27개가 "쿠버네티스 클러스터 헬스"만 보여주고 "우리 CNAPP 서비스가 잘 도는가"는 안 보여준다는 지적 반영. 처음엔 12패널 통합 대시보드 1개로 만들었으나 "빠짐없이 다 보고 싶다"는 요청으로 **AWS CloudWatch 24위젯과 동등한 커버리지**로 재설계 — `gitops/monitoring/dashboards/`에 분야별 4개(**EKS 개요**·**애플리케이션**[Lambda 6종 개별+ALB+Cognito+CloudFront]·**인프라**[RDS+SQS+Step Functions+S3 감사버킷]·**AI**[Bedrock 전종+엔진 EMF 전종]) 31패널. `grafana_dashboard: "1"` 라벨로 sidecar 자동 로드. **`kube-prometheus-stack-values.yaml`에 CloudWatch datasource `uid: cloudwatch-monitoring` 고정**(안 고정하면 apply마다 랜덤값이라 대시보드가 매번 깨짐) — `app-monitoring.yaml` 소스③ 범위도 `presync/`만→`gitops/monitoring/` 전체(`directory.recurse`+values 파일만 exclude)로 확장. **검증 중 CloudWatch metric math 버그 3건 발견·수정**: expression 쿼리도 `statistic` 필드 필수(없으면 500) · 쿼리 id를 대문자로 넣으면 CloudWatch `Id` 규칙 위반으로 전체 400(math로 서로 참조하는 쿼리만 소문자 id 명시, 단독 쿼리는 id 생략) · wildcard 분포 패널은 dimensions 값이 빈 문자열이 아니라 배열 `["*"]`여야 함. Grafana API로 31패널 전부 실데이터 검증(28개 실값·3개는 최근 활동 없어 정상 empty) → ConfigMap 전환 후 sidecar 자동 로드 재확인. **⚠️ ALB ARN suffix·Cognito Pool ID·CloudFront Distribution ID 3종은 apply마다 새로 발급되는 비고정 값**이라 `infra/console` 재apply 후 수동 갱신 필요(gitops/README.md에 명시, Lambda·RDS·SQS·SFN·감사버킷은 이름 고정이라 무관). **`[PULL 필요]`** (준형: `gitops/monitoring/dashboards/` 신규 — 다음 EKS 세션에서 `app-monitoring.yaml` 재배포 시 자동 포함. `infra/console` 재apply 시 위 3개 값 갱신 필요할 수 있음)

- **2026-07-06 / 진우 / 🔧 `app-monitoring`(kube-prometheus-stack) 실배포 성공 + Prometheus 영구 미기동 버그 근본 해결(PreSync 훅)** — 준형 라이브 apply 후 `infra/monitoring` 재apply(로그그룹 이름 충돌 1건은 `terraform import`로 해결) → `kubectl apply -f gitops/argocd/app-monitoring.yaml`로 Grafana·Prometheus 최초 실배포 시도, **Prometheus가 영구히 `OutOfSync`로 안 뜨는 버그 발견·근본 해결**. 원인 2단: ① `alertmanagers`·`prometheuses` 등 대형 CRD 4종이 `kubectl apply`의 262144바이트 annotation 제한을 넘어 생성 실패(`ServerSideApply=true` syncOption만으론 미해결) ② CRD를 나중에 넣어도 operator 파드가 "CRD 없음" 상태로 이미 떠서 그 사실을 캐싱해버려 영원히 재확인 안 함(재시작 전까지 조용히 멈춤). **해결**: `gitops/monitoring/presync/prometheus-crds-job.yaml` 신설 — ArgoCD **PreSync 훅**(Helm 본 배포보다 먼저 실행)이 공식 CRD 전용 릴리스 자산을 `kubectl apply --server-side`로 먼저 설치해 원인①②를 애초에 차단(수동 개입 불필요, 자기정리). `app-monitoring.yaml`을 3-소스 Application으로 확장. **⚠️ 버전 고정 필수**: CRD 버전(`v0.75.2`)이 차트가 내부 고정하는 prometheus-operator 버전과 정확히 일치해야 `Synced`(안 맞으면 기능은 되지만 diff 안 깨끗 — 실측). 실배포 확인: Grafana 3/3·Prometheus 2/2 Running, Grafana ServiceAccount에 IRSA role-arn 정상 부착 확인. 상세 트러블슈팅 4건은 troubleshooting.md·`gitops/README.md`(신규 "함정" 절)에 기록. **`[PULL 필요]`** (준형: `gitops/` 신규 파일 — 다음 EKS apply 세션에서 `app-monitoring.yaml` 재배포 시 이 fix가 자동 적용됨. ArgoCD applicationsets CRD도 같은 크기제한 함정이라 이번 fix 패턴이 참고될 수 있음)

- **2026-07-06 / 준형 / 🔁 라이브 재검증(5레이어 apply→전 데이터평면 관통) + gitops 부트스트랩 첫 라이브 + 라이브 갭 2건 규명 + deploy.ps1 하드닝 + RAG 모델 스왑** — 인프라 다시 올려 전 데이터평면을 재관통(무회귀). **① apply 5레이어(monitoring 제외=진우 담당):** shared 77·karpenter 23(헬스게이트 통과)·target 6·backend 48·console 29 = **183개 원샷**. ⚠️ **deploy.ps1 함정 실측** — `-AutoApprove` 없이 백그라운드로 돌리면 terraform 승인 프롬프트에서 **무한 대기**(1시간 방치 후 발견, 리소스 0=비용0). → **`deploy.ps1` 하드닝**: apply/destroy 시 `[Console]::IsInputRedirected`(비대화형)면서 `-AutoApprove` 없으면 **2초 만에 명확히 실패**(무한 행 방지). monitoring 직전 중단 후 진우 state 오염(stale `.tflock`) 정리. **② 데이터평면 라이브 관통:** RDS 스키마(pgvector 0.8.0·6테이블) → ingest Lambda→SQS→normalize(실 RDS 3건 기록) → correlation(**골든 5노드 크로스클라우드 Critical**·8 backfill) → orchestrator(**실 Bedrock 6종 tool-use·7415토큰**·증거기반 refuted) → RAG 검색(Titan 쿼리임베딩→pgvector cosine top-4 의미랭킹 정확) → console ALB 전 엔드포인트 200 → remediation(실 S3 차단·Object Lock 감사·finding=remediated). **③ gitops 부트스트랩(프로젝트 최초 라이브):** ArgoCD 설치(⚠️ applicationsets CRD가 커서 `kubectl apply` 262144B 초과 → `--server-side` 필수) → app-target **Synced**, git→cluster reconcile(shop ns·Deployment 3·Service 자동생성) 증명. **④ 라이브 갭 2건(코드 아님·환경/계정):** ⓐ ECR 비어(이 머신 docker·gh 없음·`build-images.yml` 미트리거) → target 파드 ImagePullBackOff(GitOps 엔진은 증명, 이미지 아티팩트만 갭) ⓑ **Sonnet 4.5 Bedrock 액세스 미개방**(관리자 API로도 Marketplace subscribe 불가=콘솔 수동 승인 필요) → RAG 답변 500. **⑤ RAG 모델 스왑:** 액세스 열린 Haiku 4.5로 전환하되 **env 오버라이드**(`CHAT_MODEL_ID`·`RAG_MODEL_ID`, Sonnet 열리면 무코드 스왑) — `apps/console-backend/src/data.ts`(준형)+`rag/retrieval/answer_gen.py`(진우) → console-backend 재빌드·Lambda 재배포 → **`/chat` 라이브 200**(Haiku 실 답변·청크 근거·refs). 무회귀(rag·e2e·validate·tsc OK). **`[PULL 필요]`** (진우: `rag/retrieval/answer_gen.py` 기본 모델을 Sonnet→Haiku(env `RAG_MODEL_ID`로 오버라이드 가능)로 — Sonnet 계정 액세스 열리면 env만 세팅. mock 무회귀)

- **2026-07-04 / 준형 / 🚀🔴 라이브 풀스택 실검증(apply→전 데이터평면 관통→destroy) — 실버그 2건 포획·수정, 크라운주얼 실동작 증명** — "코드는 됐지만 라이브 관통은 안 해봤던" 데이터 평면 전체를 실 AWS로 관통. **전 6레이어 apply(210개, 처음 apply되는 psycopg2 레이어·5실핸들러·ECR IAM·karpenter 헬스게이트 전부 에러 0)** → **RDS 스키마 적용**(EKS 파드로 psql, pgvector 0.8.0+6테이블) → **파이프라인 관통**(ingest Lambda→SQS→normalize Lambda[psycopg2 실 RDS 쓰기]→correlation[골든 5노드 Critical·8건 backfill]→orchestrator) → **엔진 실 tool-use**(실 Bedrock이 9종 read-only API 자율 호출: GetBucketPolicy·GetBucketAcl·SimulatePrincipalPolicy 등 → 공개버킷·과도IRSA 실탐지, 증거기반 verdict) → **RAG**(실 Titan 24청크 임베딩 적재→pgvector cosine→/chat 실 Sonnet 답변) → **console real pg**(ALB→Lambda→RDS 전 엔드포인트 200, 조인 포함) → **remediation**(dry-run→실 S3 차단→Object Lock 감사→findings=remediated 소멸루프). **🔴 실버그 2건(라이브에서만 드러남):** ① `engine/core/tools.py` S3 핸들러가 `NoSuchBucket` 미포획 → 조사 전체 크래시(+ `bedrock_planner._run_tool` 광역 except로 방어심층화) ② `apps/console-backend` ALB 응답 `statusDescription`이 "200"이라 ALB가 502(→"200 OK" 형식 필요, 이전세션 "스키마탓" 오진 정정). 둘 다 수정·재배포 후 통과. **검증 후 destroy·잔존 $0.** 회귀(run_demo·run_e2e·validate) 무회귀. **`[PULL 필요]`** (진우: engine/core·evidence 파일 수정 — 실 tool 실행 예외를 error toolResult로 강등하는 방어. mock 무영향)

- **2026-07-04 / 준형 / 🧠 엔진 실 tool-use 전종 확장 + RAG 실 경로 완성(retriever·/chat) — "AI 능동조사 + 지식베이스 설명"의 실 배선 마감** — Phase2 라이브 전, 목업만이던 "핵심 조사·설명"을 실 코드로 채움(전 회귀 통과). **① RealToolExecutor 9종 확장:** `engine/core/tools.py`가 S3 2종만 실구현이던 걸 **계약④ AWS allowlist 전종**(s3 GetBucketPolicy·GetBucketAcl·GetPublicAccessBlock · ec2 DescribeSecurityGroups · iam SimulatePrincipalPolicy·ListAttachedRolePolicies·GetRolePolicy · macie2 GetFindings · accessanalyzer ListFindings)으로 확장 → 골든 경로 f3(개방SG)·f4(과도IRSA)·f6(공개S3)·f7(PII)을 **실 증거로 조사** 가능. LLM 플래너 toolConfig는 이미 allowlist 전종을 enum으로 노출해 즉시 정합. Macie·AccessAnalyzer 미활성 계정이면 크래시 대신 중립결과로 우아하게 강등(라이브 강건성). 서비스별 클라이언트 지연생성. **가짜 boto3로 9종+강등 오프라인 검증 0 fail.** **② RAG 실 경로(진우 파일 — 수정+플래그):** `rag/retrieval/retriever.py`의 실 검색이 NotImplementedError였던 걸 **Titan Embed v2(적재부 CorpusLoader와 동일 모델로 쿼리 임베딩=벡터 정합) → pgvector cosine top_k** 구현. `answer_gen.py`는 실 converse가 이미 있었고(모델ID만 어제 확정) `tokens: -1` placeholder를 converse `usage.totalTokens` 실값으로 채움(케이스별 비용 관측 정합). **③ console-backend `/chat` 실 RAG:** 에코 placeholder를 Titan 임베딩→pgvector→Sonnet converse(rag/와 동일 파이프라인 TS판, 같은 rag_chunks·같은 모델)로 배선 + `@aws-sdk/client-bedrock-runtime`(런타임 external, IAM은 기존 backend_bedrock). **검증:** run_demo 10개·run_e2e·contracts validate exit 0 + console/backend tsc·build. **`[PULL 필요]`** (진우: `rag/retrieval/retriever.py`·`answer_gen.py` 실 경로를 채움 — mock 무회귀, `mock=False`+`PG_DSN`이면 이제 실제 pgvector 검색·토큰 캡처 동작. 검색 SQL은 `embedding <=> vec`(cosine) top_k, 적재와 동일 Titan v2 필수)

- **2026-07-03 / 준형 / 🚀 미구현 실전환 갭 7종 봉합 — Lambda 실코드 배포·CI 이미지빌드·OIDC 로그인·조치 승인·real pg·RAG model ID** — "기능은 목업으로 다 됐지만 실클라우드 배선이 비어있던" 항목들을 코드로 채움(전 회귀 통과). **① Lambda 스텁→실코드:** `infra/backend/build_lambdas.py` 신설(각 패키지 pipeline/·attackpath/·engine/ + contracts JSON을 zip 루트에 나란히 배치 → 코드 경로 무변경 동작 + psycopg2 manylinux x86_64 레이어). `infra/backend` 5개 Lambda(ingest·normalize·correlation·orchestrator·remediation)를 `index.handler` 스텁에서 **실 핸들러**(`pipeline.ingest.handler.handler` 등)로 배선 + psycopg2 레이어 부착(ingest 제외 — RDS 무관). `deploy.ps1`이 backend apply/plan 전 build 자동 실행(validate는 스킵). **② CI 이미지 빌드:** `.github/workflows/build-images.yml` 신설(GitHub OIDC 키리스 → member·product Docker build→ECR push, workflow_dispatch+경로 트리거) + `infra/shared` github_ci 역할에 ECR push 최소권한 정책 attach(기존 0개). **③ ACCOUNT_ID placeholder 제거:** 단일 계정 확정(D1)이라 3개 k8s 매니페스트의 `ACCOUNT_ID`를 실 계정 리터럴(066107819776)로 확정 — "apply 후 수동 치환" 단계 자체를 없앰. **④ SSO 옵션B 프론트 배선:** `apps/console/src/lib/oidc.ts` 신설(PKCE·외부 라이브러리 없이 Web Crypto — Cognito Hosted UI 직접 OIDC) + `/callback` 페이지·라우트 + API 클라이언트 `Authorization: Bearer <id_token>` + `auth.ts`가 실모드에서 ID 토큰 custom:groups(GUID)로 역할 판정 + console-backend `auth.ts`가 Bearer 헤더 수용(레거시 x-amzn-oidc-data와 병행). **⑤ 조치 승인 배선:** Remediation.tsx 승인/거부 버튼이 로컬 state만 바꾸던 걸 `useRemediationDecision` mutation으로 실 `POST /remediations/:id/{approve,reject}` 호출(성공 시 findings·audit 캐시 무효화). **⑥ console-backend real pg:** data.ts를 async 전환 + `pg`·`@aws-sdk/client-secrets-manager` 실 쿼리 구현(findings·finding_explanations·cases·attack_paths 조인, Secrets Manager 자격증명) + **esbuild 번들**(pg를 dist에 인라인 → VPC Lambda 배포 가능, aws-sdk는 런타임 external). **⑦ RAG model ID 확정:** `rag/retrieval/answer_gen.py`의 bare name(404)을 실 Global inference profile `global.anthropic.claude-sonnet-4-5-20250929-v1:0`로(aws bedrock list-inference-profiles 서울 확인). **검증:** run_demo 10개·run_e2e·contracts validate 전부 exit 0 + shared·backend terraform validate Success + console/console-backend tsc·build 통과. **`[PULL 필요]`** (진우: `rag/retrieval/answer_gen.py` model ID를 실 ID로 확정함 — mock 무회귀, 실 RAG 호출 시 이제 안 터짐. pipeline/·attackpath/ 패키지는 build_lambdas.py가 그대로 번들만 함(로직 무변))

- **2026-07-03 / 준형 / 🧱 infra 구조 리팩터 — Karpenter를 `infra/karpenter` 새 레이어로 분리 + 순서강제 실행기 `deploy.ps1` + 의존성 점검(전 6레이어 validate)** — 라이브 apply→destroy 사이클에서 드러난 구조 결함 3건을 객관적으로 수정(독립 리뷰 에이전트로 교차검증). **① Karpenter 분리:** `infra/shared/karpenter.tf`(helm·kubectl provider + 컨트롤러 + NodePool)를 통째로 **새 레이어 `infra/karpenter`로 이관**. shared가 '순수 AWS 폴대'여야 하는데 라이브 EKS 클러스터(helm)에 결합돼 apply 실패(`context deadline exceeded`) 시 shared 전체가 무너지고 destroy 때 helm state를 손으로 만져야 했음 → 분리로 실패·수명주기 격리. shared엔 `karpenter.sh/discovery` **태그만** 남기고, 그 값을 `local.cluster_name` 하나로 묶어 cluster_name·서브넷태그·노드SG태그 3곳이 항상 일치(리뷰 지적). karpenter는 shared를 remote_state로 참조(`eks_cluster_name`·`eks_oidc_provider_arn`) + `data.aws_eks_cluster`로 엔드포인트·CA 직접 조회. **하드닝:** 컨트롤러 `replicas=1`(기본 2는 2노드에 anti-affinity로 스케줄 압박 → helm wait 타임아웃 원인 후보), `timeout` 600→900. **② `infra/deploy.ps1`(순서강제 실행기):** apply 정방향(shared→karpenter→target·backend·console→monitoring)·destroy 역방향 자동 순회, 중간 실패 시 즉시 중단. destroy 때 shared를 target보다 먼저 지워 `remote_state` 깨진 전례를 코드로 차단. **⚠️ ASCII 전용** — Write 도구가 만든 BOM 없는 UTF-8 `.ps1`을 PS5.1이 cp949로 읽어 한글 주석 멀티바이트가 **다음 코드 라인을 삼켜** `& terraform`이 실행조차 안 되던 실버그를 추적·수정(증상: `$LASTEXITCODE` 빈값+출력 0줄). 상세 한글설명은 infra/README §3.5. **③ gitops 정합:** `gitops/autoscaling/karpenter.yaml`(NodePool 중복 정의 + `instance-category t,m,c`로 프리티어 거부될 값)을 **삭제** — 노드층 Karpenter는 이제 infra/karpenter가 SSOT, gitops는 HPA(파드층)만. **검증:** `terraform fmt` + **전 6레이어 `validate` 전부 Success** + 교차의존 grep(참조하는 output 전부 실재, 누락 0). 문서: infra/README(레이어표·순서·§3.5)·gitops/README·CLAUDE 트리 갱신.

  **➕ 같은 날 라이브 풀사이클 실검증 완료(apply 207→검증→destroy 208, 잔존 $0):** `deploy.ps1 -Action apply`가 6레이어를 순서대로 원샷 apply(exit 0). **Karpenter 실검증(프로젝트 최초)** — ① 차트 1.1.1이 K8s 1.34에서 컨트롤러 시작 즉시 panic(CrashLoop, "존재≠호환" 교훈) → **1.13.0 업그레이드로 해결**, ② 모듈 v20 권한셋에 없는 `iam:ListInstanceProfiles`(1.13 GC용) 최소스코프 1문 보충 → ERROR 0, ③ **전체 수명주기 증명**: 컨트롤러 1/1 Ready → NodePool/EC2NodeClass READY(discovery 태그 발견) → 용량초과 파드 투입 → **t3.micro 스팟 노드 ~30초 프로비저닝·조인**(프리티어 제약 준수) → scale 0 → **consolidation 유휴노드 자동 회수**. 콘솔 CloudFront 200·대시보드 24위젯 확인. **destroy 하드닝 3건 추가 실측·반영**: Lambda Hyperplane ENI가 SG를 ~25분 붙잡음(→ SG `timeouts delete=40m` backend·console + available 전환 후 수동 삭제 가속절차 README 기록) · IGW detach↔NAT public IP 매핑 해제 레이스(→ `aws_instance.nat`에 `depends_on=[module.vpc]`로 destroy 순서 강제) · helm `wait=true`가 즉시-panic CrashLoop을 못 잡음(terraform green≠컨트롤러 healthy — apply 후 kubectl 확인 필수, karpenter/README §4.1). `infra/karpenter/README.md` 신설(실검증 기록·함정 4건). **`[PULL 필요]`** (진우: `infra/shared/karpenter.tf` **삭제→`infra/karpenter/`**, monitoring 등 타 레이어 의존성 무변. apply/destroy는 이제 `./infra/deploy.ps1` 사용)

- **2026-07-03 / 준형 / 🌐 SSO 활성 배선 미리 작성(도메인 `cnapp-agentic.cloud`) + 진우 EKS access entry 추가** — 라이브 풀스택 apply(shared·backend·target·console) 검증 중 **"SSO 실 로그인은 도메인+ACM 대기 = 계획된 보류"** 가 드러남(현재 ALB는 cert 없어 HTTP forward, 인증 게이트 비활성). → 도메인 구매(가비아, 진우) + **Route53 호스팅 영역 생성(AWS CLI, id `Z063995231WAW63UT354D`, ⚠️영구유지·destroy금지)** + 네임서버 위임(진우 입력, DNS 전파 대기). **구조 결정 = 옵션 B: SPA가 Cognito Hosted UI로 직접 OIDC 로그인**(프론트→Cognito→Entra SAML→역할 표시. 기존 ALB authenticate-cognito는 이 방식엔 미사용). `infra/console` 배선 **미리 작성**(전부 `enable_custom_domain=false` 기본 → 현 apply 무영향): `domain-sso.tf`(신설 — us-east-1 ACM+Route53 DNS검증·apex→CloudFront alias·SPA public Cognito 클라이언트[PKCE, 시크릿無]·프론트 주입 output 3종) + `main.tf`(CloudFront 조건부 커스텀 도메인·인증서) + `variables.tf`(`enable_custom_domain`·`domain_name`). `fmt`+`validate` 통과. **다음 세션 런북 = manual-infra §5**(DNS 전파 확인→`-var enable_custom_domain=true` apply→**프론트 OIDC 배선[앱코드 TODO]**→Entra 식별자 `urn:amazon:cognito:sp:ap-northeast-2_wAdKW5HjX` 확정). **별건: 진우(jw_kim) EKS cluster admin access entry 추가** — kubectl 접근 막힘(access entry에 jh_lee만) 해소, `infra/shared` `cluster_admin_principal_arns` default에 반영 + 라이브 apply 완료(jw_kim admin 확인). **`[PULL 필요]`** (진우: 이제 `aws eks update-kubeconfig --name cnapp-agentic-shared` 후 kubectl 됨)

- **2026-07-03 / 진우 / 🚀 `infra/monitoring` 실 apply 완료 + CloudTrail 연결 + `gitops/monitoring` 신설 (첫 실 apply, 준형 4층 뒤)** — 준형이 `shared`·`target`·`console`·`backend`(`pipeline+engine→backend` 병합 포함) apply를 끝낸 직후 `infra/monitoring`을 처음으로 실제 apply. **① apply**: `terraform plan`(27 to add·0 change·0 destroy) → apply 성공, 6개 output 전부 발급. 대시보드를 `aws cloudwatch get-dashboard`로 직접 조회해 **정확히 24개 위젯이 설계한 좌표 그대로 겹침 없이 등록**됨을 확인(§13이 우려했던 "AWS 시맨틱은 apply 후 육안 확인 필요" 캐비어트 최소 해소 — Windows 콘솔 cp949가 위젯 제목의 em-dash를 못 읽어 크래시하는 함정은 `PYTHONUTF8=1` 환경변수로 우회). **② CloudTrail 연결**: 기존 트레일 `cnapp-org-trail`에 `aws cloudtrail update-trail`로 1회 연결 — `CloudWatchLogsLogGroupArn`이 로그그룹 순수 ARN이 아니라 **`:*` 접미사가 붙은 형태를 요구**해서 처음엔 `InvalidCloudWatchLogsLogGroupArnException`으로 실패, 접미사 추가 후 성공(트레일 자체는 Terraform state 밖 그대로 — 순수 API 호출, tfstate 무영향). **③ `gitops/monitoring/kube-prometheus-stack-values.yaml`·`gitops/argocd/app-monitoring.yaml` 신설** — `grafana_irsa_role_arn` 실값이 나오면서 그동안 "진짜 blocked"였던 항목 해소. values는 Grafana ServiceAccount에 IRSA annotation + CloudWatch를 `additionalDataSources`로 추가(Prometheus는 차트 기본 데이터소스) + Alertmanager 비활성화(CloudWatch→SNS→Teams 중복 방지) + 영구볼륨 없이 소형 리소스. app-monitoring.yaml은 `app-target.yaml`과 달리 **멀티소스 ArgoCD Application**(소스①=공식 `prometheus-community` Helm 차트, 소스②=이 레포의 values 파일, `$values` 참조로 결합) — IAM 역할 이름이 고정값이라 apply→destroy 반복해도 이 값을 다시 안 써도 됨. `infra/monitoring/.gitignore` 신설(다른 레이어와 동일 패턴 — `tfplan`·`build/` 등 실수로 커밋 방지, 실제로 tfplan 파일이 untracked로 남아있던 걸 발견). **남은 것**: Teams webhook 실 URL 수동 주입 · `kubectl apply -f gitops/argocd/app-monitoring.yaml` 실행(ArgoCD 부트스트랩 후) · EMF 위젯 실데이터는 backend Lambda가 실 finding 처리해야 채워짐. `infra/monitoring/README.md` §16 신설. **`[PULL 필요]`**

- **2026-07-03 / 진우 / 📊 AI 관측 3종 추가 — Bedrock 비용 알람·케이스별 토큰(비용 귀속) EMF·tool별 breakdown EMF (대시보드 22→24위젯, 알람 6→7종)** — `infra/monitoring` 대시보드에서 "AI 관측이 다 됐냐"를 스스로 점검하다 발견한 3가지 공백을 채움. **① 비용 알람(`bedrock_cost_high`)**: 기존 "Bedrock 추정 비용" 위젯은 보기만 하는 것이지 임계값 알람이 아니었음 — 같은 metric math 식을 재사용해 alarm 신설(`period` 300→3600, 신규 변수 `var.bedrock_hourly_cost_alarm_usd` 기본 $1.0/시간, 무한루프 조기감지용 안전망). **② 케이스별 Bedrock 토큰**: `engine/evidence/bedrock_planner.py`의 `converse()` 응답엔 이미 `usage.inputTokens/outputTokens`가 들어있는데 안 꺼내 쓰고 있었음 — tool-use 루프 반복마다 누적해서 `EvidenceOutput`(신규 필드 `input_tokens`/`output_tokens`, 규칙 플래너는 기본값 0)→`case.py`의 `set_evidence(tokens=...)`로 흘림. **`case.schema.json`이 처음부터 정의해뒀지만 지금까지 항상 0으로 방치돼 있던 `model_trace[].tokens`를 이번에 처음 채움**(새 스키마 필드 아님, 기존 계약의 미사용 필드를 메운 것). `orchestrator.py`의 `_emit_case_metrics`에 `BedrockInputTokens`/`BedrockOutputTokens` EMF 메트릭 추가 — CaseId는 지표 Dimension으로 안 넣음(카디널리티 폭발 방지), 케이스 단위 정확한 값은 CloudWatch Logs Insights로 로그를 CaseId 검색해서 봄(지표=집계, 로그=드릴다운 역할분리). **③ tool별 breakdown**: `EvidenceOutput.plan`이 이미 `(tool, resource_id)` 목록을 들고 있었는데(규칙·LLM 플래너 둘 다) "설명용"으로만 쓰이고 관측엔 한 번도 안 흘러갔던 걸 발견 — 새 함수 `_emit_tool_usage_metrics`가 tool별로 별도 EMF 라인(`Dimensions=[["Tool"]]`)을 찍음. 대시보드에 위젯 2개 추가(케이스별 토큰 합계·tool별 SEARCH 분포), CloudFront는 y좌표가 밀림(22→24위젯). **검증**: 오프라인 fake Bedrock 클라이언트로 2턴 tool-use 루프 재현해 토큰 누적(800/200)·`model_trace.tokens`(1000)·EMF JSON 출력 형태 확인 + 전 회귀(run_demo 9개·run_e2e·contracts validate) 무회귀 + `infra/monitoring` `fmt`+`validate` 재통과. 건드린 파일: `engine/evidence/{evidence,bedrock_planner}.py`·`engine/core/case.py`·`engine/reasoning/orchestrator.py`·`infra/monitoring/{main,variables}.tf`·`infra/monitoring/README.md`(§15 신설)·루트 `README.md`(위젯/알람 개수 갱신). **`[PULL 필요]`** (준형: engine/core·engine/evidence 파일 변경 있음 — `set_evidence()` 시그니처에 `tokens` 키워드 인자 추가는 기존 호출부 무변경으로 하위호환)

- **2026-07-03 / 진우 / 🔍 kube-bench KSPM 스캐너 신설 — 워크로드 스캐너 실질적 완료 + Inspector 오기재 정정** — "infra apply 후"로 미뤄뒀던 워크로드 스캐너 잔여분을 다시 들여다보니 **Inspector·kube-bench가 서로 다른 처지였음**: ① **Inspector는 애초에 별도 코드가 필요 없었음** — `scanners/cspm/cspm.py`의 `scan_securityhub()`가 `securityhub.get_findings()`로 계정 내 모든 FAILED finding(Inspector 포함, ASFF 공통 포맷)을 이미 가져오게 돼 있어 Inspector 발견사항도 그대로 커버됨. README의 "Inspector 미착수 ⬜"는 오기재였고, 실제로 남은 건 AWS 계정에서 Inspector 서비스를 켜는 것뿐(순수 infra 항목). ② **kube-bench는 진짜 미작성**이었고, **Trivy와 똑같은 mock-first 이중 경로 구조로 지금 당장(EKS 없이) 만들 수 있는 부분**이라 신설: `scanners/workload/kube_bench.py`(`KubeBenchScanner`) — `scan_from_json(kube_bench_json, target)`(mock, 검증 완료) / `scan_cluster(target)`(kubectl로 aquasecurity 공식 Job 배포 → JSON 로그 회수, EKS 미apply라 미검증 — Trivy `scan_image()`와 동일 처지). **모델링 단순화 문서화**: kube-bench 원본은 노드/클러스터 스코프라 특정 파드를 지목 못 함(같은 control의 대체 소스 `trivy-k8s:KSV*`가 실제 파드별 매니페스트 스캔에 더 적합) — "이 실행이 어떤 워크로드를 대표하는가"를 호출자가 `target` 파라미터로 지정하는 방식으로 데모 단순화(Trivy의 `image` 파라미터와 동형), 클래스 docstring에 명시. `pipeline/normalize/normalizer.py`에 `_parse_kube_bench` 신설(`source_format="custom"`+`source=="kube-bench"`로 분기 — kube-bench 고유 스키마가 기존 asff/ocsf/prowler-json/trivy-json 어디에도 안 맞음. kube-bench는 severity를 안 내놓아 control-catalog `severity_default`를 그대로 씀). `scanners/workload/run_demo.py`에 kube-bench 데모 통합 — f2(product)·f13(member) 골든 일치 + PASS→remediated 상태 전환까지 확인. `scanners/README.md`·루트 `README.md`(폴더트리·역할분담·스캐너 현황표) 갱신. 전 회귀(run_demo 9개·run_e2e·contracts validate) 재통과. **`[PULL 필요]`** (준형: workload 폴더에 새 파일 추가 + normalizer 분기 추가 — CI(`scanners.workload.run_demo`)는 기존 항목 그대로라 `ci.yml` 변경 불필요)

- **2026-07-03 / 준형 / 🧱 infra 레이어 병합 — `infra/pipeline` + `infra/engine` → `infra/backend` (디렉터리 단순화)** — 준형 판단("너무 많이 분리하면 오히려 마이너스, 대신 각 테라폼이 뭘 배포하는지 README에 잘 정리"): 데이터 평면(수집·정규화)·추론 평면(상관·오케스트레이터·조치)이 둘 다 shared만 참조하는 백엔드 Lambda 뭉치라, 레이어 분리 실익보다 단순함이 커서 **하나로 병합**. **코드 폴더 `pipeline/`·`engine/`·`attackpath/`는 그대로**(배포 껍데기만 합침). 공통 boilerplate(provider·remote_state.shared·lambda_assume) 1벌 dedup, egress SG 2→1개(`${project}-backend-lambda`) 통합, 나머지 리소스·이름 전부 보존(Lambda 5·remediation SFn·감사 Object Lock·IAM 6·SQS·EventBridge 2-pass). **state 이관 불요**(둘 다 apply 전 = 순수 코드 병합). **monitoring 참조 갱신**: 미사용 pipeline remote_state 제거 + engine→backend 리포인트(`remediation_state_machine_arn`·`audit_bucket`). `infra/backend`+`infra/monitoring` **validate 통과**. 문서 정합: `infra/backend/README.md` 신설(배포 리소스·흐름·스왑 명시)·infra/README(다이어그램·표·순서)·project-draft §4.6·크로스레퍼런스 7파일. **apply 순서 = shared → (target|backend|console 병렬) → monitoring.** **`[PULL 필요]`** (진우: `infra/pipeline`·`infra/engine` **삭제됨** → `infra/backend`로 통합, monitoring이 backend state 참조로 바뀜)
- **2026-07-03 / 진우 / 🔧 준형 판단(a·b) 반영 — correlation n5 pillar 도출 + Trivy dedup 2세그먼트로 수정** — 어제 재검증에서 판단 보류했던 2건, 준형이 방향 확정해줘서 반영. **a) `attackpath/correlation/correlation.py`** — n5 노드의 pillar를 하드코딩(`"ciem"`) 대신 실제 매칭된 finding에서 도출(`r5.get("pillar", "ciem")`). `_R5_ENTRA_APP`이 pillar가 다른 control 2종(ciem·cspm)을 매칭 대상으로 둬서, DB 쿼리 순서에 따라 어느 쪽이 먼저 매칭되든 그래프가 항상 정확한 pillar를 반영하게 됨. **b) `pipeline/normalize/normalizer.py`** — Trivy `_parse_trivy()`의 `dedup_key`를 3세그먼트(`resource_id|control_id|cve`)에서 계약① 정의대로 2세그먼트(`resource_id|control_id`)로 수정 — 이제 이미지 하나에 KEV CVE가 여러 개 있어도(전부 같은 control `INTERNAL-VULN-KEV-001`) finding 1건으로 dedup되고 CVE들은 `sources`에 누적됨(골든 `mock-findings.json`의 "이미지당 1건" 의도와 정합). title도 골든 톤에 맞춰 `"KEV-listed CVE ({cve}) in {artifact} image"`로 갱신. **부수 수정**: `scanners/workload/run_demo.py`의 골든 검증이 옛 동작(CVE 3건=finding 3건, `dedup_key` 서로 다름)을 기대하고 있어서 새 동작(1건으로 합쳐짐)에 맞게 갱신 — `sources[0]` 인덱스 의존 체크도 dedup 후 set 기반 병합이라 순서가 안 보장돼 `any(...)` 순회로 수정. 전 회귀(run_demo 10개·contracts validate·py_compile) 재통과 확인. **`[PULL 필요]`**

- **2026-07-03 / 준형 / 🤝 진우 재검증 머지·정합 확인 + 진우 판단필요 4건 응답(apply 준비 완료)** — 진우 밤샘 작업(monitoring 6층·재검증 6건·EMF)과 내 apply-blocker 수정(EKS 1.34·RDS 16.9·시크릿 recovery_window)이 **깨끗이 공존** 확인: run_demo 10개·run_e2e·contracts validate + **terraform 6층 전부 validate 통과**. 진우가 내 우려(UNKNOWN·audit·타깃버킷)를 이미 다 고쳤고(특히 스캐너 mis-tagging이 UNKNOWN의 진짜 원인이었음), 내 audit/compliance shape 수정도 진우가 값까지 맞춰 확장. **진우 요청·판단필요 처리:** ① **[요청] `infra/console/outputs.tf`에 `cloudfront_distribution_id` 추가**(monitoring CloudFront 위젯 게이트) ✅ ② **[c] RemediateSG IAM 스코프 과다**(내 remediation 스코핑에서 SG만 안 좁혀졌다는 정확한 지적) → `aws:ResourceTag/Layer=target` 조건으로 좁힘(remote_state 결합 없이 RDS/NAT/ALB SG 보호) ✅ ③ **[a] correlation n5 pillar 하드코딩** → 내 handler에 ORDER BY 넣으면 control_id 알파벳순상 cspm이 ciem보다 먼저 와 **오히려 결정적으로 틀려짐** → 진짜 수정은 `correlation.py`가 n5 pillar를 매칭된 finding에서 도출(진우 로직, 플래그) ④ **[b] Trivy dedup_key 3-seg vs 골든 2-seg** → **내 판단: 계약①이 dedup_key="resource_id|control_id"(2-seg)로 정의하므로 normalizer가 계약 위반. "이미지+control당 1 finding, CVE는 sources 누적"이 맞음 → normalizer 2-seg로 수정 권장(진우 파일·골든 title도 함께)** ⑤ **[d] Remediation.tsx 승인버튼 미배선**(준형 프론트) → 실 데모용 배선은 나중(mock 단계 정상). FYI: 진우 알림대로 CloudWatch 알람은 SEARCH() 미지원(다른 레이어 alarm 설계 시 참고). **`[PULL 필요]`** (진우: a·b는 correlation.py·normalizer.py 확인 요청)
- **2026-07-03 / 진우 / 🔎 전체 레포 재검증(4개 영역 병렬 리뷰) — 실버그 6건 수정 + 판단 필요 4건 발견(apply 전 필독)** — "전체 폴더 싹싹 검증" 요청으로 자동 회귀(run_demo 10개·contracts validate·terraform validate 7레이어·tsc 2개·vite build) 전부 통과 확인 후, 코드 자체를 다시 읽어 버그 헌팅. **내일 apply 세션 전 준형이 반드시 확인할 것 — 특히 ①⑤.**

  **수정 완료(6건):**
  1. **`infra/target/variables.tf`·`main.tf` — S3 버킷명 전역충돌 apply-blocker.** `member_pii_bucket` 기본값이 `"member-pii-prod"` 리터럴(계정ID 접미사 없음) — 다른 버킷(`front-${account_id}`·`audit-${account_id}`)과 달리 전역 유일성이 없어 **다른 AWS 계정이 이미 선점했으면 apply가 그 자리에서 실패**. `${var.member_pii_bucket}-${account_id}`로 수정. `apps/target/member/k8s/deployment.yaml`의 `MEMBER_PII_BUCKET` env는 기존 IRSA role-arn의 `ACCOUNT_ID` placeholder 패턴과 동일하게 `"member-pii-prod-ACCOUNT_ID"`로 — **apply 후 `terraform output member_pii_bucket` 값으로 수동 치환 필요**(IRSA ARN 치환과 같은 타이밍에 함께).
  2. **`scanners/cspm/cspm.py` — 실계정 Prowler 스캔이 전부 깨지는 버그.** `scan_prowler()`가 CLI를 `-M json-ocsf`(OCSF 출력)로 실행하면서 envelope엔 `source_format="prowler-json"`으로 잘못 태깅 → 정규화부가 `_parse_prowler`(다른 필드 구조)로 잘못 라우팅해 실 스캔 결과가 전부 `control_id=None → INTERNAL-UNKNOWN-001`로 깨짐. mock 경로(`scan_from_json`)는 format을 직접 지정해 우회하므로 run_demo로 안 잡혔음. `scanners/ciem/entra.py`의 Azure 실 경로도 이 메서드에 위임해서 동일 영향. `"ocsf"`로 수정.
  3. **`engine/remediation.py` — 감사 기록 조용한 실패(§17 위반 가능).** 실 변경(dry_run=False) 성공 후 `AUDIT_BUCKET`/`DB_HOST`+`DB_SECRET_ARN` 환경변수가 없으면 `_write_audit`/`_mark_remediated`가 예외 없이 조용히 return — "실 클라우드 변경은 있었는데 불변 감사 기록이 없는" 상태가 될 수 있었음(infra/engine이 항상 이 env를 배선하므로 지금은 latent risk). `_write_audit`이 이제 `RuntimeError`를 던져 Lambda 에러로 드러나게 함(infra/monitoring의 `lambda_errors` 알람이 즉시 잡아줌).
  4. **`apps/console-backend/src/data.ts` — MSW↔실API 전환 시 숫자가 바뀌는 버그.** `getAudit()`/`getCompliance()`가 프론트 자체 mock(`apps/console/src/mocks/view-fixtures.ts`)과 **타입(shape)만 같고 실제 값이 다름**(감사 5건 vs 12건, 컴플라이언스 점수 0점 vs 17점) — `VITE_USE_MOCK=false` 스왑 시 데모 숫자가 바뀌는 문제였음. view-fixtures.ts 기준으로 값 전체 동일하게 맞춤.
  5. **`contracts/finding.schema.json` — 스키마 자체 결함.** `resource_id` 정규식 `^(aws|azure):[a-z_]+:.+$`이 숫자를 허용 안 해서 `s3_bucket` 타입(20개 골든 mock 중 6개, 30%)이 전부 스키마 위반이었음 — `contracts/validate.py`의 커스텀 assert(b)는 문자열 일치만 보고 이 정규식 자체는 한 번도 검사 안 해서 지금까지 안 걸림(jsonschema 라이브러리로 직접 검증해서 발견). `[a-z0-9_]+`로 수정.
  6. **`contracts/validate.py` — Windows 콘솔 크래시.** 다른 모든 `run_demo.py`엔 있는 `sys.stdout.reconfigure(encoding="utf-8")`가 이 파일에만 없어서 로컬(Windows)에서 `python contracts/validate.py` 실행 시 UnicodeEncodeError로 죽었음(CI는 Ubuntu라 안 걸림). 추가.

  **⚠️ 발견했지만 판단 필요해서 안 고침(준형 확인 요청) — 4건:**
  a. **`attackpath/correlation/correlation.py` n5 노드 pillar 하드코딩** — `_R5_ENTRA_APP`이 서로 다른 pillar(ciem/cspm)의 control 2종을 매칭 대상으로 두는데, `Node("n5", ..., "ciem")`은 pillar를 하드코딩. 지금은 `mock-findings.json`에서 ciem finding(f8)이 cspm finding(f17)보다 먼저 나와서 우연히 맞지만, `attackpath/correlation/handler.py`의 실 DB 쿼리엔 `ORDER BY`가 없어 실제로는 순서가 보장 안 됨 — cspm이 먼저 매칭되면 그래프 노드 pillar가 실제 finding과 불일치할 수 있음(`validate_graph`는 enum만 체크해서 못 잡음).
  b. **Trivy `dedup_key` 설계 불일치** — `pipeline/normalize/normalizer.py`의 `_parse_trivy()`는 CVE별로 별도 finding을 만드는 3세그먼트 dedup_key(`resource_id|control_id|cve`)를 쓰는데, 골든 `mock-findings.json`의 KEV 항목은 이미지+control당 1건인 2세그먼트(`resource_id|control_id`) 형태로 손으로 작성돼 있음(제목 문구도 다름 — 코드는 "CVE-XXXX in {artifact} ({pkg})", mock은 "KEV-listed CVE (CVE-XXXX) in product image"). 스캐너/정규화 설계 의도가 "CVE 여러 개=finding 여러 개"인지 "이미지+control당 finding 1개(sources에 CVE 누적)"인지 확정 필요 — 후자가 의도라면 정규화부 수정, 전자가 의도라면 스키마 설명·골든 mock을 3세그먼트로 갱신.
  c. **`infra/engine`의 `RemediateSG` IAM 문 스코프 과다** — `ec2:RevokeSecurityGroupIngress`가 `security-group/*`(계정 내 전체 SG)에 걸려있음. 같은 정책의 S3(`${project}-*`)·IAM(`${project}-target-*`) 문은 스코프가 좁은데 이것만 안 좁혀짐 — 권한상승은 아니지만(revoke만 가능) 잘못된 SG ID가 들어오면 RDS/NAT/ALB의 SG를 건드릴 가용성 리스크. 좁히려면 `infra/engine`이 `infra/target`을 `remote_state`로 참조해야 해서 레이어 의존성이 새로 생김 — apply 순서에 영향 줄 수 있어 이번엔 안 건드림.
  d. **`apps/console/src/pages/Remediation.tsx` 승인/거부 버튼 미배선** — `onDecide`가 백엔드 `/remediations/:id/approve`를 아예 호출 안 하고 로컬 `useState`만 바꿈. 보안 문제는 아니고(백엔드 쪽 approver 게이트는 실재 확인됨) mock 단계라 예상된 상태지만, 실 데모에서 승인 흐름을 보여주려면 이 배선이 필요.

  검증 방법: 4개 병렬 리뷰(스캐너/파이프라인/RAG/attackpath, 엔진, 앱 3종, infra 전체) + `jsonschema` 라이브러리로 mock 데이터를 스키마에 직접 검증(그동안 안 하던 방식, 5번을 이렇게 찾음). 수정 후 전 회귀(run_demo 10개·contracts validate·terraform validate 7층·tsc 2개) 재통과. **`[PULL 필요]`**

- **2026-07-03 / 진우 / 🐛 `infra/monitoring` 전체 재검증 — apply 실패 가능성 있던 알람 버그 등 3건 수정** — 어제 만든 레이어를 apply 전에 처음부터 다시 정독하며 발견: **①** `triage_escalate_rate_zero` 알람이 metric math에 `SEARCH()`를 썼는데 **CloudWatch 알람은 SEARCH를 지원 안 함**(대시보드 전용 — `terraform validate`는 HCL 스키마만 봐서 못 잡고, 그대로 뒀으면 apply 시 이 알람 리소스 생성이 AWS API 레벨에서 거부됐을 가능성이 큼) → `engine/reasoning/orchestrator.py`의 EMF 발행을 `Dimensions=[[], ["Verdict","RiskLevel"]]`(빈 세트 포함)로 바꿔 무디멘션 총계 시계열을 표준 EMF 기능으로 같이 얻고, 알람은 SEARCH 없이 그걸 직접 참조. **②** `bedrock_errors` 알람이 `ModelId` 디멘션 없이 조회 → Bedrock 지표는 모델별로만 발행돼 데이터가 안 잡혀 알람이 영원히 죽어있던 문제 → 디멘션 추가. **③** Bedrock 모델별 위젯은 모델 수만큼 자동 배치되는데 그 아래 비용/EMF 위젯 y좌표가 하드코딩이라 Sonnet 추가 시 겹칠 뻔함 → 상대좌표(`local.bedrock_rows_end_y`)로 전환. `fmt`+`validate`+`py_compile`+`run_demo`+`run_e2e` 재통과, README §13에 근거 기록. **`[PULL 필요]`** (준형: CloudWatch 알람에서 SEARCH()가 안 먹힌다는 게 이번에 처음 확인된 제약이라, 다른 레이어에서 비슷한 alarm+SEARCH 조합을 쓸 계획이 있다면 참고)
- **2026-07-03 / 진우 / 📊 `infra/monitoring` 운영 관측 레이어 완성 + 엔진 EMF 계측(§7.1 참고)** — 내일 준형 4층 apply 후 바로 `infra/monitoring` apply할 수 있게 새 5번째 레이어를 코드로 완성(shared·pipeline·console·engine remote_state만 참조, 자체 리소스는 이 폴더만). **① 대시보드 17위젯**(기존 4→17): Lambda 6종(remediation 누락분 수정)·SQS 백로그나이·RDS 스토리지/IOPS·Step Functions(remediation HITL)·S3 감사버킷·Cognito 로그인·Bedrock 호출/에러/토큰(모델별)+비용 metric math·엔진 EMF 4종(SEARCH 기반, Verdict×RiskLevel). **② CloudTrail→CloudWatch Logs 배관**(로그그룹+IAM 역할 — 트레일 자체는 계속 수동 유지, apply 후 콘솔에서 1회만 수동 연결). **③ Teams 알림 풀스택**(SNS·Lambda 실코드 `lambda_src/teams_notifier.py`·시크릿(값은 로테이션 후 수동주입)·알람 6종). **④ `engine/reasoning/orchestrator.py`에 EMF 계측 추가**(`_emit_case_metrics`, Lambda 밖에선 미발행이라 run_demo/run_e2e 무회귀) — 엔진 축 위젯·트리아지 알람이 이제 실데이터를 받을 준비 완료. `fmt`+`init -backend=false`+`validate` 3개 통과. `docs/manual-infra.md`(Teams 웹훅 레닥션+실 연동상태 반영)도 갱신. **`[PULL 필요]`** (준형: `infra/console/outputs.tf`에 `cloudfront_distribution_id` output 1줄 추가 요청 — CloudFront 위젯 게이트, `infra/monitoring/README.md` §12.4)
- **2026-07-02 / 준형 / 🛠️ 보너스 — 조치(remediation) 실행기 실코드 + 감사 S3 Object Lock(HITL 클로즈드루프 완성)** — 남은 보너스를 미리 채움(apply 전 무비용). **① `engine/remediation.py`** — Step Functions ApplyFix Task의 Lambda 진입점(read-only 조사와 분리된 **유일한 '쓰기' 경로**, 격상 역할). MVP 카탈로그 3종 실구현: `s3_block_public`(공개 버킷 PAB 4종 활성) · `sg_remove_open_ingress`(0.0.0.0/0·::/0 인바운드만 골라 revoke) · `iam_least_privilege`(현재→제안 정책 diff 후 put). 각 액션 **dry-run(계획만)/apply(실변경)**, 성공 시 **② 감사 기록을 S3 Object Lock 버킷에 불변 저장** + **③ RDS 상태 갱신**(remediation_requests=applied · findings=remediated = console §6.1 "수정→소멸" 루프). **infra/engine 배선:** 감사 S3 버킷(object_lock GOVERNANCE·versioning·PAB·SSE) + remediation Lambda VPC 배치(RDS) + IAM(감사 PutObject·RDS secret·VPC) + env(AUDIT_BUCKET·DB_*) + output audit_bucket. 스텁 주석을 `engine.remediation.handler`로 갱신. py_compile+import OK, engine validate 통과, run_e2e 무회귀(exit 0). ⚠️ 실 변경 API라 apply 세션에서 dry-run 먼저 확인. **`[PULL 필요]`**
- **2026-07-02 / 준형 / 🔎 오늘 작업 독립 리뷰(에이전트 2) 반영 + 전 MD 정합·가독성 스윕** — 오늘 만든 것(infra 3층·스키마·wrapper)을 **독립 리뷰 에이전트 2개**(terraform / schema+handlers)로 객관 재검토. **진짜 버그 2건 수정(둘 다 내 `engine/handler.py`, 같은 뿌리):** case 빌더는 `verdict`·`confidence_score`를 **`case["evidence_meta"]`**에 쓰는데(set_evidence) 핸들러가 `case["reasoning"]`에서 읽어 ① `finding_explanations.confidence_score`가 항상 NULL ② 반환 verdict 항상 null → 둘 다 `evidence_meta`에서 읽도록 수정(run_e2e와 동일 위치). **터라폼 하드닝 1건:** `infra/console`이 `acm_certificate_arn` 빈값이면 naive `apply`가 HTTPS 리스너에서 하드 실패 → **HTTPS 리스너 `count` 가드 + HTTP 폴백(cert 없으면 Lambda로 직접 forward)**으로 **인증서 없이도 단계별 apply 가능**하게(SSO는 cert 확보 후 활성). 리뷰가 제기한 SAML `custom:groups` 매핑은 **정식 형식이라 유지**(Cognito 커스텀 속성은 `custom:` 접두 필요, false positive). **문서 스윕:** README(authenticate-oidc→cognito·폴더트리 infra 5층+db+slice·stale Status 행·변경요약 6줄→1줄 포인터로 가독성)·CLAUDE §3(authenticate-oidc 잔존 정정)·infra/shared README(console 소유 진우→준형)·console-app-design §12(구현됨 표시+조치 카탈로그 MVP 3종 확정)·pipeline/engine/attackpath README(handler.py 스왑코드 존재 반영). handler 4개 import+terraform 3층 validate 재통과. **`[PULL 필요]`**
- **2026-07-02 / 진우 / 🔑 SSO App Registration 완료(`infra/console` apply 선행 조건 해소) — Entra 그룹 클레임 라이선스 제약으로 GUID 매칭 전환** — 준형 요청(§7.1 "apply 전 TODO")에 따라 Cognito SAML IdP용 Entra Enterprise App `cnapp-agentic-console-sso` 신설. **당초 계획이 두 번 틀어짐**: ① 그룹 클레임을 "애플리케이션에 할당된 그룹"으로 하려 했으나 **Entra 무료 티어라 앱에 그룹 할당 자체가 막힘**(P1/P2 필요) → "보안 그룹"(사용자가 속한 전체 그룹) 모드로 전환 + `viewer@`·`approver@` 개별 사용자 할당. ② "클라우드 전용 그룹 이름 내보내기" 옵션이 "보안 그룹" 모드에서 비활성화돼(사이드 어사인먼트 그룹에만 지원) 그룹 **이름**을 못 뽑아냄 → `apps/console-backend/src/auth.ts`를 이름 문자열(`cnapp-approver`) 매칭에서 **그룹 개체 ID(GUID) 매칭**으로 변경(오히려 이름보다 불변 식별자라 더 견고, 임시방편 아님). **`infra/console/variables.tf`의 `saml_metadata_url` 기본값을 실제 발급 URL로 반영**해 준형의 apply 선행 조건 해소. `docs/manual-infra.md` §3.6.1에 App Reg 상세(client ID·그룹 GUID 2종·SAML 식별자/ACS URL) 기록 + **⚠️ apply 후 리마인더**: SAML 식별자가 지금 도메인 기반 임시값(`urn:amazon:cognito:sp:cnapp-agentic-demo`)이라 apply로 실 Cognito User Pool ID 나오면 Entra 쪽 식별자 필드 재확인 필요(§3.6.5). `terraform validate`+`npm run typecheck` 둘 다 통과. **`[PULL 필요]`** (준형: `infra/console/variables.tf` 변경 — SSO 메타데이터 URL 채워짐, apply 시 §3.6.5 리마인더 확인 요망)
- **2026-07-02 / 준형 / 🗄️ DB 스키마(진짜 공백 메움) + Lambda 핸들러 wrapper 4종 — 실 데이터 평면 실체화** — apply 전 채울 것 중 **최우선 = DB 스키마**(SQL 파일이 하나도 없어 pipeline·engine·rag·console 실코드가 전부 "테이블 있다고 가정"만 하던 공백). **① `infra/shared/db/schema.sql`** 신설(RDS와 co-locate) — `CREATE EXTENSION vector` + 6개 테이블(findings·attack_paths·cases·finding_explanations·remediation_requests·rag_chunks), 전부 계약①③⑦⑥ + console §5에서 도출, 멱등(IF NOT EXISTS)·FK·인덱스·pgvector HNSW. RDS는 private라 VPC 내부에서 `psql -f`로 1회 적용(shared README에 절차). **② Lambda 핸들러 wrapper 4종**(`pipeline/ingest/handler.py`·`pipeline/normalize/handler.py`·`attackpath/correlation/handler.py`·`engine/handler.py`) — 스텁의 실코드 스왑 대상. 기존 클래스(Ingestor·Normalizer·CorrelationEngine·Orchestrator) 재사용 + 이벤트 어댑팅 + RDS upsert(psycopg2 지연 import, 스키마 정합) + 2-pass EventBridge 발행. **engine/handler.py는 REAL_TOOLS=1이면 RealToolExecutor+BedrockEvidenceAgent(run_real.py 구성 미러링) = 실 Bedrock tool-use 크라운주얼 배포판.** py_compile+import 4개 OK, terraform 스텁 주석을 실 핸들러 경로로 갱신(validate 재통과). ⚠️ 실 RDS/Bedrock 대상이라 apply 세션에서 라이브 검증(로컬 무 DB라 로직만 확인). **`[PULL 필요]`** (진우: normalize/correlation는 각 주인 공유 — handler.py가 진우 로직을 그대로 호출, RDS I/O만 얹음)
- **2026-07-02 / 준형 / 🏗️ infra terraform 3층 완성(`infra/console`·`infra/pipeline`·`infra/engine`) — apply-ready 배포 코드 + slice 정체 문서화** — 남은 배포 인프라를 §4.6대로 다 만듦(전부 `fmt`+`init -backend=false`+`validate` **3개 통과**, 무비용): ① **`infra/console`** — S3+CloudFront(OAC) 프론트 · **Cognito User Pool(custom:groups) ← Entra SAML IdP** · **ALB(authenticate-cognito HTTPS→Lambda 타깃)** · console-backend Lambda(VPC/RDS, read-only + StartExecution만). ② **`infra/pipeline`** — EventBridge(SecurityHub Imported)→**SQS+DLQ**→ingest/normalize Lambda(normalize는 VPC 배치·RDS·PutEvents). ③ **`infra/engine`** — 상관·오케스트레이터 Lambda(shared의 **evidence read-only+bedrock 정책 attach**, 2-pass EventBridge 체인) + **remediation Step Functions(HITL, 격상 역할 분리)**. **Lambda = 배포 가능 스텁 + 실코드 스왑 포인트(archive_file)** — 로직은 `run_e2e`/`run_real`로 로컬 검증됨, 인프라는 '프로덕션 배관 모양'. **slice 질문 답변**: `infra/slice`는 3레이어와 종류가 다른 **일회용 저비용 픽스처**(크라운주얼 재현용)라 유지하되 §4.6·CLAUDE 트리에 "레이어 아님" 명시(병합은 독립·저비용 목적 상실). **apply 전 TODO**: console ACM 인증서·**진우 Entra SAML 메타데이터(SSO)**·engine SM ARN 주입·console-backend `npm run build`(dist). **`[PULL 필요]`** (진우: pipeline/engine은 각 주인 공유 소유 — Lambda 스텁을 실코드로 교체하는 CI 빌드 스텝이 실 apply 시 필요. SSO는 Entra SAML 메타데이터 URL 필요)
- **2026-07-02 / 진우 / f16 노드 매핑 확정 — n4(order-sp) 고정 + control 타이틀 "무만료"→"장기 유효(6개월 초과)" 완화 + 실 order-sp 시크릿 24개월 재발급(manual-infra §3.6.4 리마인더 닫음)** — 준형 리뷰가 제기한 f16(`INTERNAL-ENTRA-SP-CRED-001`) 노드 매핑(n4 vs n5) 확정. **처음엔 n5(overpriv-app)로 옮기려 했으나 코드 확인 후 취소** — `attackpath/correlation/correlation.py`의 `_R3_AZURE_SP = "INTERNAL-ENTRA-SP-CRED-001"`가 **이 control_id를 가진 finding을 코드로 n4에 고정**(`_first(findings, _R3_AZURE_SP)["resource_id"]`를 그대로 n4 노드에 씀) — n5로 옮기면 n4·n5가 같은 resource_id(overpriv-app)를 갖게 돼 **그래프 붕괴**. 대신 **f16=n4(order-sp) 고정 확정**, 실물을 맞춤: **Entra 포털이 "무만료" 옵션 자체를 지원 안 함(최대 24개월/730일, 플랫폼 보안 기본값)**을 확인 → order-sp 클라이언트 시크릿을 6개월→**24개월(2028-07-01, 비밀 ID `e0589f10-3ad7-4975-81c3-a21f324d1e14`)로 재발급**(기존 `acbe478f-...` 삭제) + control-catalog·mock-findings·target-app-design §2.0·scanners/ciem·pipeline/normalize 데모의 타이틀 문구를 "credential without expiry" → **"credential with excessive validity (>6 months)"**로 완화(현실적으로 달성 가능한 기준 — 실제 CIEM 툴도 보통 "무만료"보다 "180일/365일 초과" 임계값 씀). manual-infra §3.6.1(overpriv-app)에서 f16 언급 제거(f8만 소스로 정정), §3.6.4 리마인더 closed 처리. 전 데모 10개+validate exit 0(구조 필드 무변경, 타이틀 텍스트만 수정이라 회귀 없음). **`[PULL 필요]`** (준형: f16 매핑 확정 + 실 Azure 조치 완료 확인 요청)
- **2026-07-02 / 진우 / 준형 리뷰 피드백 2건 반영 — ① 실 Prowler 정규화 = OCSF 파서로 확정 ② Sonnet model ID 404 TODO 명시** — 준형이형이 짚은 2건 처리. **① `pipeline/normalize`에 `_parse_ocsf` 신설(핵심).** 준형 제안은 "① OCSF 파서 vs ② json-asff 재사용"이었고 처음엔 하이브리드를 제안했으나, **설계서 재확인 후 뒤집음**: project-draft §24("Prowler→OCSF 확정")·계약⑤("Prowler가 S3에 떨군 OCSF 객체")·**준형 `ingest.from_s3_event()`가 이미 `source_format="ocsf"`로 봉투화** → normalize에 ocsf 분기가 없어 이음새 갭이 실재했음. ASFF는 AWS 전용이라 Azure Entra를 못 담지만 **OCSF는 클라우드 중립 → 파서 하나로 AWS+Azure 둘 다 커버**(멀티클라우드 OCSF 통합 셀링포인트 정합)라 OCSF로 확정. `_parse_ocsf`는 OCSF 버전 편차 대비 `metadata.event_code`(check_id)·`resources[].group.name`(service)·`severity`/`severity_id`를 방어적 탐색. `normalize/run_demo`에 OCSF fixture 2건(AWS S3·Azure Entra) 추가 + **dedup 머지로 가려지는 silent-fail 방지용 OCSF 단독 검증** 추가. 전 데모 10개+validate exit 0. **② `rag/answer_gen.py` `_BEDROCK_MODEL_ID`**는 bare name(404) — 실 RAG 스왑 세션에서 Haiku처럼 global inference profile ID로 교체하도록 TODO 주석 명시(지금 mock이라 무해). pipeline/README 파서표에 ocsf 행 추가. **`[PULL 필요]`** (준형: 피드백 #2 방향=OCSF로 확정, ingest ocsf 봉투가 이제 정규화됨 — from_s3_event ocsf 봉투가 normalize까지 관통)
- **2026-07-02 / 준형 / 🖥️ console-backend 구현(`apps/console-backend`, ALB→Lambda TS) — 콘솔 실데이터 스왑 keystone** — 프론트(`apps/console`)가 기대하는 API 표면(§15.2)을 그대로 응답하는 서버 = MSW `handlers.ts`의 서버판. `data.ts`(mock=계약 JSON 읽기 / real=pgvector 스텁)·`handler.ts`(ALB 라우팅·10 엔드포인트)·`auth.ts`(x-amzn-oidc-data JWT→custom:groups→viewer/approver, 조치 승인은 approver만). **검증:** `npm run typecheck`(tsc) 통과 + 스모크(findings 18 open·detail explanation+case 조인·attack-path 5노드·scores·RBAC viewer403/approver200·404) OK. 프론트 `VITE_USE_MOCK=false`+`VITE_API_BASE`로 **MSW→실 API 무변경 스왑**. 실배포는 infra/console(다음). 폴리글랏(console=TS). **`[PULL 필요]`**
- **2026-07-02 / 준형 / 🔎 전 영역 리뷰 반영 — 준형 버그·하드닝 수정 + 진우 피드백 정리(진우 리뷰 2건 포함)** — 리뷰(에이전트 2 + 준형): **콘솔·타깃앱 = clean(tsc·py_compile OK, 계약 정합, 진짜 버그 0), contracts = clean, terraform validate 3개 통과.** **준형 수정:** ① **ArgoCD glob 버그**(진우 발견) `gitops/argocd/app-target.yaml` — `k8s/*.yaml`만 두면 depth1 namespace만 잡히고 앱 매니페스트(depth2) 0개 배포 → `{k8s/*.yaml,*/k8s/*.yaml}`로 수정. ② **IRSA 역할명 불일치**(agent 발견) — `apps/target/order·member` 매니페스트가 `cnapp-target-*-irsa`인데 infra/target은 `cnapp-agentic-target-*-irsa` → 매니페스트 정정. ③ **infra/shared 하드닝**(Checkov 지적 해소) — NAT·EKS노드에 IMDSv2 강제(`http_tokens=required`) + EBS 암호화. ④ 중복·stale `contracts-validate.yml` 삭제(ci.yml에 흡수·진우 발견). ⑤ Karpenter discovery 태그 `cnapp-eks`→`cnapp-agentic-shared`(실 클러스터명). ⑥ CLAUDE §4 트리에 `.github`·`gitops`·`run_e2e` 추가(진우 지적 정합 누락). **⚠️ 진우 확인 요청(진우 파일이라 미수정, 피드백만):** (a) `rag/retrieval/answer_gen.py` `_BEDROCK_MODEL_ID="anthropic.claude-sonnet-4-5"` = bare name(404) → Haiku처럼 `global.anthropic.claude-sonnet-...` inference profile로 교체 (b) `pipeline/normalize` OCSF 파서 없음 — 실 Prowler는 OCSF/ASFF 출력인데 mock은 native prowler-json → 실전환 시 normalizer에 OCSF 파서 추가 or Prowler `json-asff`로 뽑아 기존 ASFF 파서 재사용 결정 필요. **`[PULL 필요]`**
- **2026-07-02 / 진우 / `scanners/ciem/` Azure Entra CIEM 스캐너 구현 완료** — `EntraCIEMScanner`: `scan_from_json(raw)`(Prowler Azure 체크 1건 → 계약⑤ envelope, mock/CI용) + `scan_prowler(checks="entra_id_*")`(실 경로 — subprocess 로직은 `scanners/cspm/cspm.py`의 `CSPMScanner.scan_prowler(provider="azure")`에 위임해 중복 방지, Prowler SP GitHub OIDC 인증 전제). `python -m scanners.ciem.run_demo`가 mock Prowler Azure 체크 4건(과도권한 App Registration·위험한 admin consent·SP 자격증명 무만료·App 설정 미스컨피그) → `INTERNAL-ENTRA-OVERPRIV-APP-001`·`INTERNAL-ENTRA-RISKY-CONSENT-001`·`INTERNAL-ENTRA-SP-CRED-001`·`INTERNAL-ENTRA-INSECURE-CFG-001` 4종 control_id 매핑, `mock-findings.json`의 골든 f8·f9·f16·f17과 resource_id·severity_id·pillar·resource_type 전부 일치 확인(OK ✅). resourceId는 스캐너 단계에서 캐논 형식(`azure:type:native_id`) passthrough로 전달(Prowler Azure엔 ARN이 없어 4.4.1a 정규화 대상이 아님). CI(`ci.yml`)에 `scanners.ciem.run_demo` 추가, `scanners/README.md`에 cspm/ 폴더 트리 누락분 보강 + CIEM 상세 섹션·현황표 갱신(전 데모 10개 exit 0 확인). **`[PULL 필요]`**
- **2026-07-02 / 준형 / ⚙️ CI/CD 코드 세팅 — GitHub Actions CI + ArgoCD CD(`gitops/`) + Karpenter/HPA 오토스케일 확정** — **CI**(`.github/workflows/ci.yml`): 매 push·PR에 ① checks(하드 게이트) = 9개 run_demo·run_e2e·contracts validate 전부 exit 0 ② shift-left(report) = Trivy config + Checkov(infra). 순수 stdlib라 $0·설치 불필요. ⚠️ 의도적 취약 타깃 포함이라 보안 스캔은 report 모드(실서비스면 hard-gate). **CD**(`gitops/`): ArgoCD Application `app-target`(pull-sync·self-heal·prune·CreateNamespace) + **오토스케일 확정 = Karpenter(spot·consolidation, Cluster Autoscaler 대체) NodePool/EC2NodeClass + 파드 HPA**. `gitops/README`에 CI/CD 분리 이유·ArgoCD 선택 근거·부트스트랩·비용 규율. **비용 근거 정리(핵심 요청)**: cost-strategy §2.7 신설 — CI=$0 상시 / CD·오토스케일=EKS apply 세션만, **진짜 절감은 destroy+spot이고 노드 오토스케일러 선택은 데모 규모선 실비 영향 적음(Karpenter는 포폴·모던 아키텍처 이유)** 정직하게 명시. project-draft §22·README(기술스택·트리·Status)·CLAUDE §7.1 정합. **실적용은 EKS apply 세션**(코드만 세팅, apply 전). **`[PULL 필요]`**
- **2026-07-02 / 준형 / 준형 컴포넌트 half 3종 mock-first 구현(`scanners/cspm`·`pipeline/ingest`·`rag/corpus`) + run_e2e 2스캐너 배선** — 진우가 각 영역 half(workload·normalize·retrieval)를 채운 것에 맞춰 **준형 half 3개를 대칭 완성**(전부 mock-first·$0·apply 없음): ① **`scanners/cspm`** — `CSPMScanner`(SecurityHub·Macie ASFF / Prowler prowler-json → 계약⑤ 봉투). scan_from_json(mock) + scan_securityhub(boto3)·scan_prowler(CLI) 실 경로. run_demo: 골든 CSPM 5종(S3·SG·PII·IRSA·secret) 정규화·dedup OK. ② **`pipeline/ingest`** — `Ingestor`(EventBridge/S3 이벤트 → 계약⑤ 봉투 → SQS). 양 입구(SecurityHub Findings Imported·Prowler S3 드롭 포인터) + 정규화 핸드오프 검증 + lambda_handler 실 진입점. ③ **`rag/corpus`** — `CorpusLoader`(진우 코퍼스 내용 → Titan 임베딩 → pgvector 적재, 계약⑥ 완성). mock=결정적 1024-dim 벡터+dry-run / real=Titan+psycopg2. 24청크·14 control 커버·계약⑥ 정합 OK. **run_e2e**를 CSPM 스캐너까지 소스로 배선(Trivy+CSPM 2개 실 정규화 관통). 전 데모(9개)+contracts validate exit 0. 새 .gitignore 3개(pycache). **`[PULL 필요]`** (진우: 각 영역 준형 half 채워짐 — 이음새 계약④⑤⑥ 정합 확인)
- **2026-07-02 / 준형 / 🔗 Phase2 배선 — end-to-end 러너 `run_e2e.py` 신설(스캐너→정규화→상관→엔진→RAG 한 줄 관통)** — 지금까지 영역별 `run_demo`만 있고 "한 줄로 흐르는" 게 없었음 → **레포 루트 `run_e2e.py`**로 통합. 흐름: `TrivyScanner`(scan_from_json fixture, 실 trivy 출력 동일 구조) → `Normalizer`(실 코드) → `CorrelationEngine`(실 코드, 스캐너발 CVE가 attack-path n1로 편입) → `Orchestrator`(Mock executor, 무비용) → `RAGAnswerGenerator`(mock). 실행: 스캐너 CVE 2건→KEV 정규화→5노드 attack-path(n1=스캐너발 ✅)→엔진 escalate 13·툴 4회·confirmed·CRITICAL→RAG S3 설명, **exit 0**. `--image`로 실 trivy 스캔도 가능(로컬 trivy·docker 미설치라 라이브는 대기, fallback 자동). **엔진 실 tool-use는 Phase1에서 별도 검증**(여기선 파이프라인 연결이 초점, 비용 0). ⚠️ 헤드업(진우): `rag/answer_gen.py` `_BEDROCK_MODEL_ID="anthropic.claude-sonnet-4-5"`는 bare name(404) — 실 RAG 스왑 시 Haiku처럼 `global.anthropic...` inference profile ID로 교체 필요. README·§7.1·troubleshooting 갱신. **`[PULL 필요]`**
- **2026-07-02 / 준형 / ❤️❤️ Phase1 완료 — 엔진 실 tool-use END-TO-END 검증 성공(프로젝트 심장 실동작 증명)** — `infra/slice` **apply → `python -m engine.run_real` → destroy** 전 과정을 실 AWS·실 Bedrock으로 실행. **실 Bedrock Claude Haiku 4.5가 스스로 read-only 툴을 골라(`s3:GetBucketPolicy`·`GetPublicAccessBlock`) 실제 공개 S3 버킷을 조사** → 실 API 응답(`Principal:"*" 공개 버킷`·`PAB 미설정`)으로 **판정 CONFIRMED(100%)**. 목업 canned가 아닌 **실제 tool use**. 검증 후 `terraform destroy`로 5개 리소스 삭제·버킷 부재 확인(비용 ~$0). **스모크(리소스 0, Mock 툴+실 Bedrock)도 선행 통과** — LLM이 규칙 플래너(4개)보다 더 꼼꼼히 6개 툴 선택. `run_real` 표시 정정(선택 N개/성공 M회 — 미구현 GetBucketAcl은 성공 제외). **"AI가 스스로 API 호출해 증거 수집" = 이제 실제로 됨.** README·§7.1·manual-infra §4 갱신. **`[PULL 필요]`** (진우: 심장 실검증 완료 — 데모 핵심 장면 확보)
- **2026-07-02 / 준형 / Bedrock 모델 액세스 완료 + Evidence model ID 확정(`global.anthropic.claude-haiku-4-5-20251001-v1:0`) + 모델 선정 근거 문서화** — **① 액세스:** Bedrock '모델 액세스' 페이지 **폐지(retired)** 확인 — 서버리스 모델은 첫 호출 시 자동 활성, Anthropic만 최초 1회 use-case 제출 필요 → **제출 완료, 플레이그라운드에서 Claude Haiku 4.5 서울 응답 확인**(manual-infra §4 신설). **② model ID:** 콘솔 '추론 프로파일'에서 Global inference profile ID 확인 → `bedrock_planner.py` `DEFAULT_MODEL_ID` 반영(bare name은 404). **③ 선정 근거(진우 질문 대응):** Bedrock엔 GPT·Gemini 없음(타 클라우드 전용, D9 위반) → Bedrock 내 Claude 선정 = tool use 신뢰성·한국어·Converse 성숙도, **비용 대안=Amazon Nova Lite(`--model` A/B 가능)**. project-draft §15에 근거 명문화. **남은 것:** infra/slice apply + `run_real` 실 end-to-end 검증(Phase1 마무리). **`[PULL 필요]`**
- **2026-07-02 / 진우 / `scanners/ciem/` 폴더 신설 확정** — CIEM 스캐너(Prowler entra_id_* → 계약⑤ 봉투화)를 `workload/`와 도메인 분리해 `ciem/` 별도 폴더로 구성. `scanners/` = cspm(준형) / workload(진우) / ciem(진우) 3폴더 확정. 문서 반영(CLAUDE §4·§7.1, scanners/README, project-draft). **`[PULL 필요]`**
- **2026-07-02 / 진우 / `rag/retrieval` 검색·답변 구현 완료** — `RAGRetriever`(mock=control_id 직접 매핑 / real=pgvector cosine) + `RAGAnswerGenerator`(mock=템플릿 / real=Bedrock Claude Sonnet converse) + `mock_corpus.py`(14개 control 한국어 청크 24개, 계약⑥ 구조). `python -m rag.retrieval.run_demo` 골든 정합 6개 OK ✅ (control_id 정합·rag_refs 채움·설명 생성·evidence 반영·코퍼스 14종 전체 커버). 실배포 스왑 = `RAGRetriever(mock=False)` + `RAGAnswerGenerator(mock=False)` (PG_DSN + Bedrock 모델 액세스 후). **`[PULL 필요]`**
- **2026-07-02 / 진우 / `scanners/workload/trivy` 워크로드 스캐너 구현 완료** — `TrivyScanner` 클래스: `scan_image()`(trivy CLI → 계약⑤ ingest-envelope) + `scan_from_json()`(mock/CI용). `python -m scanners.workload.run_demo` 골든 정합(3 CVE → INTERNAL-VULN-KEV-001, resource_id 캐논, pillar=vuln) OK ✅. 버그 2건 발견·수정: ① normalizer `_parse_trivy` ArtifactName 태그 미제거(`shop/product:latest` → resource_id에 `:latest` 붙어 mock↔실 불일치) → 태그 제거 로직 추가. ② `scan_from_json` image 파라미터 봉투 미반영 → scan_batch_id에 이미지명 포함. 실배포 스왑 = `scan_image("ECR이미지:tag")` 호출만. **`[PULL 필요]`**
- **2026-07-02 / 준형 / ❤️ Phase1 엔진 "뇌" 구현 — Bedrock LLM tool-use 플래너(`BedrockEvidenceAgent`) + 실 slice 진입점(`engine/run_real.py`)** — 프로젝트 심장(챗봇 탈출)의 실배포판. `engine/evidence/bedrock_planner.py` 신설: **LLM이 Bedrock Converse `toolConfig`로 read-only API를 스스로 골라 호출**(규칙 `PLAN_BY_CONTROL` 대체). `EvidenceAgent`와 **동일 인터페이스**(`investigate(findings)→EvidenceOutput`)라 Orchestrator에 주입만 하면 스왑(규칙 데모 무변). **allowlist(계약④) 2중 강제**: ① toolConfig `api` enum=allowlist(LLM이 스키마 레벨에서 그 밖 선택 불가) ② `executor._check()`(실행 직전 재확인). `Orchestrator(evidence_agent=...)` 스왑 파라미터 추가. `engine/run_real.py` = `RealToolExecutor`+`BedrockEvidenceAgent` 실 vertical slice 진입점(전제·비용·model ID 404 함정 주석). **검증:** py_compile OK, 목업 데모 무회귀(골든 정합 유지), **가짜 Bedrock 클라이언트로 에이전틱 루프 오프라인 검증 OK**(toolUse 파싱→Mock 실행→toolResult 되먹임→end_turn→verdict=confirmed). **⚠️ 실 Bedrock 호출은 미검증** — apply 세션(Bedrock 액세스+서울 model ID 확정+infra/slice)에서 준형과 함께. boto3는 지연 import(미설치 환경 무영향). **`[PULL 필요]`** (진우: engine 공유 이해 — Evidence 뇌가 규칙→LLM으로 스왑됨)
- **2026-07-02 / 준형 / 🐛 진우 `hypothesis.py` cross_cloud 가설 버그 수정 + 진우 정규화부 문서 반영(README·draft·§7.1)** — **① 버그(진우 `engine/reasoning/hypothesis.py`):** `_has_cross_cloud()`가 `edge.get("edge_type")=="cross_cloud"`를 검사했는데, 계약③ 엣지는 `type`(값 `credential_theft` 등) + `cross_cloud`(불리언) 구조라 **`edge_type` 키·`"cross_cloud"` type 값이 아예 없음** → 함수가 항상 False → `_CROSS_CLOUD_HYPOTHESIS`(크로스클라우드 체인 가설)가 **한 번도 추가 안 됨**(데모 Hypothesis 출력에도 누락). 골든 검증이 가설 목록을 안 봐서 CI 통과된 채 숨어 있었음. **→ `edge.get("cross_cloud")`(불리언 플래그)로 수정** — 이제 데모에 "AWS 워크로드 침해→평문 Azure SP→Entra 장악" 크로스클라우드 체인 가설 출력, 골든 정합·case 스키마 유지(exit 0). **② 문서:** 진우 `pipeline/normalize`(정규화부) 완료를 README(역할분담·Status·폴더트리)·project-draft 상태줄·§7.1 진우 진행에 반영. **`[PULL 필요]`** (진우: reasoning 영역 코드라 수정 내용 확인 요청 — 의도한 크로스클라우드 가설이 이제 실제로 발화함)
- **2026-07-02 / 진우 / `pipeline/normalize` 정규화부 구현 완료** — `Normalizer` 클래스: ASFF(Security Hub·Macie)·prowler-json(AWS+Azure)·trivy-json → 계약① OCSF-lite finding 변환. control-catalog 역인덱스(정확+와일드카드 fnmatch), resource_id 캐논화(ARN→`cloud:type:native_id`), severity 변환, dedup(sources 누적). `python -m pipeline.normalize.run_demo` 골든 control_id 7종 전부 매핑 OK ✅. 실배포 스왑 = Lambda 핸들러에서 `Normalizer().normalize(envelope)` 호출만. **`[PULL 필요]`**
- **2026-07-02 / 준형 / 진우 App Reg 작업 파악 + mock↔real 정합 리마인더 명시(§7.1 진우 진행 갱신)** — 진우 `ef0ad44`(App Reg 3종) 검토: 보안 OK(시크릿 값 미커밋·appId/tenantId만=식별자), Prowler SP 키리스(D4). **§7.1 진우 진행에 App Reg 3종 ✅ 반영.** manual-infra §3.6.4 신설로 **실전환 시 맞출 2건** 못박음: ① mock placeholder GUID → 실 appId 스왑(실 finding 흐를 때) ② **f16(무만료 SP cred) 노드 매핑 — contracts는 n4(SP)인데 실물은 무만료 시크릿을 overpriv-app(n5쪽)에 붙임 → 진우와 n4/n5 확정 필요.** 지금은 mock이라 정상, 리마인더 목적. **`[PULL 필요]`** (진우: f16 매핑 확인 요청)
- **2026-07-02 / 진우 / Azure App Registration 3종 + Federated Credential 완료** — ① `cnapp-agentic-overpriv-app`(`Directory.ReadWrite.All`, 24개월 시크릿 — f8·f16 소스) ② `cnapp-agentic-order-sp`(`Directory.Read.All`+`Application.Read.All`, 6개월 시크릿 — f5 평문 노출 예정) ③ `cnapp-agentic-prowler-sp`(`Directory.Read.All`+`Policy.Read.All`+`AuditLog.Read.All`, 시크릿 없음 — GitHub OIDC Federated Credential). `manual-infra.md §3.6` 갱신. SSO용 App Reg는 `infra/console` apply 후. **`[PULL 필요]`**
- **2026-07-01 / 준형 / `docs/cost-strategy.md` 신설(포폴 셀링포인트)** — 비용 최적화를 "돈 없어서 감수"가 아니라 **의도적 엔지니어링 트레이드오프**로 정리한 결정 원장. mock-first(빌드 대부분 AWS $0)·apply→destroy 규율·경량 대체(NAT Instance/RDS t3.micro/pgvector)·Bedrock 모델 티어링+Triage 게이트 비용통제 내재화·Azure 평가판+신원만·거버넌스(Orgs/IC/CT) 회피 — 각 결정에 절감+트레이드오프, **정직한 "포기한 것" 절**(HA 없음·mock≠실검증 등), 비용 order-of-magnitude 표. README docs 안내·폴더트리에 링크. (엔진 실 tool-use vertical slice 실행 플랜은 준형 요청으로 클로드 메모리에만 보관 — 나중 apply 시 체크리스트.) **`[PULL 필요]`**
- **2026-07-01 / 준형 / attackpath 문서 정합 스윕 + `attackpath/README.md` 신설 + project-draft R5 오류 수정** — `attackpath/README.md`(영역 전체 워크스루: R1~R5 표·그래프 모델·2-pass·목업스왑) 신설. 전 md 검토 중 **project-draft §4.4 R5 오류 발견**: `identity_takeover` 엣지를 `cross_cloud:true`로 적었으나 n4(azure)→n5(azure)는 **Azure 내부라 false**(경계 횡단은 R3 `credential_theft` AWS→Azure 하나뿐). 실구현·`mock-attack-paths.json`과 정합하게 수정하고, 새 `validate_graph`가 이 불변식을 강제함을 명시. target-app-design §3에 산문 4단계↔그래프 5노드(n1~n5) 매핑 추가. console·contracts/README·manual-infra는 정합 확인(무변). **`[PULL 필요]`**
- **2026-07-01 / 준형 / `attackpath/model` 그래프 데이터 모델 구현 + correlation 리팩터(진우 파일 변경)** — 진우가 비워둔 `attackpath/model/`(제 몫)에 **계약③ SSOT 그래프 모델** 구현: `Node`·`Edge`·`AttackPathGraph`(add_node/add_edge/apply_chain_severity/to_dict) + `validate_graph`(cloud·pillar·edge type enum, 엣지가 실존 노드 참조, **cross_cloud 플래그↔실제 경계횡단 일치** 불변식). **correlation.py 리팩터**: 인라인 `_node`/`_edge`/`_CRITICAL_CHAIN_LEN` 제거 → 모델 사용, `correlate()`가 `validate_graph` 통과 후 `to_dict()` 반환(진우 R1~R5 로직 무변). **⚠️ 진우 correlation 출력 변경**: 계약③ `additionalProperties:false` 위반이던 `finding_ids` 키를 출력 dict에서 제거(모델이 backfill용 provenance로 보유·직렬화 제외) → 출력이 계약③에 깨끗하게 정합. `attackpath/run_demo.py` 윈도우 UTF-8 수정(엔진과 동일 버그). 골든 정합 5노드/4엣지/backfill 8건 OK ✅ 유지. **`[PULL 필요]`**
- **2026-07-01 / 진우 / `attackpath/correlation` 구현 — R1~R5 상관 규칙 완성** — `CorrelationEngine.correlate(findings)`: R1(KEV+open SG) → R2(과도 IRSA) → R3(평문 Azure cred+SP) → R4(공개 S3+PII 동일 버킷) → R5(Entra 과도권한 App) 체인 발화 시 attack-path 그래프(계약③) 생성. 체인 길이 ≥3 → severity Critical 격상. 2-pass backfill(attack_path_id 역주입). 골든 정합(5노드 AWS3+Azure2 / 4엣지 / cross_cloud credential_theft / backfill 8건) OK ✅. 실배포 스왑 = findings 파라미터를 RDS 쿼리로 교체만. **`[PULL 필요]`**
- **2026-07-01 / 준형 / infra/console·console 앱 소유 stale 정정 → 준형** — project-draft §4.6 terraform 소유표가 아직 "infra/console·console 앱 = 진우"로 남아 있던 것(앱 2개 준형 재배정 때 §5만 고치고 §4.6 누락). **준형으로 정정**(§4.6 표·코드블록·apps 행·apply 불릿 + CLAUDE §4 트리). 원칙: **앱 관련 인프라(shared·target·console)는 준형**(앱 2개·CI/CD·공유인프라 주도), 콘솔 SSO는 준형 Cognito ↔ **진우 Entra App Reg**(manual-infra §3) 연동 — 진우는 IdP(Entra)만. **`[PULL 필요]`**
- **2026-07-01 / 진우 / engine 재구성 완료 — hypothesis/+orchestrator/ → reasoning/ 1폴더로 합침** — 준형 폴더 규칙(소유자별 2개 최소화) 반영: `hypothesis/hypothesis.py` + `orchestrator/orchestrator.py`를 `reasoning/`으로 이동, import 경로 갱신(`engine.reasoning.hypothesis`, `engine.reasoning.orchestrator`), run_demo.py import 갱신(`engine.reasoning.orchestrator`). 빈 캐시 폴더(triage/ 포함) 전부 삭제. 재실행 exit=0 확인. `engine/` = `core/`(공유) + `evidence/`(준형) + `reasoning/`(진우) 3폴더 확정. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 폴더 규칙 명문화(하위 최소화·소유자별 2개) + engine 재구성** — 누락됐던 규칙 복원: **하위 폴더는 소유자별 2개(a=준형·b=진우)로 최소화, 공유 코드만 예외 폴더**(사람 이름 폴더는 여전히 금지 — 이름은 컴포넌트로). project-draft §4.6·CLAUDE §4에 반영. engine을 `core`(공유)+`triage`+`evidence`(과다 분할)에서 **`core`(공유) + `evidence`(준형: triage.py·evidence.py)** 2폴더로 합침. 진우 몫 → reasoning/ 1폴더 완료(위 항목). run_demo 재실행 통과. **`[PULL 필요]`**
- **2026-07-01 / 진우 / 엔진 Hypothesis·Reasoning·Orchestrator 구현 — 전체 루프 완성** — `HypothesisAgent`(control_id 기반 가설 자동 생성 + 크로스클라우드 체인 가설), `ReasoningAgent`(evidence → 한국어 내러티브·CRITICAL 위험수준·권고사항), `Orchestrator`(Triage→Hypothesis→Evidence→Reasoning 단일 루프), `case.set_reasoning()` 추가. `run_demo.py` Orchestrator 기반으로 단순화(GOLDEN_HYPOTHESES 하드코딩 제거). 실행: 20건→12 escalate, 툴 4회, confirmed, CRITICAL, stage=reasoning, 골든 정합 OK ✅. 실배포 스왑 = executor·LLM 플래너 교체만. **`[PULL 필요]`**
- **2026-07-01 / 진우 / Azure/Entra ID 초기 설정 완료 — `cnappagentic.onmicrosoft.com` 테넌트·Teams 웹훅** — M365 Business 체험으로 `cnappagentic.onmicrosoft.com` 데모 전용 테넌트 생성. 관리자 계정(jw_kim·jh_lee Global Admin), 보안 그룹(cnapp-viewer·cnapp-approver), 데모 계정(viewer@·approver@) 생성 완료. Teams `cnapp-agentic` 워크스페이스·`cnapp-alerts` 채널·Workflows 웹훅 URL 발급. `manual-infra.md §3` 갱신. 미착수: App Registration(SSO·CIEM)·SP·Defender for Cloud. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 엔진 Evidence·Triage 목업 능동조사 구현(`engine/`) — 데모 심장 1차** — `python -m engine.run_demo`로 **트리아지 게이트(20→12 승급, 비용통제) → Evidence가 계약④ allowlist 강제하며 read-only 툴 4회 자가 호출(챗봇 탈출) → confirmed 판정 → case(계약⑦) 검증**까지 재현. **MockToolExecutor·규칙 플래너**라 `RealToolExecutor`(boto3)·Bedrock LLM으로 갈아끼우면 실AWS 전환(Evidence/Triage 로직 무변 — 계약이 SSOT라 스왑 매끄러움). 담당: 준형=Triage·Evidence / 진우=Hypothesis·Reasoning. 독립 리뷰 반영(타임스탬프 timedelta·검증 강화·가드). **`[PULL 필요]`**
- **2026-07-01 / 준형 / 진행 관리 단일 지점 = §7.1 확정 + 하이브리드 진행 원칙 + README 역할분담 순서·진행상황** — §7.1을 낡은 "다음 착수 계획"에서 **"현황 + 다음 할 일"** 트래커로 갈아엎음("지금 어디까지/다음 뭐"를 여기서 관리, 큰 그림은 README). §0에 규칙 6(진척 시 §7.1 갱신) 추가. **진행 원칙 = 하이브리드**: 빌드 순서는 인프라 먼저지만 **목업 우선으로 논리 병렬** + **리스크 큰 실검증(엔진 실 tool-use·스캐너 1개→실 finding)은 실클라우드 조기 검증(apply→테스트→destroy 사이클)** (free credit 감안, 상시 방치 금지). README **역할 분담을 진행 순서(①앱&환경 → ⑦attack-path)로 재배치 + 진행상황 컬럼** 추가(①앱&환경: 준형 앱2개 / 진우 M365·Entra 테넌트). **`[PULL 필요]`**
- **2026-07-01 / 진우 / Cognito SSO 설계 검증 3건 수정 반영** — ① `authenticate-oidc` → `authenticate-cognito` 전면 교체(project-draft §10·아키텍처 다이어그램, console-app-design §4·§7·§15 전 위치 — Cognito 전용 액션이 더 단순하고 클라이언트 시크릿 불필요). ② **Identity Pool 제거** — 프론트가 AWS를 직접 호출하지 않으므로 User Pool만으로 충분, infra/console에서 만들 필요 없음. ③ **console-app-design §7 그룹 클레임 매핑 경로 추가** — Entra SAML attribute→Cognito `custom:groups`→`x-amzn-oidc-data` JWT→Lambda viewer/approver 분기. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 타깃 앱(`apps/target`) + `infra/target` 결함 IaC 구현 + 콘솔 스텁 4화면 완성 — 독립 리뷰 2회 반영** — **타깃 앱:** member=**Python/FastAPI 확정**(§7 피드백 닫음, `apps/target/member` REST + **PII seeder**=faker 한국형 합성 rrn→S3, Macie 미끼) / product(f1 KEV 이미지·f2 privileged) / order(f5 평문 Azure SP=가짜값) k8s 매니페스트 + namespace `shop`. **`infra/target`:** 결함 IaC 토글(`enable_s3_public` f6·`enable_open_sg` f3·`enable_overpriv_irsa` f4, 기본 off), remote_state로 infra/shared OIDC 참조, **IRSA `:aud`+`:sub` 역할별 고정**. 리뷰 반영: infra/shared에 `eks_oidc_provider`(URL) 출력 추가(target 참조 정합), IRSA sub 조건, botocore ClientError. **콘솔 스텁:** Login(SSO 흐름·역할선택)·Audit(골든 타임라인 12건·필터·불변)·Compliance(ISMS-P↔control 매핑·충족률)·Remediation(실 finding 연동·approver 게이트). **모든 시크릿·PII는 가짜**(격리 데모 전제). py_compile·terraform fmt·tsc·vite build 전부 통과. **`[PULL 필요]`**
- **2026-07-01 / 준형 / `apps/console` 스캐폴드 착수 — 관제 앱 골격(목업 우선, 빌드 통과)** — §15 청사진대로 **Vite+React+TS+Tailwind+TanStack+React Flow+Recharts+MSW** 스캐폴드. **contracts가 타입 SSOT**(types.ts 손번역 + `gen-types.mjs` 재생성), **MSW handlers가 `@contracts` alias로 `mock-*.json` 직접 서빙**(vite `fs.allow`=repoRoot) → **백엔드·AWS 0으로 전 화면 동작**. 화면 8종(핵심 4=Dashboard·Findings·**FindingDetail[Evidence 탭=능동조사 UC0]**·AttackPath[React Flow AWS/Azure 레인] + 스텁 4). API 표면(§15.2)·화면↔mock(§15.4) 배선. `npm i`(252pkg)·`msw init`·`tsc --noEmit`·`vite build`(499 모듈) 전부 통과. 실행: `apps/console`에서 `npm run dev`. 다음 = 실 화면 다듬기·나머지 화면·(엔진 나오면) MSW off 스왑. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 진우 문서 검증 6건 총괄 반영 — 전 md stale 스윕 + README 역할분담 신설(카탈로그 14종)** — ① **CLAUDE §5 앱 분담 stale**(진우 0b1d493 병합 때 옛 행으로 되돌아감) → "타깃+관제 2개 모두 준형 전담"으로 정정. ② **project-draft §4.4.1(b) 카탈로그 표** 누락분 4행 추가(ENTRA-SP-CRED·ENTRA-INSECURE-CFG·ECR-SCAN + 신규 S3-LOGGING) → **13→14종**, README·contracts/README 계수 일괄 갱신. ③ **console §3·§5.1** "D3/Recharts" → **React Flow**(§15.1 확정). ④ **f15 title↔control 모순**(NOENCRYPT인데 source는 버저닝) → 신규 `INTERNAL-S3-LOGGING-DISABLED-001`(cspm) 추가 후 재매핑. ⑤ **§15 모델 ID**: `claude-haiku-4-5`는 Anthropic 별칭·**Bedrock invoke ID 아님(404)** → Bedrock 형식/서울 inference profile 구현 시 확정 주석. ⑥ **R1 "同 resource" 모호**(KEV 파드 vs open SG는 resource_id 다름) → "워크로드↔부착 SG 토폴로지 인접"으로 완화 + 파드→ENI→SG 매핑·mock 선언 주석. **README에 "역할 분담(2인)" 섹션 신설**("관제 앱"=준형 / "운영 관제"=진우 구분 명시). validate 통과(14종). **`[PULL 필요]`**
- **2026-07-01 / 준형 / contracts f16·f12 객관 기준 최종 확정(진우 합의) — 카탈로그 13종 복구** — ① **f16** → `OVERPRIV-APP-001`(의미 느슨: "과도권한 앱"인데 finding은 "SP 자격 무만료")을 `INTERNAL-ENTRA-SP-CRED-001`(ciem, title 정확 일치)로 재매핑 + **카탈로그에 SP-CRED-001 재추가**(11→13, 병합 중 유실분 복구). ② **f12/ECR** → pillar `vuln`→`cspm`(control도): "scan-on-push 비활성"은 CVE가 아니라 **미스컨피그**이고 소스가 전부 posture(`securityhub:ECR.1`·`config`·`prowler:ecr_*_scan_*`)라 cspm이 객관 — vuln 기둥은 실 CVE(Inspector/Trivy) 전용으로 유지. validate 통과. **원칙 = "기둥(pillar)은 finding을 만드는 소스의 성격으로 판정"**(처음부터 일관 적용). **`[PULL 필요]`**
- **2026-07-01 / 준형 / 진우 커밋(0b1d493) contracts 리그레션 2건 수정 — main RED→GREEN** — pull 후 `validate.py` 2건 FAIL 발견: ① **f5** `resource_id`가 `aws:eks_pod:shop/order`로 리버트돼 `resource_type`(secret_plaintext)와 assert(b) 불일치 → 다시 `aws:secret_plaintext:shop/order/AZURE_SP_CRED`로 복원(c463054 교정본). ② **f17**이 `ENTRA-RISKY-CONSENT-001`(ciem)로 재매핑됐으나 pillar `cspm` 유지 → assert(a) 위반. **설계 충실**(Azure Defender secure-score=cspm 축) 방향으로 `INTERNAL-ENTRA-INSECURE-CFG-001`(cspm) 카탈로그 재추가 후 f17 재매핑. validate 통과. ※진우의 **f16→OVERPRIV-APP-001 재매핑**은 CI-green이나 SP-cred-no-expiry↔over-priv-app 의미가 느슨 — **진우 검토 요청**. **`[PULL 필요]`**
- **2026-07-01 / 준형 / console 외부 리뷰 반영 — Evidence 탭 소스 이중성 해소 + 스택 확정** — UC0(Evidence·판정)=**case 단위**, UC1(설명)=**finding 단위**로 확정: `finding_explanations`에서 `evidence_json` 제거·`case_id` FK 추가, **`cases` 테이블 신설**, 상세는 `finding_id∈case.finding_ids` 조인(§5·§6·§15). `ai_status`≠done placeholder 규칙 명시(AI 죽어도 finding 표시). §15 스택 **확정**: attack-path=React Flow·목업=MSW·console-backend=TypeScript + **폴리글랏(console=TS / engine·pipeline=Python)**. → 이 커밋 후 apps/console 스캐폴딩 착수 OK. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 앱 2개 구현 청사진 신설(피드백→코딩 흐름)** — 코딩 전에 "어떻게 만들지"를 설계 문서에 박음: **console §15**(Vite+React+TS·TanStack·Tailwind·React Flow·MSW 목업·contracts→TS 타입, API 표면, 폴더구조, 화면↔mock 1:1, 데모 동선) + **target §7**(retail-store fork+커스텀 member, 취약 이미지, 결함 f1~f9↔IaC 토글 매핑, 폴더구조, 배포·회귀). 각 절에 피드백 요청 포인트 명시 → 리뷰 후 착수. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 프로젝트 명칭 확정 → §24 전부 닫힘** — 명칭 = "에이전틱 AI 기반 멀티클라우드 CNAPP 보안 플랫폼"(잠정), 레포 = `cnapp-agentic`. **열린 설계 미확정 0건**(UC5 PDF는 console 보너스).
- **2026-07-01 / 준형 / target-app-design §2.1을 "할 일→완료"로 정정 + §2.0 SP행 추가** — §2.1 제목·본문을 *완료형*("validate.py가 강제")으로 교체(이미 c463054에서 처리된 걸 미완 TODO로 오해 방지). §2.0 표 f5 행을 실제 캐논 resource_id로·골든 편입된 f16(SP, 노드 n4) 행 추가(골든 10건). §6 결함 메모의 "AWSGoat 참고"를 §1.1(AWSGoat 미사용)과 일관되게 TerraGoat/CloudGoat/EKS Goat로 정정. (문서 정합만, 코드 변경 없음)
- **2026-07-01 / 준형 / contracts 계약 정합 4건 수정(target-app-design §2.1 닫음) + validate.py CI 게이트** — ① f5 `resource_id`를 캐논 위반(`aws:eks_pod:…`+type secret_plaintext) → `aws:secret_plaintext:shop/order/AZURE_SP_CRED`로 교정(4.4.1a 예시도 갱신). ② 의미 안 맞는 id 재사용 해소 — 신규 control 3종(`INTERNAL-ECR-SCAN-DISABLED-001`·`INTERNAL-ENTRA-SP-CRED-001`·`INTERNAL-ENTRA-INSECURE-CFG-001`)을 카탈로그에 추가하고 f12/f16/f17 재배정. ③ **f16(Azure SP)을 골든 경로에 편입** — attack-path 노드 n4에 대응 finding이 없던 빈틈을 메움(assert c 충족). ④ `contracts/validate.py`(4-assert: pillar·resource_id↔type·node-finding·dedup_key) + `.github/workflows/contracts-validate.yml` CI 게이트 신설. 검증 통과. **`[PULL 필요]`**
- **2026-07-01 / 준형 / 타깃 앱 기능 베이스 확정(retail-store-sample-app) + 정답지 contracts 매핑** — target-app-design §1.1 신설: 기능 껍데기 = **AWS retail-store-sample-app**(catalog·orders 2서비스 + 커스텀 member), AWSGoat 미사용·goat류는 패턴 참고만. §2.0 **정답지=contracts 매핑표**(골든 9건↔리소스·control_id), §2.1 **계약 정합 체크리스트 4건**(f5 resource_id↔type 불일치, control 3종 신규 추가, 검증기 4-assert CI 게이트화) — *이건 contracts 구현 전 고칠 후속*. **`[PULL 필요]`**
- **2026-07-01 / 준형 / `infra/shared` 스캐폴드 착수(레이어드 0번)** — VPC(NAT Gateway 끔 + S3·DynamoDB Gateway Endpoint)·**raw NAT Instance**(t4g.nano, fck-nat AMI)·EKS(spot t3.small·scale 0~2·IRSA·access entry)·ECR 4종·**RDS PostgreSQL t3.micro+pgvector**(private·Secrets Manager)·**GitHub OIDC**(키 없음)·**Evidence read-only 정책**(contracts/evidence-allowlist.json과 1:1)·Bedrock invoke 정책. `terraform fmt`·`validate` 통과. **apply는 게이트 후**(state 버킷 부트스트랩·Bedrock 모델 액세스·TODO 마커·fck-nat AMI 계정 검증). **`[PULL 필요]`**
- **2026-07-01 / 진우 / project-draft §4.4 2-pass 트리거 이벤트 연쇄 확정** — attack-path Lambda 완료→Triage Lambda 기동 사이 누락 이벤트 발견·보완: `cnapp.attackpath.correlation.completed` 이벤트 명시(DB upsert 완료 후 발행 → Triage 구독, `attack_path_id!=null` 조건 유효성 보장). **`[PULL 필요]`**
- **2026-06-30 / 준형 / `contracts/` 졸업 — 계약 7종 *.json + control-catalog + mock 데이터 생성** — 산문(4.4/4.4.1) → 실제 JSON Schema(draft 2020-12) 졸업: `finding`·`case`·`attack-path`·`evidence-allowlist`·`ingest-envelope`·`rag-chunk` 스키마 + `control-catalog.json`(INTERNAL control 10종). 목업: `mock-findings.json`(20건, 골든 9건 체이닝, open/remediated/suppressed 혼합)·`mock-attack-paths.json`(골든 1경로, 4 엣지 type·cross_cloud)·`mock-cases.json`(엔진 능동조사 한 장면, Evidence 4회). JSON 유효성·교차참조 정합 검증 통과. **이제 양쪽 병렬 가능** — 진우는 이 목업으로 콘솔, 준형은 infra/shared. **`[PULL 필요]`**
- **2026-06-30 / 준형 / 검증 후속 전부 확정 + README 폴더구조 갱신** — 진우 "피드백 없음" → 대기 항목 확정: **4.4.1 정규화 규칙 3종**(`resource_id` 캐논 표·INTERNAL `control_id` 카탈로그·remediated source별 스코프) 신설하고 §24 3건 닫음, attack-path **2-pass**·**핵심 영역 분담** 전부 `확정`으로 전환(§5/4.1). README를 실제 폴더구조(contracts·scanners·pipeline·rag·attackpath·troubleshooting)로 갱신 + docs 안내 dead-link(infra-status→manual-infra) 수정. **`[PULL 필요]`**
- **2026-06-30 / 준형 / CLAUDE.md §6·§7 복구(머지 유실) + §5 손상 수정 + §7.1 Next Up 계획 추가** — `HEAD~4` 머지에서 §6 협업규칙·§7 작업기준이 날아가고 §5 "시간 컷" 불릿이 깨진 채 잘렸던 것 복원(§0의 §6 dead-link 해소). §7.1에 합의된 다음 착수 계획 신설: **착수=둘 동시(contracts+infra 병렬), 데모 컷=절대사수 풀세트, Azure 격리 테넌트=진우 확보.** **`[PULL 필요]`**
- **2026-06-30 / 준형 / attack-path↔Evidence 순서 2-pass로 통일 + 검증 피드백 3건 미확정 등록** — UC0(§9)와 진우의 트리거/게이트가 상충(Evidence→상관 vs 상관→Evidence)하던 걸 **2-pass**(① 1차 규칙상관이 `attack_path_id` 부여 → ② 트리아지 게이트 → ③ Evidence 증거수집 → ④ 2차 확정·내러티브)로 통일. 추가 검증에서 ①`resource_id` 정규화 규칙 ②INTERNAL `control_id` 카탈로그·매핑 ③remediated 스코프(스캐너별) 미정 발견 → §24 등록(contracts 졸업 시 진우와 확정). `severity_id` 주석 오류("OCSF식" → 내부 컨벤션, 실제 OCSF와 반대) 정정. **`[PULL 필요]`**
- **2026-06-30 / 진우 / 설계 미확정 9개 항목 전부 닫음** — ① D4 Azure 키리스 인증(Entra Workload Identity Federation) 확정 ② 트리아지 게이트 임계값(`severity_id≤2` OR `attack_path_id!=null`) ③ 자동 조치 카탈로그 MVP 3종(S3 block·SG 제거·IAM diff) ④ 임베딩 모델 Titan v2 서울 확인·fallback(Cohere Embed Multilingual v3) ⑤ RDS 자동재시작 방지(EventBridge Scheduler+Lambda) ⑥ Prowler Azure cron(`0 17 * * *`) ⑦ attack-path 트리거(배치 완료 EventBridge 이벤트) ⑧ §24 타깃앱 세부·자동조치 체크박스 ⑨ console §14 동기화. project-draft·console-app-design·manual-infra 동기화. **`[PULL 필요]`**
- **2026-06-30 / 준형 / project-draft 17~24번 섹션 복구(유실 사고 수정)** — `cb2c55a`(이음새 계약 커밋)에서 거버넌스(17)·Shift-Left(18)·테스트(19)·KPI(20)·로드맵(21)·비용 가드레일(22)·확장(23)·미확정(24)이 실수로 삭제됐던 것을 `53f8bd6`(v5.4)에서 추출해 복원. console 설계서가 참조하던 17/19/21/24 dead-link 해소. **`[PULL 필요]`**
- **2026-06-30 / 준형 / 작업 로그(troubleshooting.md) 신설 + 레포 구조 트리 갱신** — 루트에 **`troubleshooting.md`** 생성: 트러블슈팅 + 진행 로그를 **중앙 1개 파일**에 `[영역]` 태그로 한 줄씩(영역별 파일 금지 — 스프롤·교차패턴 손실 방지). 작업규칙 5번에 기입 형식 명시. CLAUDE 4번 폴더트리를 실제 구조(contracts·scanners·pipeline·rag·attackpath·manual-infra 포함)로 갱신, project-draft 4.6과 정합. **`[PULL 필요]`**
### 변경 로그 아카이브

> 15개 초과 항목을 아래로 이동.

- **2026-06-30 / 준형 / 이음새 계약 3종 추가 + terraform 레이어링·폴더소유 확정(4.6)** — 반반 분담으로 드러난 이음새를 계약으로 박음: **⑤ 수집 봉투** **⑥ 임베딩 모델+rag_chunk** **⑦ 엔진 case 핸드오프**(트리아지 게이트) + **attack-path 상관규칙 R1~R5**. **4.6 신설**: terraform=레이어드, 폴더=컴포넌트, 공유편집 4파일, CI apply 자동화.
- **2026-06-30 / 진우 / 비용 최적화 + 설계 잔여 갭 해소** — Aurora→RDS t3.micro, NAT Instance+Gateway Endpoint, Prowler Azure 진입, Bedrock 모델 배정(Haiku/Sonnet), CIEM 분담 독립 행, Entra CIEM RAG 룰북.
- **2026-06-30 / 준형 / 공통 계약 4종 초안 확정 + 미확정 2건 닫음** — 4.4 합의 계약(OCSF-lite·엔진 입출력·attack-path·Evidence allowlist), 레포=모노레포 확정, attack-path=커스텀 엔진 확정.
- **2026-06-30 / 준형 / 작업 분담 표 통합 + 실명 반영** — 트랙1·2 → 준형·진우, 상시 협의 문서화.
- **2026-06-30 / 준형 / 작업 분담 균형안 도입** — 영역 반반, 의존성 순서, 합의 인터페이스 2개, 시간 컷 우선순위 명시.
- **2026-06-30 / 준형 / Azure 역할을 데이터→신원(Entra ID) 중심으로 전면 전환** — 골든 시나리오 크로스클라우드 신원 탈취 경로로 교체, 설계서 3종·README 동기화.

---

## 1. 한 줄 요약

멀티클라우드(**AWS = 워크로드의 주인 / Azure = 신원의 주인(Entra ID)**) 환경의 설정(Posture)부터 워크로드·IaC 코드까지 **code-to-cloud 보안 위험을 점검·통합·상관분석**하고, 그 위에 **에이전틱 AI(Bedrock 멀티에이전트 + RAG)**로 발견 항목을 설명·우선순위화·자동 개선하는 **CNAPP형 보안 플랫폼**. (데이터/회원 PII는 AWS S3 전용 — Azure에는 데이터를 두지 않는다.)

---

## 2. 핵심 키워드

- **Multi-Cloud** — AWS(워크로드의 주인) + Azure(신원의 주인, Entra ID). *각 클라우드 장점대로 — 컨테이너 워크로드는 AWS, Microsoft 생태계 신원은 Azure.* 흩어진 보안 상태를 OCSF로 정규화해 단일 뷰로 통합하고, **AWS 워크로드 침해가 Azure 신원(Entra) 장악으로 번지는 크로스클라우드 attack-path**까지 추적. (데이터는 AWS S3 전용, 중복 저장은 명분이 약해 Azure는 신원만.)
- **CNAPP** — CSPM·CIEM·취약점·KSPM·데이터(DSPM 맛)·attack-path 6기둥. CSPM을 넓히지 않고 **워크로드(코드 방향)로 깊게** 심화. SOC(실시간 위협탐지/대응)는 범위 밖(확장 훅만).
- **Agentic** — 챗봇이 아니라 에이전트가 능동적으로 일한다. **챗봇 탈출의 단일 기준 = LLM이 read-only API를 스스로 호출해 증거를 모으는가(tool use).** Orchestrator → Triage → Hypothesis → Evidence → Reasoning 루프(가설→증거→판정).

---

## 3. 핵심 설계 결정 (요약) ✅

상세·근거는 설계서 11번. 가장 자주 부딪히는 결정만 추림:

아래 표는 각 설계 결정과 그 요점을 정리한 것이다.

| 결정 | 요점 |
|---|---|
| **단일 계정 / 단일 리전(서울)** | Organizations·Identity Center·Control Tower **절대 안 켬**(무료 크레딧 소멸 트리거). CloudTrail/Config는 전 리전 수집. |
| **키 없는 인증** | CI/CD는 GitHub OIDC→IAM Role, 파드는 IRSA/Pod Identity. 장기 자격증명 미사용. |
| **EKS + ArgoCD GitOps** | 워크로드 운영·배포의 핵심 축. |
| **앱 2개** | ① 취약 타깃 앱(findings 소스) ② 관제 앱(HALO의 NOVA 대응, 얼굴). **둘은 직접 통신 안 함 — agentless.** |
| **취약점은 IaC에 심음** | 앱 코드 버그가 아니라 인프라/설정 결함. 기능 최소, 결함 다양성 최대. |
| **Bedrock + 수동 RAG** | SageMaker 미사용. 벡터DB = **pgvector(RDS/Aurora PostgreSQL)** 확정(코퍼스 작아 OpenSearch는 오버스펙). |
| **앱 SSO = Cognito 허브** | Entra ID(IdP) ─SAML→ Cognito ─OIDC→ ALB(authenticate-cognito). 워크포스 SSO 크레딧 회피. |
| **Azure = 신원(Entra ID) 중심** | 점검 핵심은 **Entra CIEM**(과도권한 앱 등록·위험한 consent·권한상승). Security Hub는 AWS 전용, Prowler로 멀티클라우드 병행. **Azure에 데이터는 두지 않음. Defender for Cloud는 2026-07-07 범위 제외**(Azure 실 리소스 0개라 CSPM 평가 대상 없음 — 실측 확인, D11) — 콘솔 Azure 점수는 실 open findings 기반 산출로 대체. |
| **데이터는 AWS S3 전용 / Macie도 AWS 전용** | 회원 PII는 AWS S3에만 보관, 데이터 탐지(DSPM 맛)는 **Macie(AWS S3 전용)**. Azure는 데이터 저장소가 아니라 신원의 주인. |
| **골든 시나리오 = 크로스클라우드 신원 탈취** | product 취약 이미지 침투 → order 과도 IRSA + **평문 시크릿의 Azure 자격증명** → member 공개 S3로 AWS PII 탈취 → 탈취 자격증명으로 **Azure Entra ID 과도권한 앱/계정 장악**(디렉터리 전체 통제권). MVP는 분석·시각화 수준, 실제 횡단 동작은 보너스. |
| **Read-only first + HITL** | 에이전트는 기본 조회만. 변경(remediation)은 분리된 승인 경로(Step Functions) + 불변 감사로그(S3 Object Lock). |

**연결 구조(꼭 기억):** 타깃 앱은 AWS/Azure에 *배포*만 됨 → 스캐너가 계정을 **read-only로 스캔** → findings → EventBridge→SQS→Lambda → OCSF 정규화 → pgvector/엔진 → 관제 앱이 findings 저장소를 **읽어서** 표시. 타깃 앱↔관제 앱 직접 API 없음.

---

## 4. 폴더 구조 🏗️

```
cnapp-agentic/
├── CLAUDE.md                 # 작업 기준 + 협업 규칙 + 변경 로그
├── README.md
├── troubleshooting.md        # 작업 로그 (트러블슈팅 + 진행, [영역] 태그 한 줄씩)
├── .github/workflows/        # CI(준형): ci.yml — 9개 데모·run_e2e·validate 회귀 + Trivy/Checkov Shift-Left
├── contracts/                # ★공유 이음새 계약(4.4): *.json 스키마 + mock-findings.json (코딩 시 생성)
├── docs/                     # ★ 설계 SSOT (변경 시 반드시 여기 먼저 반영)
│   ├── project-draft.md      #   메인 설계서 (방향·범위·계약 4.4·구조 4.6·로드맵)
│   ├── target-app-design.md  #   타깃 앱 상세 (결함 목록·골든 attack-path)
│   ├── console-app-design.md #   관제 앱 상세 (화면·백엔드·RBAC·RAG 매핑)
│   └── manual-infra.md       #   수동 관리 리소스 현황 (계정·부트스트랩·Azure/Entra)
├── apps/
│   ├── target/               # 취약 타깃 앱 (product/order/member). 코드만
│   └── console/              # 관제 앱 (React). 코드만
├── scanners/                 # cspm(준형) / workload(진우) / ciem(진우). 코드만
├── pipeline/                 # ingest(준형) / normalize(진우). 코드만
├── engine/                   # core(공유·예외) / evidence(준형: triage·evidence) / reasoning(진우: hypothesis·reasoning)
├── rag/                      # corpus(준형) / retrieval(진우). 코드만
├── attackpath/               # model(준형) / correlation(진우). 코드만
├── gitops/                   # CD(준형): ArgoCD Application + HPA(파드층) 선언형. 노드층 Karpenter는 infra/karpenter로 이관(2026-07-03). apply는 EKS 세션
├── run_e2e.py                # 스캐너→정규화→상관→엔진→RAG end-to-end 러너(공유 데모)
└── infra/                    # Terraform — 레이어드(4.6). apply는 여기서만
    ├── deploy.ps1            #   ★레이어 순서 강제 실행기(apply 정방향·destroy 역방향, 실패 시 중단). ASCII 전용(PS5.1 cp949 함정 회피)
    ├── shared/               #   기반(준형 최초 apply): VPC·EKS·OIDC·RDS pgvector·Bedrock·ECR + Karpenter discovery 태그만
    ├── karpenter/            #   동적 노드 오토스케일러(2026-07-03 shared에서 분리): 컨트롤러 helm·IRSA·spot SQS·NodePool·EC2NodeClass. shared 직후 apply
    ├── target/               #   취약 워크로드+의도적 결함(준형, 휘발성·격리)
    ├── console/              #   ALB(authenticate-cognito)→Lambda·Cognito SSO·S3+CloudFront·조치 트리거 ✅코드
    ├── backend/              #   수집·정규화+상관·오케스트레이터 Lambda+조치 SFn·감사 (구 pipeline+engine 병합 2026-07-03) ✅코드
    ├── monitoring/           #   운영 관측(진우, shared+backend+console 다음, 마지막): Grafana IRSA·대시보드24위젯·CloudTrail 연동·Teams 알림 ✅코드
    ├── slice/                #   ★레이어 아님 = 엔진 실 tool-use 최소비용(<$1) 검증용 일회용 픽스처(Phase1 실검증)
    └── {scanners,rag,attackpath,…}/  # (미구현) 나머지 영역별 terraform(영역 주인이 apply)
```

> **레포 = 모노레포 확정**(설계서 24번). 배포는 폴더별 분리(target=EKS, console=S3+Lambda). **terraform = 레이어드(project-draft 4.6)**: `infra/shared`(기반, 준형 최초 apply) → `infra/<영역>`(영역 주인이 apply, 쪼개기 영역 단위까지). 컴포넌트 폴더(scanners·pipeline·engine·rag·attackpath)는 코드만. **폴더는 컴포넌트로 나눔(사람 이름 폴더 금지)** — 단 하위는 **소유자별 2개**(a=준형·b=진우), **개수 최소화**(공유 코드만 예외 폴더). 세부는 파일로 구분. 소유표·이음새는 4.6. 공유편집 4파일=`contracts/`·`engine/core/`·`docs/`·`CLAUDE.md`. 코딩 시 공통 계약은 `contracts/*.json`(4.4 초안 졸업). 구조를 바꿀 땐 docs에 먼저 반영.

---

## 5. 작업 분담 (균형안 — 효율용 트랙)

> **원칙:** 일을 나누되 **둘 다 상대 영역까지 완전히 이해**한다. 각 영역을 반반 갈라 양쪽이 핵심을 다 만진다. 상세·의존성·인터페이스는 설계서 4번이 SSOT.
> ⚠️ **이 분담은 상시 협의로 계속 조정되는 살아있는 문서.** 변경 시 이 표와 설계서 4번을 **함께** 갱신한다.

| 영역 | 준형 | 진우 | 상태 |
|---|---|---|---|
| 앱 | **타깃 앱(결함 심기) + 관제 앱(대시보드·시각화)** | — (앱 2개 모두 준형 전담) | 확정 |
| 토대 | CI/CD·Shift-Left·**공유인프라 주도** | 모니터링·관제·추적(Grafana·CloudTrail) | 확정 |
| 스캐너-CSPM | CSPM(Config·Prowler·Security Hub·**Macie(AWS S3)**) | 워크로드(Inspector·Trivy·kube-bench·**Defender(Azure)**) | 확정 |
| 스캐너-CIEM | IAM Access Analyzer(AWS) | Entra ID(Azure — **Prowler entra_id_\* 체크**) | 확정 |
| 수집·정규화 | 수집부(EventBridge→SQS) | 정규화부(Lambda→OCSF) | 확정 |
| 엔진(Bedrock) | **Evidence(tool use)**·Triage | Hypothesis·**Reasoning**·Orchestrator | 확정 |
| RAG | 코퍼스·임베딩·pgvector 적재 | 검색·LLM 답변 생성 | 확정 |
| attack-path | 그래프 데이터 모델 | 상관 로직·내러티브 | 확정 |

> **전 영역 확정(진우 검증 완료).** 단 살아있는 분담 — 바뀌면 이 표와 설계서 4.1을 함께 갱신.

- **의존성(병목):** 0 공유인프라(준형 최우선) → 1 앱·모니터링 ∥ → 2 스캐너 ∥ → 3 수집→정규화 → 4 RAG ∥ → 5 엔진(Evidence∥Reasoning) → 6 출력 ∥ → 7 데모 합류.
- **공통 계약 7종 + 정규화 규칙 3종 확정(project-draft 4.4·4.4.1):** 계약 ① OCSF-lite 스키마(`resource_id`·`dedup_key`·`ai_status`) ② 엔진 입출력 ③ attack-path 그래프 JSON ④ Evidence 툴 allowlist ⑤ 수집 봉투 ⑥ 임베딩 모델+rag_chunk ⑦ 엔진 case 핸드오프(트리아지 게이트) + **상관규칙 R1~R5**. 정규화 규칙 **(a) `resource_id` 캐논 (b) INTERNAL `control_id` 카탈로그 (c) remediated 스코프(source별)**. `contracts/*.json` + `mock-findings.json`(R1~R5 체이닝) + `control-catalog.json`으로 졸업 → 직렬 의존 끊고 병렬.
- **시간 컷 우선순위:** 엔진 능동조사(1) > attack-path 상관(2) > 스캐너·수집·RAG(3) > 관제앱·CI/CD·포장(4). **"AI가 스스로 증거 모아 공격경로 판단하는 한 장면"** 사수.

**공유 자산(양쪽 함께):** 수집 파이프라인(EventBridge→SQS→Lambda, OCSF 정규화), 에이전틱 엔진 코어(`engine/`), 인프라 골격(`infra/shared/`), 관제 대시보드(`apps/console/`).

---

## 6. 협업 규칙 🔒

1. **작업 시작 전 `git pull`** — 항상 최신 main을 받고 시작. 충돌 줄이기.
2. **작업 후 `commit` + `push`** — 의미 단위로 커밋하고 바로 push해 상대가 받을 수 있게. 커밋 메시지는 `타입: 내용`(예: `feat: ...`, `docs: ...`, `infra: ...`).
3. **설계 변경은 docs에 먼저 반영** — 방향·범위·구조·결정이 바뀌면 [docs/](docs/)의 해당 설계 문서(project-draft / target-app-design / console-app-design)를 **먼저** 업데이트하고, 영향이 있으면 본 CLAUDE.md도 갱신. 코드/문서 불일치를 남기지 않는다.
4. **무료 티어 가드레일 준수** — Organizations/Identity Center/Control Tower 금지. 종량제 서비스(Config·Security Hub·Inspector·Macie·Defender)는 데모 기간만 켜고 `destroy`. Budgets 알림 유지.
5. **보안 기본** — 장기 키 커밋 금지(OIDC/IRSA 사용), 시크릿은 Secrets Manager+KMS. 에이전트는 read-only first, 변경은 HITL 승인 경로로만.

---

## 7. 앞으로의 작업 기준 🗓️

- **모든 작업은 `docs/`와 이 `CLAUDE.md`를 기준으로 진행한다.** 새 기능·결정·구조 변경은 먼저 설계 문서와 정합성을 확인하고, 어긋나면 문서를 갱신한 뒤 코드를 작성한다.
- 우선순위 컷라인(설계서 21번): **① 절대사수** = CSPM 본체 + RAG 설명 + 대시보드 + Azure 통합 + Shift-Left + KSPM, **② 보너스** = 공급망 서명·attack-path 정교화·ISMS-P 리포트, **③ 확장** = CWPP 런타임·SOC.
- 미확정 항목(설계서 24번)은 결정될 때마다 docs에 반영하고 여기 요약을 갱신한다.

### 7.1 현황 + 다음 할 일 (Next Up — ★진행 관리 단일 지점. 작업 후 여기부터 갱신)

> **이 절만 보면 "지금 어디까지 / 다음 뭐"가 나온다.** 큰 그림 현황은 [README 구현 현황](../README.md) 표, 상세 작업 로그는 [troubleshooting.md](../troubleshooting.md).

**현황 + 다음 (사람별 · 2026-07-01):**

*준형*
- ✅ 한 것: 관제 앱 **8화면 목업 동작** · 타깃 앱 **member 실행 + shop 포털** · **엔진 Evidence·Triage 능동조사(목업, `python -m engine.run_demo`)** · **attack-path 그래프 데이터 모델(`attackpath/model`, correlation이 이 모델로 조립)** · 공통 계약(14 control·목업·CI) · `infra/shared`·`infra/target` 스캐폴드(apply 전) · **Phase1 배관+뇌 코드: `RealToolExecutor`(boto3 S3)·`infra/slice`·`BedrockEvidenceAgent`(Converse tool-use 플래너)·`engine/run_real.py` — 오프라인 루프 검증 OK** · **Bedrock 모델 액세스 ✅(use-case 제출 완료, Claude Haiku 4.5 서울 동작 확인) + model ID 확정(`global.anthropic.claude-haiku-4-5-20251001-v1:0`, `DEFAULT_MODEL_ID` 반영, manual-infra §4)**
- ✅ **Phase1 완료(2026-07-02) — 엔진 실 tool-use 실검증**: `infra/slice` apply → `run_real` → destroy 전 과정 실행. **실 Bedrock Claude Haiku가 스스로 `s3:GetBucketPolicy`·`GetPublicAccessBlock` 호출 → 실 S3 응답 CONFIRMED(100%)** → 즉시 destroy(버킷 삭제 확인, 비용 ~$0). 심장(챗봇 탈출)이 목업 아닌 실동작 증명.
- ✅ **Phase2 배선(2026-07-02) — end-to-end 러너 `run_e2e.py`**: 스캐너(Trivy+CSPM)→정규화→상관(attack-path)→엔진→RAG를 한 줄로 관통(exit 0). 스캐너 2개 실 정규화 관통 / 라이브 `trivy image`·실계정 스캔은 대기 / 엔진·RAG=무비용 Mock(실 tool-use는 Phase1).
- ✅ **준형 컴포넌트 half 3종 mock-first 완료(2026-07-02)**: `scanners/cspm`(SecurityHub·Macie·Prowler→계약⑤ 봉투, 골든 5종·dedup, +실 boto3/CLI 경로) · `pipeline/ingest`(EventBridge/S3 이벤트→계약⑤ 봉투→SQS, 양 입구·정규화 핸드오프) · `rag/corpus`(청크→Titan 임베딩→pgvector 적재, 계약⑥ 24청크·14 control 커버, +실 경로). 진우 half(workload·normalize·retrieval)와 **각 영역 대칭 완성**. 전 run_demo exit 0.
- ✅ **CI/CD 코드 세팅(2026-07-02)**: **CI** `.github/workflows/ci.yml`(10개 데모·run_e2e·validate 하드 게이트 + Trivy/Checkov Shift-Left report, $0) · **CD** `gitops/`(ArgoCD Application `app-target` + Karpenter NodePool/EC2NodeClass + HPA 선언형). 오토스케일=Karpenter(spot·consolidation)+HPA로 확정(CAS 대체). 근거·개념 = cost-strategy §2.7·gitops/README.
- ✅ **console-backend(2026-07-02)**: `apps/console-backend`(ALB→Lambda, TS) — 프론트 API 표면(§15.2)을 mock-first로 서빙(MSW 서버판). tsc 통과 + 스모크(findings·detail 조인·attack-path·scores·**RBAC viewer403/approver200**·404) OK. `USE_MOCK=false`+`PG_DSN`로 pgvector 스왑. **콘솔 MSW→실 API 전환 준비 완료**.
- ✅ **infra terraform 3층 완료(2026-07-02)**: `infra/console`(ALB authenticate-cognito→Lambda·Cognito SAML SSO·S3+CloudFront OAC·조치 StartExecution) · `infra/pipeline`(EventBridge→SQS+DLQ→ingest/normalize Lambda·VPC/RDS 배치) · `infra/engine`(상관·오케스트레이터 Lambda + shared의 evidence/bedrock 정책 attach + remediation Step Functions·격상 역할 분리). **`terraform fmt`+`init -backend=false`+`validate` 3개 다 통과.** Lambda=배포 가능 스텁+실코드 스왑 포인트(archive_file). **slice=아키텍처 레이어 아닌 저비용 픽스처로 문서에 명시**(§4.6). apply 전 TODO: console ACM 인증서·진우 Entra SAML 메타데이터·engine SM ARN 주입.
- ✅ **DB 스키마 + Lambda wrapper(2026-07-02)**: `infra/shared/db/schema.sql`(pgvector 6테이블, 진짜 공백 메움) + 핸들러 wrapper 4종(ingest·normalize·correlation·orchestrator, 기존 로직 재사용 + RDS I/O + 2-pass 발행, engine은 REAL_TOOLS로 실 Bedrock). py_compile+import OK, terraform 스왑 주석 갱신·validate 재통과.
- ✅ **보너스 조치(remediation) 실코드 + 감사 Object Lock(2026-07-02)**: `engine/remediation.py`(HITL 쓰기 경로 — S3 block·SG revoke·IAM diff, dry-run/apply + S3 Object Lock 감사 + RDS 수정→소멸 루프) + infra/engine 감사 버킷·VPC·IAM 배선. engine validate·run_e2e 무회귀.
- ✅ **infra 구조 리팩터 + 라이브 풀사이클 실검증(2026-07-03)**: 구조 결함 수정 — ① Karpenter를 `infra/karpenter` 새 레이어로 분리(shared=순수 폴대 복원) ② `infra/deploy.ps1` 순서강제 실행기 ③ deploy.ps1 cp949 인코딩 실버그 수정(ASCII 전용화) ④ gitops karpenter.yaml 중복 삭제. 이어서 **실 AWS 풀사이클 검증 완료**: apply 207개(6레이어 원샷·순서 자동) → **Karpenter 전체 수명주기 실증**(1.13.0 컨트롤러 Ready·t3.micro 스팟 ~30초 프로비저닝·consolidation 회수·ERROR 0) → 콘솔 200·대시보드 → destroy 208개·**잔존 $0**. destroy 하드닝 3건 실측 반영(SG delete 40m·NAT depends_on·ENI 가속절차). 상세는 변경 로그.
- ✅ **실전환 갭 7종 봉합(2026-07-03)**: Lambda 스텁→실코드(build_lambdas.py+psycopg2 레이어, 5함수) · CI 이미지빌드(build-images.yml, OIDC 키리스) · ACCOUNT_ID 리터럴 확정 · SSO 옵션B 프론트 OIDC(oidc.ts PKCE·/callback·Bearer) · 조치 승인 버튼 배선 · console-backend real pg(async+esbuild 번들) · RAG model ID 확정. 전 회귀 통과.
- ✅ **엔진 조사 전종 확장 + RAG 실 경로(2026-07-04)**: RealToolExecutor S3 2종→**AWS allowlist 9종**(골든 f3·f4·f6·f7 실조사, 오프라인 검증 0 fail) · RAG retriever 실검색(Titan+pgvector cosine, 진우 파일) · answer_gen 토큰 캐처 · console-backend `/chat` 실 RAG(Titan→pgvector→Sonnet). "AI 능동조사 + 지식베이스 설명"이 목업 아닌 실 배선. 전 회귀 통과.
- ✅ **라이브 대부분 완료(2026-07-06)**: 전 레이어 apply · **커스텀 도메인+ACM+SSO 실 로그인**(jh_lee@ APPROVER·실데이터 20건) · **관제앱 전 페이지 비주얼 고도화 + AI 어시스턴트 챗** · **보안 하드닝 7종**(#1~#7) · 데이터평면 관통(ingest→normalize→correlation→orchestrator 실 Bedrock)·RAG /chat·remediation · gitops(ArgoCD·member/product/order Running) 다 라이브 검증.
- ✅ **실 findings 라이브 관통(2026-07-06)**: 워크로드 스캐너 2종을 **실제로 라이브 실행** — kube-bench(실 3노드 Job, CIS 4.1.1 cluster-admin 실 탐지)·Trivy(인클러스터 ECR 스캔, 실 205 CVE) → 실 normalize Lambda → RDS → **라이브 콘솔 23건(골든 20 + 실 3)**. `INTERNAL-KSPM-CLUSTERADMIN-001` 신설·normalizer 노이즈 규칙 개선. + 전구간 TLS(#1)·CloudFront 로깅(#6) 마감 + 콘솔 `/system` AI 관측 뷰·AI 재조사 라이브 트리거 신설. 상세는 변경 로그.
- ✅ **풀사이클 재구축 + ArgoCD·타깃앱 실기동 + 실버그 봉합(2026-07-07)**: destroy했던 전 스택 재apply → **ArgoCD 최초 손 부트스트랩**(server-side)·`app-target` Synced/Healthy·**타깃 앱 member/product/order Running**(build-images.yml로 ECR push→ArgoCD 배포, CI→ECR→ArgoCD 완전 관통). **실데이터 크래시 2건 봉합**: Finding 상세 `sources` 누락 크래시 + Evidence 탭 case 필드 누락(둘 다 console-backend 실쿼리가 프론트 필드를 덜 SELECT — mock엔 안 나던 것). **PII seeder 403**(member IRSA `s3:HeadObject` 무효액션+`ListBucket` 부재) 근본수정 → Macie 미끼 적재. **Karpenter 고아노드**(destroy 실패 원인) deploy.ps1 자동 스윕 코드화. **deploy.ps1 'all'에서 monitoring 제외**(진우 별도). 관제 콘솔 사용 매뉴얼(`docs/console-manual.md`) 신설. playwright 9/9 렌더+실 API 전수 감사로 검증.
- ▶ **다음 = ① Prowler CSPM 라이브(opt-in)**: 이 머신 Python 3.8이라 CLI 불가 + 인클러스터는 광역 read IRSA(SecurityAudit) 필요라 opt-in 보류(단 실 CSPM 3건이 콘솔에 이미 유입됨 — 출처 미확정). **② Security Hub·Macie·Inspector·GuardDuty 4종**은 계정 구독제약(`SubscriptionRequiredException`)이라 데모 직전에만 켜서 검증. ③ SSO는 fresh apply마다 Cognito 풀ID 바뀌어 **Entra 식별자 수동 교체 필요**(manual-infra §5).

*진우*
- ✅ 한 것: TF state 버킷 · Cognito SSO 설계(authenticate-cognito·그룹 클레임) · 2-pass 트리거 이벤트 · Azure/Entra 테넌트 · **엔진 Hypothesis·Reasoning·Orchestrator** · **Azure App Registration 3종(과도권한 overpriv-app·order-sp·prowler-sp) + Prowler SP GitHub Federated Credential**(manual-infra §3.6) · **`pipeline/normalize` 정규화부**(ASFF·prowler-json·trivy-json → OCSF-lite, dedup)
- ✅ 워크로드 스캐너(`scanners/workload/trivy`) — Trivy 이미지 스캐너 완료(scan_image/scan_from_json, 계약⑤ 봉투화, run_demo OK)
- ✅ RAG 검색·답변(`rag/retrieval/`) — `RAGRetriever`+`RAGAnswerGenerator`+`mock_corpus`(14 control 24청크), run_demo 골든 6개 OK
- ✅ Azure CIEM 스캐너(`scanners/ciem/`) — `EntraCIEMScanner`(scan_from_json + scan_prowler는 `CSPMScanner(provider="azure")` 위임, 중복 방지) → 계약⑤ 봉투화. `run_demo`가 f8·f9·f16·f17 4종 골든(과도권한 App·위험 consent·SP 무만료 cred·설정 미스컨피그) resource_id·severity·pillar 전부 일치 확인 OK
- ✅ **App Registration(SSO) 완료(2026-07-02)** — `cnapp-agentic-console-sso` 신설, 그룹 클레임(라이선스 제약으로 "보안 그룹"+GUID 매칭), `infra/console/variables.tf`에 메타데이터 URL 반영 → 준형 apply 선행 조건 해소(manual-infra §3.6.1)
- ✅ **`infra/monitoring` 운영 관측 레이어 완성(2026-07-03)** — CLAUDE §5 "모니터링·관제·추적" 코드화. Grafana IRSA(CloudWatch read-only) + CloudWatch Dashboard **17위젯**(Lambda 6종·SQS·RDS·ALB·Step Functions·S3 감사버킷·Cognito·Bedrock 호출/에러/토큰(모델별)+비용 metric math·엔진 EMF 4종) + **CloudTrail→CloudWatch Logs 배관**(로그그룹·IAM 역할, 트레일 자체는 수동 유지) + **Teams 알림 전체 스택**(SNS·Lambda 실코드 `lambda_src/teams_notifier.py`·시크릿·알람 6종). `fmt`+`init -backend=false`+`validate` 3개 통과. CloudFront 위젯은 `infra/console`에 distribution ID output이 없어 변수 게이트(기본 비활성)로 스켈레톤만 — 준형 쪽 output 1줄 추가 대기. **`engine/reasoning/orchestrator.py`에 EMF 계측 추가**(README §2③.1 스켈레톤 그대로, `_emit_case_metrics` — Lambda 밖에선 미발행이라 run_demo/run_e2e 무회귀 확인) → 엔진 축 위젯·트리아지 알람이 이제 실데이터 받을 준비 완료. `docs/manual-infra.md`(Teams 웹훅 레닥션+연동 상태, CloudTrail 연동 안내) 동반 갱신. **`[PULL 필요]`** (준형: `infra/console/outputs.tf`에 `cloudfront_distribution_id` output 추가 요청 — CloudFront 위젯 게이트 여는 열쇠, `infra/monitoring/README.md` §12.4 참고)
- ✅ **kube-bench KSPM 스캐너 완료(2026-07-03)** — `scanners/workload/kube_bench.py`(`KubeBenchScanner`), Trivy와 동일한 이중 경로 구조: `scan_from_json(kube_bench_json, target)`(mock, EKS 불필요·지금 검증 완료) / `scan_cluster(target)`(kubectl로 aquasecurity 공식 Job 배포→JSON 로그 회수, **EKS 미apply라 미검증** — Trivy `scan_image()`와 동일한 처지). `pipeline/normalize/normalizer.py`에 `_parse_kube_bench` 신설(`source=="kube-bench"`일 때 분기, severity 없는 CIS 벤치마크라 control-catalog `severity_default` 사용). `scanners/workload/run_demo.py`에 kube-bench 데모 통합 — f2(product)·f13(member) 골든 일치 + PASS→remediated 전환까지 확인. **모델링 단순화 문서화**: kube-bench는 원래 노드/클러스터 스코프라 파드를 지목 못 함 — "이 실행이 어떤 워크로드를 대표하는가"를 호출자가 `target` 파라미터로 지정(Trivy의 `image` 파라미터와 동형), 코드 docstring에 명시. **Inspector는 재확인 결과 별도 코드 불필요**로 판명 — `scanners/cspm/cspm.py`의 `scan_securityhub()`가 ASFF 공통 파서로 이미 커버(Inspector finding도 Security Hub API로 올라옴), 계정에서 서비스만 켜면 됨. 전 회귀(run_demo 9개·run_e2e·contracts validate) 재통과.
- ✅ **AI 관측 3종 추가(2026-07-03)** — Bedrock 비용 알람(`bedrock_cost_high`)·케이스별 Bedrock 토큰 EMF(`BedrockInputTokens`/`OutputTokens`, `case.schema.json`이 원래 정의해뒀던 `model_trace[].tokens` 미사용 필드를 처음 채움)·tool별 호출 breakdown EMF(`EvidenceOutput.plan` 기존 필드 재활용). 대시보드 22→24위젯, 알람 6→7종. 상세는 변경 로그·`infra/monitoring/README.md` §15.
- ✅ **`infra/monitoring` 실 apply 완료(2026-07-03)** — 준형 4층(`shared`·`target`·`console`·`backend`) 뒤 첫 실 apply, 27개 리소스 생성(0 변경·0 삭제), 대시보드 24위젯 실제 렌더링 API로 확인. CloudTrail(`cnapp-org-trail`)에 CLI로 1회 연결(`:*` 접미사 함정 기록). `grafana_irsa_role_arn` 실값 나와서 `gitops/monitoring/kube-prometheus-stack-values.yaml`+`gitops/argocd/app-monitoring.yaml`(멀티소스 ArgoCD Application) 신설 — 그동안 "진짜 blocked"였던 항목 해소. 상세는 변경 로그·`infra/monitoring/README.md` §16.
- ✅ **`infra/monitoring` destroy 완료(2026-07-03, 당일 세션 종료)** — 오늘 세션 마무리하며 비용 규율대로 destroy(27개 리소스 전부 삭제, 0 잔존 확인). ArgoCD·app-target·app-monitoring은 세션 중 kubectl로 배포한 적이 없어 별도 클러스터 정리 불필요했음. **부수 확인**: CloudTrail(`cnapp-org-trail`)의 `CloudWatchLogsLogGroupArn`이 삭제된 로그그룹을 계속 가리키는 dangling 상태로 남음(트레일 자체는 정상 작동, 로그 전달만 조용히 실패) — 다음에 `infra/monitoring` 재apply하면 로그그룹이 같은 이름으로 재생성돼 자동 복구(고정 이름이라 별도 재연결 불필요). 준형 `shared`·`target`·`console`·`backend` 4층은 이번 세션에선 안 건드림(계속 살아있음, 준형 판단으로 destroy 여부 결정).
- ✅ **`app-monitoring` 실배포 성공 + Prometheus 미기동 버그 근본 해결(2026-07-06)** — 준형 라이브 apply 뒤 `infra/monitoring` 재apply(로그그룹 충돌 1건 `terraform import`로 해결) → Grafana·Prometheus 최초 실배포. CRD 크기 제한+operator 캐싱 버그 2단을 ArgoCD **PreSync 훅**(`gitops/monitoring/presync/`)으로 근본 해결(수동 개입 불필요, 재현 가능). Grafana 3/3·Prometheus 2/2 Running, IRSA 배선 실작동 확인. 상세는 변경 로그·troubleshooting.md·`gitops/README.md`.
- ✅ **Grafana 분야별 대시보드 4종(2026-07-06)** — AWS CloudWatch 24위젯과 1:1 대응하는 EKS/애플리케이션/인프라/AI 대시보드(31패널)를 `gitops/monitoring/dashboards/`에 신설, sidecar 자동 로드로 GitOps 관리. CloudWatch metric math 버그 3건 발견·수정 후 실데이터 검증 완료. 상세는 변경 로그·troubleshooting.md·`gitops/README.md`.
- ✅ **Grafana 커스텀 도메인(2026-07-07)** — AWS Load Balancer Controller+Ingress+ACM+Route53로 `https://grafana.cnapp-agentic.cloud` 상시 접속(port-forward 불필요). Karpenter 노드가 ASG가 아니라 NodePort+수동 Target Group 방식이 안 통해서 파드 IP 직접등록(IP 모드) 컨트롤러로 결정.
- ✅ **CloudWatch 대시보드 3종 "No data" 완전 정상화 + 골든 시나리오 실행 + Postgres 데이터소스·5번째 대시보드 + admin 비밀번호 교체(2026-07-07)** — 버그 4층(queryMode 필드 누락·metricEditorMode가 expression 쿼리 깨뜨림·Cognito FederationSuccesses 오인) 전부 수정, 실 파이프라인(ingest→normalize→correlation→orchestrator→remediation) 한 번 관통시켜 나머지 빈 패널도 채움. RDS `grafana_readonly` 계정+Postgres 데이터소스 신설(마스터 비밀번호 비노출 처리) + "데이터 파이프라인 운영 건강" 대시보드(콘솔과 안 겹치게 파이프라인 자체 건강만) + admin 비밀번호를 전용 랜덤값으로 교체. ArgoCD 컨트롤러 상태캐시 버그도 발견·해결. 상세는 변경 로그·`infra/monitoring/README.md` §3.2~3.5·7·`troubleshooting.md`.
- ✅ **Defender for Cloud 결론 확정(2026-07-07, 범위 제외)** — CloudPosture(Defender CSPM)까지 켜고 30일 무료체험 확인, "테넌트 수준 가시성" 권한(Security Reader, 루트 관리그룹)까지 부여했지만 그래도 "평가된 리소스 0"·"총 보안 점수 해당없음" — **권한 문제가 아니라 애초에 Azure 실 리소스가 0개(신원 객체뿐)라 CSPM 엔진이 평가할 대상 자체가 없는 것으로 최종 확인**(포털 인벤토리 "총 리소스 0"으로 실측). CloudPosture는 Free로 되돌림, Discovery·FoundationalCspm은 Free 옵션이 API에 없어 Standard로 남지만 리소스 0개라 실질 $0. `docs/project-draft.md`(D11·§16 Azure 자산표·아키텍처 다이어그램)에 이 결정 반영 — Azure는 이제 순수 Entra CIEM만. 콘솔 Scores의 Azure 점수는 원래 목표(가짜 74점 교체)를 준형이 이미 다른 방식(실 open findings 심각도 가중 페널티, `console-backend/data.ts getScores()`)으로 해결해뒀음 확인.
- ⚠️ **다음 작업 시작 시 준형이 반드시 할 것: `infra/shared`→`infra/backend` 순서로 `terraform apply`** — Prowler 스캔 자동화(`.github/workflows/prowler-scan.yml`) 코드는 완성됐지만 라이브 미반영 상태(진우 계정으로 apply하면 무관한 EKS access entry가 같이 destroy될 위험이 있어 의도적으로 안 함 — 준형 계정으로는 이 문제 없을 가능성 높음, 근거는 위 변경 로그 항목). apply 후 GitHub Actions `prowler-scan` 워크플로 `workflow_dispatch`로 1회 수동 실행해 S3→ingest→콘솔 반영 검증까지 필요.
- ▶ 다음: X-Ray 분산 트레이싱(보류) · `shop-target`(타깃 앱) ECR 이미지 push 상태 재확인 필요 · `infra/console` 재apply 시 ALB/Cognito/CloudFront 대시보드 값 갱신 필요(gitops/README.md 참고)
- ✅ 폴더 정리 완료: `engine/` = `core/`(공유) + `evidence/`(준형) + `reasoning/`(진우) 3폴더 확정(진우가 reasoning/로 합침 — 조율 완료)

*공통 미완:* 타깃 앱 이미지 push(ImagePullBackOff) · 스캐너 실클러스터 검증 · Teams webhook 주입

**진행 원칙 (하이브리드 — free credit 감안, 2026-07-01 확정):**
- **빌드 순서**는 인프라 먼저가 자연스러움(§5 의존성). 단 **목업 우선**으로 논리(엔진·콘솔·상관)를 인프라 없이 **병렬로 빠르게** 만든다.
- **리스크 큰 실검증은 실클라우드로 조기에** — 특히 **엔진 실 read-only API tool-use**(목업으론 완전 검증 불가)와 **스캐너 1개→실 finding 1개** 경로. 단 `apply → 테스트 → destroy` **사이클**로(상시 켜두기 X — 취약 타깃 방치·크레딧 소모 방지).
- **시간 부족 시 사수** = 엔진 능동조사 1경로(Evidence tool-use real) + 골든 attack-path 1경로 시각화(§5 시간 컷 우선순위).
